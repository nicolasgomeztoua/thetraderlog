- Fixed chart flicker by extracting annotation rendering into separate useEffect (no longer triggers full chart recreation)
- Added isNull(trades.deletedAt) soft-delete filter to discoverSymbols() in prefetch module
