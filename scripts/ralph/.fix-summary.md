- Removed unreachable branch in fetchWithRetry where both if/else paths returned true
- Added hex color regex validation on annotation color input (z.string().regex(/^#[0-9a-fA-F]{6}$/))
- Added isNull(trades.deletedAt) soft-delete filter to list, create, and clearAll ownership checks in chartAnnotations router
- Fixed annotations disappearing after interval switch by adding interval to annotations useEffect dependency array
