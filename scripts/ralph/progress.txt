# Ralph Progress Log
Started: Sun Mar 15 19:49:56 WET 2026
---

## Codebase Patterns
- No existing tsvector/tsquery usage in the codebase — this is a greenfield addition
- HTML stripping pattern exists in `getJournalAdjacency`: `content.replace(/<[^>]*>/g, " ")`
- Journal content is stored as HTML (Tiptap editor, contentFormat: "html")
- `updateContent` mutation in dailyJournal.ts is the hook point for search vector updates (upsert pattern)
- `getRange` uses `gte/lte` on normalized dates — same pattern for search date filtering
- Trade notes are on the `trades` table (`notes` field), linked by date, not by journalId
- Checklist text lives on `dailyChecklistTemplates.text` (user-level, not per-journal)
- Attachment captions are on `journalAttachments.caption`
- ilike already used in admin routers for case-insensitive search (see AGENTS.md)

## 2026-03-15 - US-000
- Audited existing utilities for full-text search
- Files reviewed: src/server/api/routers/dailyJournal.ts, src/server/db/schema.ts, src/lib/ (search utilities), all AGENTS.md files
- **Findings:**
  - No tsvector/tsquery usage anywhere in codebase
  - No search/filter utilities in src/lib/ (only ilike usage in admin routers)
  - `getRange` pattern: date normalization + gte/lte filtering — reusable for search date scoping
  - `getJournalAdjacency` has HTML stripping regex: `content.replace(/<[^>]*>/g, " ")` — reuse for search vector building
  - `updateContent` mutation is the hook point for updating search vectors on save
  - dailyJournals table columns: id, userId, date, content (HTML), contentFormat, dayStartedAt, createdAt, updatedAt
  - Related data for search: trades.notes (by date), dailyChecklistTemplates.text (user-level), journalAttachments.caption (by journalId)
- **Learnings for future iterations:**
  - Content is HTML from Tiptap — must strip tags before indexing
  - Trade notes are date-correlated, not FK'd to journals — need timezone-aware date matching to gather notes
  - Checklist templates are user-level (not per-journal) — may not be useful for per-journal search vectors
  - Use weighted tsvector: journal content (A) > trade notes (B) > captions (C)
- `db.execute` in Drizzle returns array directly, NOT `.rows` — use Drizzle query builder instead
- Biome requires alphabetically sorted imports (`isNotNull` before `isNull`)
---

## 2026-03-15 - US-001
- Added tsvector column and GIN index to dailyJournals table
- Files changed: src/server/db/schema.ts
- Added `customType` import from drizzle-orm/pg-core for tsvector type definition
- Added `searchVector` column (tsvector, nullable) to dailyJournals
- Added GIN index `daily_journal_search_vector_idx` on searchVector
- `bun run db:push` applied successfully
- **Learnings for future iterations:**
  - Drizzle ORM doesn't have a built-in tsvector type — use `customType` from drizzle-orm/pg-core
  - GIN index supports `.using("gin", column).concurrently()` syntax in Drizzle
  - The searchVector column is nullable so existing rows won't break
---

## 2026-03-15 - US-002
- Created search vector update utility at src/lib/journal/search.ts
- Files changed: src/lib/journal/search.ts (new)
- Exports: stripHtmlTags, buildSearchVectorSql, gatherJournalSearchText, updateJournalSearchVector
- Uses Drizzle query builder (not raw SQL) for gathering trade notes, captions, templates
- Uses raw SQL only for tsvector operations (setweight, to_tsvector) and UPDATE
- **Learnings for future iterations:**
  - Use Drizzle query builder for SELECT queries — db.execute returns RowList, not .rows
  - Biome enforces alphabetical import sorting
  - tsvector weight concatenation uses || operator in PostgreSQL
---

## 2026-03-15 - US-003
- Hooked search vector update into the updateContent mutation
- Files changed: src/server/api/routers/dailyJournal.ts
- Search vector updates fire asynchronously (non-blocking, best-effort with .catch())
- Covers both update and create paths in the upsert
- **Learnings for future iterations:**
  - Biome import ordering: @/lib/journal comes before @/lib/shared alphabetically
  - Fire-and-forget pattern with .catch() is appropriate for non-critical side effects
---

## 2026-03-15 - US-004
- Created backfill script at scripts/backfill-search-vectors.ts
- Files changed: scripts/backfill-search-vectors.ts (new)
- Uses standalone DB connection (not the app's cached connection)
- Reuses gatherJournalSearchText and buildSearchVectorSql from US-002
- Run with: bun run scripts/backfill-search-vectors.ts
- **Learnings for future iterations:**
  - Scripts need their own DB connection — can't import from @/server/db (requires env.js)
  - Use relative imports from scripts/ to src/ (e.g., "../src/lib/journal/search")
---
