# Ralph Progress Log
Started: Wed Feb 18 17:15:47 WET 2026
---

## Codebase Patterns
- Equity curve: use `buildEquityCurve()` from `src/lib/analytics/risk.ts` — returns EquityPoint[] with peak, drawdown, drawdownPercent per trade
- Drawdown periods: use `findDrawdownPeriods()` from `src/lib/analytics/risk.ts`
- Aggregate stats (winRate, avgWin, avgLoss, profitFactor): use `calculateAggregateStats()` from `src/lib/analytics/stats.ts`
- Risk of Ruin already computed in `analytics.getRiskMetrics` router (line 1056) with account-based ruinThreshold
- Prop schema fields on accounts: maxDrawdown, drawdownType, dailyLossLimit, profitTarget, consistencyRule, minTradingDays, challengeStartDate, challengeEndDate, challengeStatus, profitSplit, payoutFrequency, linkedAccountId
- Prop enums: drawdownTypeEnum (trailing/static/eod), challengeStatusEnum (active/passed/failed), payoutFrequencyEnum, accountTypeEnum (prop_challenge/prop_funded/live/demo)
- propFieldsSchema in `src/lib/shared/schemas.ts` — Zod validation for account prop fields
- DailyLossProgress component already exists in `src/components/dashboard/widgets/today-performance-widget.tsx:12-55` — shows progress bar for daily loss limit
- ComplianceGauge circular gauge in `rule-compliance-widget.tsx:11-77` — reusable pattern for circular progress
- RiskGauge component at `src/components/analytics/risk-gauge.tsx` — circular SVG gauge with risk level badges
- MonteCarloChart at `src/components/analytics/monte-carlo-chart.tsx` — percentile distribution display
- accounts.getStats (line 439) returns totalTrades, wins, losses, winRate, totalPnl, initialBalance, currentBalance
- Error constants: ERR_ACCOUNT_NOT_FOUND, ERR_CHALLENGE_ACCOUNT_NOT_FOUND, ERR_ACCOUNT_NOT_CHALLENGE in `src/lib/constants/errors.ts`
- Constants barrel export: `src/lib/constants/index.ts` — add new prop constants here
- parsePnl() helper exists in both `src/lib/analytics/risk.ts` and `src/lib/analytics/stats.ts`
- Prop constants: `src/lib/constants/prop.ts` — COMPLIANCE_STATUS, COMPLIANCE_THRESHOLDS, COMPLIANCE_STATUS_COLORS, CHALLENGE_STATUS_LABELS, DRAWDOWN_TYPE_LABELS, PROP_ACCOUNT_TYPES, isPropAccountType()
- Compliance thresholds: SAFE_MIN=0.3 (>30% buffer), CAUTION_MIN=0.1 (10-30% buffer), below 0.1 = danger
- Prop compliance calculations: `src/lib/analytics/prop-compliance.ts` — all pure functions, no DB calls
- Percentage inputs (maxDrawdown, dailyLossLimit, profitTarget) are raw numbers (6 = 6%), divide by 100 for dollar conversions
- Biome barrel export ordering: alphabetical (prop-compliance before risk, stats, thresholds)
- Soft-delete filtering: always include `isNull(trades.deletedAt)` when querying trades to exclude soft-deleted records
- `isPropAccountType()` from `@/lib/constants/prop` validates both prop_challenge and prop_funded account types
- Dashboard widget pattern: `DashboardWidget` wrapper provides terminal chrome, loading skeleton, href link. Use `skeletonVariant` for appropriate loading state
- Conditional rendering for prop widgets: check `isPropAccountType(selectedAccount?.accountType)` and return null if not prop
- Biome import ordering: `import type` BEFORE `import` from same module path
- `COMPLIANCE_STATUS_COLORS[status]` maps compliance status to Terminal design text colors (text-profit/text-primary/text-loss)
- AG Charts (`ag-charts-react`) is the charting library — NOT Recharts. Use `ANALYTICS_COLORS`, `CHART_AXIS_STYLE` from `@/lib/analytics`
- Drawdown chart props: `equityCurve`, `maxDrawdownPercent`, `dailyLossLimit`, `initialBalance` — thresholds computed as negative P&L from $0 start

---

## 2026-02-18 - US-000
- Audited existing codebase for prop compliance reusable utilities
- No files changed (audit only)

### Existing Utilities to REUSE (with file:line):

**src/lib/analytics/risk.ts:**
- `buildEquityCurve(trades)` (:127) — builds cumulative P&L curve with peak/drawdown tracking. REUSE for drawdown chart and trailing drawdown calculation
- `findDrawdownPeriods(curve)` (:168) — finds all drawdown periods from equity curve. REUSE for drawdown analysis
- `calculateRiskOfRuin()` (:376) — formula-based RoR calculation. REUSE for Monte Carlo comparison
- `calculateKellyPercent()` (:348) — Kelly criterion. REUSE as-is
- `calculateRiskMetrics()` (:417) — complete risk metrics calculator. REUSE for prop compliance risk section
- `TradeForRisk` interface (:9) — minimal trade interface for risk calcs
- `EquityPoint` interface (:45) — equity curve point type
- Private `parsePnl()` (:102) — safe PnL parsing (note: also exists in stats.ts as public export)

**src/lib/analytics/stats.ts:**
- `calculateAggregateStats(trades, beThreshold)` (:131) — win rate, avg win/loss, profit factor. REUSE for Monte Carlo inputs
- `parsePnl()` (:51) — public export, safe PnL string→number parsing
- `TradeForStats` interface (:11) — minimal trade interface for stats

**src/lib/shared/schemas.ts:**
- `propFieldsSchema` (:121) — Zod validation for all prop firm fields
- `drawdownTypeEnum`, `challengeStatusEnum`, `payoutFrequencyEnum`, `accountTypeEnum` — shared enums

**src/server/db/schema.ts:**
- Account table (:206-223) — prop fields: maxDrawdown, drawdownType, dailyLossLimit, profitTarget, consistencyRule, minTradingDays, challengeStartDate, challengeEndDate, challengeStatus, profitSplit, payoutFrequency, linkedAccountId

**src/server/api/routers/accounts.ts:**
- `getStats` (:439) — basic account stats (trades, P&L, balance). Can extend or query alongside
- `convertToFunded` (:277) — challenge→funded conversion logic
- `markChallengeFailed` (:338) — mark challenge as failed

**src/server/api/routers/analytics.ts:**
- `getRiskMetrics` (:1056) — already uses account's maxDrawdown as ruinThreshold. Pattern to follow for prop compliance endpoint

### Existing UI Components to REUSE:

**src/components/dashboard/widgets/today-performance-widget.tsx:**
- `DailyLossProgress` (:12-55) — progress bar for daily loss limit. Can extract to shared component or reference pattern

**src/components/dashboard/widgets/rule-compliance-widget.tsx:**
- `ComplianceGauge` (:11-77) — circular SVG gauge. REUSE pattern for compliance gauges
- `CategoryBar` (:80-100) — horizontal progress bar with label. REUSE pattern for metric bars

**src/components/analytics/risk-gauge.tsx:**
- `RiskGauge` — full circular gauge with risk level badges. REUSE for drawdown gauge on prop page

**src/components/analytics/monte-carlo-chart.tsx:**
- `MonteCarloChart` — percentile distribution display. REUSE pattern for challenge outcome simulator

**src/components/analytics/equity-curve.tsx:**
- EquityCurve chart using AG Charts. REUSE pattern for drawdown chart overlay

### Local Helpers to EXTRACT:

- `DailyLossProgress` from today-performance-widget.tsx — currently local, should be extracted if reused in prop status widget
- `ComplianceGauge` pattern — can be generalized for multiple gauge use cases

### NEW Utilities Needed:

1. **src/lib/constants/prop.ts** — COMPLIANCE_STATUS, COMPLIANCE_THRESHOLDS, COMPLIANCE_STATUS_COLORS, CHALLENGE_STATUS_LABELS, DRAWDOWN_TYPE_LABELS, PROP_ACCOUNT_TYPES
2. **src/lib/analytics/prop-compliance.ts** — Pure calculation functions:
   - calculateDrawdownStatus() — current vs max drawdown with status
   - calculateDailyPnl() — sum netPnl for trades on a date
   - calculateDailyLossStatus() — today's P&L vs daily loss limit
   - calculateProfitTargetProgress() — current P&L vs profit target
   - calculateConsistencyMetric() — max single-day P&L vs consistency rule
   - calculateTradingDays() — distinct trading dates count
   - calculateTrailingDrawdown() — high-water-mark drawdown from equity curve
   - calculateStaticDrawdown() — simple formula
   - getOverallComplianceStatus() — worst status across all metrics
   - simulatePropChallenge() — Monte Carlo for challenge pass/fail
3. **accounts.getPropCompliance** tRPC endpoint — single endpoint returning all compliance data
4. **UI components:** PropStatusWidget, ComplianceGrid, DrawdownChart, TradingDaysTimeline, ChallengeHistory, ChallengeSimulator

### Learnings for future iterations:
- buildEquityCurve() starts at $0 cumulative P&L, NOT initial balance. For prop compliance, need to account for initialBalance when computing absolute drawdown thresholds
- maxDrawdown is stored as percentage (e.g., "6.00" for 6%), dailyLossLimit same. Parse with parseFloat() and divide by 100 for decimal
- getRiskMetrics router (analytics.ts:1074-1095) shows pattern for reading account's maxDrawdown and converting to decimal threshold
- DailyLossProgress already exists and handles prop daily loss display — consider extracting rather than duplicating
- Account props stored as decimal strings, all need parseFloat() conversion

---

## 2026-02-18 - US-001
- Created `src/lib/constants/prop.ts` with all prop compliance constants
- Exported from `src/lib/constants/index.ts` barrel file
- Files changed: `src/lib/constants/prop.ts` (new), `src/lib/constants/index.ts` (updated)
- **Learnings for future iterations:**
  - Constants file pattern: use `as const` for literal type inference, `Record<ComplianceStatus, string>` for mapped types
  - `isPropAccountType()` helper available for type-safe prop account checks
  - `ComplianceStatus` type exported for use in utility functions and UI components
  - COMPLIANCE_STATUS_BG_COLORS also available for badge backgrounds (bg-profit/10, bg-primary/10, bg-loss/10)

---

## 2026-02-18 - US-002
- Created `src/lib/analytics/prop-compliance.ts` with all pure calculation functions
- Exported from `src/lib/analytics/index.ts` barrel file
- Files changed: `src/lib/analytics/prop-compliance.ts` (new), `src/lib/analytics/index.ts` (updated)
- Functions implemented:
  - `calculateDrawdownStatus()` — drawdown vs limit with compliance status
  - `calculateDailyPnl()` — sum trades by date (ISO date string comparison)
  - `calculateDailyLossStatus()` — daily P&L vs daily loss limit (converts % to dollar limit)
  - `calculateProfitTargetProgress()` — progress toward profit target (converts % to dollar target)
  - `calculateConsistencyMetric()` — largest single-day % of total profit vs consistency rule
  - `calculateTradingDays()` — count distinct trading dates from exitTime
  - `calculateTrailingDrawdown()` — high-water-mark drawdown from equity curve + initial balance
  - `calculateStaticDrawdown()` — simple initialBalance - currentBalance formula
  - `getOverallComplianceStatus()` — returns worst status from an array
- **Learnings for future iterations:**
  - `statusFromBuffer()` private helper centralizes threshold logic — remaining buffer → status
  - All percentage inputs (maxDrawdown, dailyLossLimit, profitTarget) come as raw numbers (e.g. 6 for 6%), not decimals — divide by 100 when converting to dollar amounts
  - `calculateTrailingDrawdown()` adds initialBalance to equity curve points since `buildEquityCurve()` tracks cumulative P&L from $0
  - Biome requires alphabetical ordering for barrel exports — `prop-compliance` goes before `risk`
  - Profit target status uses different thresholds than drawdown (progress >= 1 = safe, >= 0.5 = caution, < 0.5 = danger)
- Unit tests: run with `bunx vitest run --config vitest.config.unit.ts` (no Docker needed)
- Floating point gotcha: `1 - 0.7` in JS is `0.30000000000000004`, not `0.3` — boundary tests must account for this

---

## 2026-02-18 - US-003
- Created `tests/unit/prop-compliance.test.ts` with 62 tests covering all 9 functions
- Files changed: `tests/unit/prop-compliance.test.ts` (new)
- Tests cover:
  - `calculateDrawdownStatus`: safe/caution/danger thresholds, edge cases (0 limit, exceeded limit), trailing/static/EOD scenarios
  - `calculateDailyPnl`: multi-trade summation, date filtering, null handling
  - `calculateDailyLossStatus`: safe/caution/danger, percentage-to-dollar conversion, positive P&L handling
  - `calculateProfitTargetProgress`: 0%/50%/100%+ progress, negative P&L clamping, 0 target edge case
  - `calculateConsistencyMetric`: compliant/non-compliant, negative days ignored, empty array, single day, boundary
  - `calculateTradingDays`: distinct date counting, null exitTime, deduplication
  - `calculateTrailingDrawdown`: high-water-mark tracking, peak recovery, monotonic curves, empty/zero edge cases
  - `calculateStaticDrawdown`: below/equal/above initial balance, zero balance
  - `getOverallComplianceStatus`: worst-status selection, empty array
- **Learnings for future iterations:**
  - Floating point precision: `1 - 0.7 = 0.30000000000000004` in JS — boundary tests at exact threshold values will fail if you assume exact equality. Use values clearly on one side of the boundary.
  - Unit test helper pattern: `makeEquityPoint()` helper creates minimal EquityPoint objects for tests — avoids boilerplate
  - Biome formatting: wrapping `.toBe()` calls requires specific formatting — Biome prefers `expect(fn()).toBe(val)` inline when possible

---

## 2026-02-18 - US-004
- Created `getPropCompliance` tRPC query in `src/server/api/routers/accounts.ts`
- Added `ERR_ACCOUNT_NOT_PROP` error constant in `src/lib/constants/errors.ts`
- Files changed: `src/server/api/routers/accounts.ts`, `src/lib/constants/errors.ts`
- Endpoint features:
  - Input: `{ accountId: string }`, validates ownership and prop account type
  - Fetches all closed trades sorted by exitTime, filters out soft-deleted trades (`isNull(deletedAt)`)
  - Computes drawdown (trailing vs static based on `drawdownType`), daily loss, profit target, consistency, trading days, timeline
  - Returns equity curve for charting, overall compliance status
  - Uses all pure utility functions from US-002 (`calculateDrawdownStatus`, `calculateDailyLossStatus`, etc.)
  - Uses `buildEquityCurve()` from risk analytics for equity curve data
- **Learnings for future iterations:**
  - Import `isNull` from `drizzle-orm` for soft-delete filtering — closed trades query should always include `isNull(trades.deletedAt)`
  - Import `asc` from `drizzle-orm` for ascending sort by exitTime
  - `isPropAccountType()` from `@/lib/constants/prop` is the right way to validate prop accounts (supports both `prop_challenge` and `prop_funded`)
  - Biome requires alphabetical ordering of named imports within import blocks
  - Biome prefers inline function calls when arguments fit on one line — avoid unnecessary wrapping

---

## 2026-02-18 - US-005
- Created `tests/integration/prop-compliance.test.ts` with 12 tests covering 8 scenarios
- Updated `tests/utils/fixtures/accounts.ts` to support all prop fields (drawdownType, dailyLossLimit, consistencyRule, minTradingDays, challengeStartDate, challengeEndDate, challengeStatus, payoutFrequency, linkedAccountId)
- Updated `tests/utils/fixtures/scenarios.ts` — `setupPropChallenge()` now accepts full `account` options and sets all prop fields by default
- Files changed: `tests/integration/prop-compliance.test.ts` (new), `tests/utils/fixtures/accounts.ts` (updated), `tests/utils/fixtures/scenarios.ts` (updated)
- Tests cover:
  - Happy path: returns all compliance metrics for account with trades across multiple days
  - Empty trades: returns safe drawdown/daily loss but danger profit target (0% progress)
  - Danger state: near max drawdown (5.5% of 6% limit → danger)
  - Daily loss limit: today's trades near daily limit (93.3% used → danger)
  - Profit target reached: trades exceeding $10000 target → safe
  - Auth validation: rejects unauthenticated and non-owner requests
  - Non-prop rejection: rejects live and demo accounts with correct error
  - Trailing drawdown: verifies trailing tracks from peak (higher than static)
  - Prop funded: accepts prop_funded accounts
  - Consistency metric: detects non-compliant when one day dominates (80% > 30% rule)
- **Learnings for future iterations:**
  - `createTestAccount` fixture now supports ALL prop fields — no need to insert directly via DB
  - `setupPropChallenge()` sets sensible defaults: 100k balance, 6% drawdown, 3% daily loss, 10% profit target, 30% consistency rule, 5 min trading days
  - Compliance threshold math: SAFE > 30% buffer remaining, CAUTION 10-30%, DANGER < 10% — so for a 6% drawdown limit, you need > 5.4% current drawdown to hit danger (>90% used)
  - Biome formatting: `createTestCaller(user.clerkId, user)` fits inline, but `createTestCaller(trailingUser.clerkId, trailingUser)` wraps to multiline — line length determines format
  - Docker/OrbStack must be running for integration tests (Testcontainers requires container runtime)

---

## 2026-02-18 - US-006
- Created `src/components/dashboard/widgets/prop-status-widget.tsx`
- Compact prop compliance widget for dashboard with:
  - DrawdownGauge (circular SVG gauge showing used % of max drawdown)
  - ComplianceBar (reusable progress bar for daily loss and profit target)
  - StatusDot (colored indicator for overall status)
  - Trading days count and days remaining countdown
  - Overall status label (safe/caution/danger)
- Uses `isPropAccountType()` to conditionally render (returns null for non-prop accounts)
- Calls `accounts.getPropCompliance` with `enabled: isProp && !!selectedAccountId`
- Uses `DashboardWidget` wrapper for loading skeleton and terminal chrome
- All key elements have data-testid attributes
- Files changed: `src/components/dashboard/widgets/prop-status-widget.tsx` (new)
- **Learnings for future iterations:**
  - Biome import ordering: `import type` must come BEFORE `import` from the same module
  - Biome formatting: `cn()` arguments need one extra level of indentation when inside JSX attribute
  - Biome class sorting: `font-mono font-semibold text-xs` not `font-mono text-xs font-semibold` — weight modifiers before size
  - Widget pattern: use `DashboardWidget` wrapper with `icon`, `title`, `href`, `loading`, `skeletonVariant` props
  - Query pattern for conditional data: `{ enabled: condition }` prevents unnecessary API calls

---

## 2026-02-18 - US-007
- Added PropStatusWidget to dashboard grid in `src/app/(protected)/dashboard/page.tsx`
- Conditionally renders before Today's Performance when `isPropAccountType(selectedAccount?.accountType)` is true
- Uses `GridItem size="md"` for 2-column width
- Files changed: `src/app/(protected)/dashboard/page.tsx` (updated)
- **Learnings for future iterations:**
  - Conditional grid items: use `{condition && (<GridItem>...</GridItem>)}` pattern — React handles null children in grids correctly
  - `isPropAccountType()` accepts `undefined` safely (returns false), no need for optional chaining guard
  - Dashboard page already has `selectedAccount` from `useAccount()` — no need to destructure `selectedAccountId` unless needed for API calls

---

## 2026-02-18 - US-008
- Added conditional Prop nav item to sidebar in `src/app/(protected)/_components/app-sidebar.tsx`
- Imports `Shield` icon from lucide-react, `isPropAccountType` from `@/lib/constants/prop`
- Uses `useMemo` to compute `navItems` array: inserts `{ title: "Prop", href: "/prop", icon: Shield }` between Analytics and AI Insights when any account is prop type
- Renders `navItems` instead of static `mainNavItems` in the sidebar menu
- Files changed: `src/app/(protected)/_components/app-sidebar.tsx` (updated)
- **Learnings for future iterations:**
  - Sidebar nav items are defined in static `mainNavItems` array — for conditional items, compute a dynamic `navItems` via `useMemo` that splices in the conditional item at the right index
  - `accounts` from `useAccount()` is available in sidebar — use `.some()` to check for prop accounts
  - Biome import ordering: `@/contexts` comes before `@/lib` alphabetically — respect this when adding new imports

---

## 2026-02-18 - US-009
- Created `src/app/(protected)/prop/page.tsx` — dedicated prop compliance page
- Features implemented:
  - Page header with "Compliance" section label and "Prop Compliance" heading (Terminal design)
  - Account selector dropdown filtered to `prop_challenge` and `prop_funded` accounts only
  - Challenge status badge (Active/Passed/Failed) with color-coded styling
  - Phase indicator for funded accounts showing "Challenge → Funded" journey
  - Auto-selects first prop account if globally selected account is not a prop account
  - Compliance summary bar showing overall status, account name, and current balance
  - Loading skeleton state while data fetches
  - Empty state when user has no prop accounts
  - No-data state when compliance data unavailable
  - All key elements have data-testid attributes
- Uses `accounts.getPropCompliance` with `enabled` guard and `staleTime: 30000`
- Syncs prop page selection back to global `setSelectedAccountId` on change
- Files changed: `src/app/(protected)/prop/page.tsx` (new)
- **Learnings for future iterations:**
  - Biome import ordering: `lucide-react` comes before `react` (l < r)
  - TypeScript `Record<string, T>` index lookup returns `T | undefined` — use a separate default variable rather than `?? map.key` to avoid possibly-undefined errors
  - Biome CSS class sorting: `text-muted-foreground` before `text-xs` (specificity order)
  - Biome formatting: JSX attributes should be on separate lines when combining `className` and `data-testid` on a div
  - Prop page uses local `propAccountId` state to allow independent selection from global account selector
  - `formatCurrency` from `@/lib/shared` not needed here — using `toLocaleString` directly for inline balance display

---

## 2026-02-18 - US-010
- Created `src/components/prop/compliance-grid.tsx` — 4-card responsive compliance grid
- Cards implemented:
  - Max Drawdown: circular SVG gauge (80px) showing used% with current/limit/remaining stats
  - Daily Loss: large dollar amount display + progress bar with used% and remaining
  - Profit Target: large dollar amount display + progress bar with completion% and amount to go
  - Consistency Rule: max day % vs limit with compliant/non-compliant indicator
- Each card has StatusDot indicator, ComplianceCardShell wrapper, and data-testid attributes
- Uses `ComplianceData` interface typed to match `getPropCompliance` return shape
- Shared helpers: `StatusDot`, `ComplianceCardShell`, `DrawdownGauge`, `MetricProgressBar`
- Responsive: 1 column on mobile, 2x2 grid on `sm:` breakpoint
- Files changed: `src/components/prop/compliance-grid.tsx` (new)
- **Learnings for future iterations:**
  - Biome formatting: single JSX return without parens when it fits one line — `return <span .../>;` not `return (\n<span .../>\n);`
  - Biome formatting: short ternary fits one line — `const x = cond ? "a" : "b";` not multiline
  - Biome formatting: short `className` + `data-testid` attributes can go on same line as opening tag
  - `ComplianceData` interface props match `getPropCompliance` return keys — new prop components can import this type
  - `formatCurrency()` from `@/lib/shared` accepts `number | string | null | undefined` — safe to use for dollar displays

---

## 2026-02-18 - US-011
- Created `src/components/prop/drawdown-chart.tsx` — equity curve with drawdown threshold lines
- Features implemented:
  - AG Charts AreaChart showing cumulative P&L over time (matching existing EquityCurve pattern)
  - Red dashed horizontal reference line at max drawdown threshold
  - Orange/amber dashed horizontal reference line at daily loss limit
  - Shaded danger zone (red fill) below max drawdown threshold
  - Color gradient: profit green above $0, loss red below
  - Tooltip showing date, P&L, drawdown %, and distance to threshold
  - Legend explaining threshold lines (P&L, Max DD, Daily Loss, Danger Zone)
  - Summary stats bar: Current P&L, Drawdown, Max DD Limit, Daily Loss Limit
  - Empty state for no equity data
  - Responsive design, Terminal design system
  - All data-testid attributes on key elements
- Files changed: `src/components/prop/drawdown-chart.tsx` (new)
- **Learnings for future iterations:**
  - AG Charts is the charting library (not Recharts) — PRD said Recharts but codebase uses `ag-charts-react`
  - Use `ANALYTICS_COLORS` and `CHART_AXIS_STYLE` from `@/lib/analytics` for chart styling consistency
  - Threshold lines use `type: "line"` series with `lineDash` for dashed appearance
  - Danger zone fill: use `type: "area"` series with very low opacity fill (`${ANALYTICS_COLORS.loss}10`)
  - `buildEquityCurve()` returns cumulative P&L from $0, so thresholds are negative dollar amounts (e.g., -$6000 for 6% of $100k)
  - Biome auto-fix: `bunx biome check --write <file>` to fix formatting in one shot

---

## 2026-02-18 - US-012
- Created `src/components/prop/trading-days-timeline.tsx` — Trading Days Timeline Component
- Features implemented:
  - Progress bar showing days traded vs minTradingDays requirement (X / Y format)
  - Days remaining countdown to challengeEndDate with color-coded urgency (red ≤5, primary ≤14, muted otherwise)
  - Calendar-style grid highlighting active trading days (green cells for traded, white/5 for past untouched, white/2 for future)
  - Today indicator with ring highlight
  - Pace indicator showing "On pace" / "Behind pace" based on expected vs actual trading days
  - Footer stats: start date, end date, days elapsed, days traded
  - "Requirement met" label when all minimum days are completed
  - Empty/missing date handling for accounts without start/end dates
- Component accepts `tradingDays` and `timeline` props matching `getPropCompliance` response shape
- Files changed: `src/components/prop/trading-days-timeline.tsx` (new)
- **Learnings for future iterations:**
  - Array destructuring from `.split().map(Number)` gives `(number | undefined)[]` in strict TS — use individual index access with nullish coalescing (`parts[0] ?? 0`) instead of destructuring
  - `allDates[0]` is `string | undefined` even when guarded by `allDates.length > 0` — TS doesn't narrow array index access, use `?? ""` fallback
  - Calendar grid week alignment: use `getDayOfWeek(firstDate)` to compute padding cells (null values) for the first partial week
  - Date generation: use local Date constructor `new Date(y, m-1, d)` to avoid timezone issues with date strings
  - `parseDate()` helper handles both `Date` objects and ISO strings from tRPC serialization (dates become strings over the wire)

---

## 2026-02-18 - US-013
- Created `src/components/prop/challenge-history.tsx` — Challenge History Component
- Features implemented:
  - Vertical timeline of all prop accounts (challenge + funded) sorted by challengeStartDate (newest first)
  - Each entry shows: account name, status badge (Active/Passed/Failed), date range, starting balance
  - Timeline dots with connecting lines, color-coded by challenge status (profit green for passed, loss red for failed, primary for active)
  - Selected account highlighted with brighter background
  - Linked funded account indicator showing "→ Funded: [account name]" connection
  - Clickable entries with `onSelectAccount` callback
  - Empty state: "No challenge history yet"
  - Account type label (Challenge/Funded) on right side
  - Uses `useAccount()` context for account data (includes all active accounts)
  - All data-testid attributes on key elements
- Files changed: `src/components/prop/challenge-history.tsx` (new)
- **Learnings for future iterations:**
  - `useAccount()` context uses `getActive` which only returns active accounts — failed/deactivated challenges may not appear. If full history needed, use `accounts.getAll` tRPC query directly
  - `accounts.getAll` includes `with: { group: true, linkedAccount: true }` relation data, while `getActive` does not
  - For timeline dots: use `border-2` + `border-current` for unfilled state, solid `bg-` class for filled/selected state
  - `Number.parseFloat()` preferred over `parseFloat()` by Biome's linting rules
  - Biome auto-inlines short `cn()` calls that fit on one line — don't fight it, let `bunx biome check --write` handle formatting

---

## 2026-02-18 - US-014
- Assembled full /prop page with all compliance components from US-009 through US-013
- Replaced placeholder comment with actual component assembly
- Files changed: `src/app/(protected)/prop/page.tsx` (updated)
- Components assembled:
  - `ComplianceGrid` (US-010): receives full `data` object from `getPropCompliance`
  - `DrawdownChart` (US-011): receives `equityCurve`, `maxDrawdownPercent`, `dailyLossLimit`, `initialBalance` from compliance data
  - `TradingDaysTimeline` (US-012): receives `tradingDays` and `timeline` props
  - `ChallengeHistory` (US-013): uses its own data from `useAccount()`, receives `selectedAccountId` and `onSelectAccount` callback
- Layout: `space-y-6` vertical spacing, 2-column grid (`lg:grid-cols-2`) for DrawdownChart + TradingDaysTimeline on desktop
- Updated loading skeleton to match full page layout (summary bar, 4 metric cards, chart + timeline, history)
- `ChallengeHistory.onSelectAccount` wired to `handleAccountChange` for seamless account switching from timeline
- **Learnings for future iterations:**
  - Biome formatting: inline ternary expressions that exceed line width must be multiline — Biome enforces `\n? value\n: fallback\n` format
  - `dailyLossLimit` for DrawdownChart is percentage, computed from dollar amounts: `(dollarLimit / initialBalance) * 100`
  - `ComplianceGrid` uses a `ComplianceData` interface — passing the full `getPropCompliance` response works because it's a superset
  - Loading skeleton should mirror the actual content layout for smooth CLS (content layout shift) — update skeleton whenever content structure changes

---

## 2026-02-18 - US-016
- Added `accountType` prop to `RiskGauge` component for prop-aware labeling
- When `accountType` is prop + `ruinThresholdSource === "account"`:
  - Subtitle changes from "Risk of Ruin" to "Challenge Failure Risk"
  - Basis text says "Based on X% max drawdown rule" instead of "X% max DD"
  - ChartTerminal title also updates to "Challenge Failure Risk"
- Updated `RiskTab` in analytics page to pass `selectedAccount?.accountType` to `RiskGauge`
- Existing behavior unchanged for non-prop accounts
- Files changed: `src/components/analytics/risk-gauge.tsx`, `src/app/(protected)/analytics/page.tsx`
- **Learnings for future iterations:**
  - `isPropAccountType()` accepts `undefined` gracefully (returns false) — safe to pass `selectedAccount?.accountType` directly
  - Compound condition pattern: `isPropAccountType(accountType) && ruinThresholdSource === "account"` ensures only prop accounts with account-based thresholds get relabeled
  - ChartTerminal title should also reflect the label change — users see the terminal header before the gauge content

---

## 2026-02-18 - US-017
- Added `simulatePropChallenge()` Monte Carlo simulation function to `src/lib/analytics/prop-compliance.ts`
- Added `SimulatePropChallengeInput` and `SimulatePropChallengeResult` interfaces
- Added private `percentile()` helper for linear interpolation of sorted arrays
- Files changed: `src/lib/analytics/prop-compliance.ts` (updated)
- Function features:
  - Runs N iterations (default 10000), each simulating random trades using winRate + avgWin/avgLoss
  - Tracks equity and high-water-mark drawdown per simulation
  - Three stop conditions: profit target reached (pass), max drawdown breached (fail), max trades exceeded (fail)
  - Returns passRate, failRate, avgTradesToPass, medianOutcome, percentiles (p10/p25/p50/p75/p90), simulations count
  - Edge cases: profitTarget <= 0 returns 100% pass, maxDrawdown <= 0 returns 100% fail
- **Learnings for future iterations:**
  - `avgLoss` input is a positive number (e.g. 200), negated inside the loop as `-avgLoss` for trade result
  - Percentile calculation uses linear interpolation between sorted array indices for smooth values
  - Already exported from barrel `src/lib/analytics/index.ts` via `export * from "./prop-compliance"`
  - No need for seeded PRNG — Math.random() is fine for Monte Carlo since exact reproducibility isn't required

---

## 2026-02-18 - US-018
- Added 12 unit tests for `simulatePropChallenge()` to `tests/unit/prop-compliance.test.ts`
- Files changed: `tests/unit/prop-compliance.test.ts` (updated)
- Test total now: 74 tests in prop-compliance file, 110 total across unit tests
- Tests cover:
  - High win rate (80%) produces high pass rate (>70%)
  - Low win rate (30%) produces low pass rate (<30%)
  - Edge case: profitTarget = 0 → 100% pass rate
  - Edge case: maxDrawdown = 0 → 100% fail rate
  - Edge case: negative profitTarget → 100% pass rate
  - Return shape: all expected fields present (passRate, failRate, avgTradesToPass, medianOutcome, percentiles, simulations)
  - Percentiles in correct order (p10 ≤ p25 ≤ p50 ≤ p75 ≤ p90)
  - passRate + failRate = 1 invariant
  - Simulations count matches iterations input
  - Default 10000 iterations when not specified
  - medianOutcome equals p50 percentile
  - avgTradesToPass is positive when passes exist
- **Learnings for future iterations:**
  - Biome formatting: `expect().toBeLessThanOrEqual(value)` fits inline when argument is short — don't wrap to multiline
  - Monte Carlo tests use statistical assertions (>0.7, <0.3) not exact values due to randomness — use generous bounds
  - `baseInput` pattern with spread overrides (`{...baseInput, winRate: 0.8}`) keeps test setup DRY

---

## 2026-02-18 - US-019
- Created `src/components/prop/challenge-simulator.tsx` — Monte Carlo Challenge Outcome Card
- Enhanced `getPropCompliance` endpoint to include `tradeStats` (totalTrades, wins, losses, winRate, avgWin, avgLoss) since trades are already loaded
- Added `ChallengeSimulator` to `/prop` page after drawdown chart section, before challenge history
- Files changed: `src/components/prop/challenge-simulator.tsx` (new), `src/app/(protected)/prop/page.tsx` (updated), `src/server/api/routers/accounts.ts` (updated)
- Features implemented:
  - Prominent pass rate display (large number, color-coded: green >=70%, amber >=40%, red <40%)
  - Pass/fail distribution bar with inline labels
  - Percentile outcome distribution visualization (interquartile range, median marker) — reuses MonteCarloChart pattern
  - Stats grid: median outcome, avg trades to pass
  - Basis info showing trade count, win rate, avg win/loss
  - Disclaimer about historical stats assumption
  - "Not enough data" empty state when < 10 trades
  - Terminal design: font-mono, data-testid attributes, dark card styling
  - Runs 10,000 iterations client-side via useMemo
- **Learnings for future iterations:**
  - `getPropCompliance` already loads all account trades — enrich its return value rather than making a second API call to `getStats` for trade statistics
  - `simulatePropChallenge` expects `winRate` as fraction (0-1), but `tradeStats.winRate` is percentage (0-100) — divide by 100 before passing
  - `maxDrawdown` for simulator needs dollar amount, computed from `data.drawdown.limit` (percentage) × `initialBalance`
  - `profitTarget` dollar amount available directly from `data.profitTarget.target`
  - Biome auto-fix: `bunx biome check --write <files>` is the fastest way to fix class ordering and formatting

---

## 2026-02-18 - US-020
- Added prop compliance to AI context — AI can now query prop challenge/funded account compliance metrics
- Extended `call-analytics.ts` to support `accounts` router with `getPropCompliance` endpoint in the allowlist
- Updated `definitions.ts` tool description to include accounts router and prop compliance endpoint details, expanded router enum to `["analytics", "trades", "accounts"]`
- Updated `schema-context.ts` to add Accounts Router section with `getPropCompliance` endpoint documentation
- Updated `planner.ts` to add accounts router with `getPropCompliance` to available analytics endpoints for report planning
- Files changed: `src/lib/ai/tools/call-analytics.ts`, `src/lib/ai/tools/definitions.ts`, `src/lib/ai/schema-context.ts`, `src/lib/ai/report-pipeline/planner.ts`
- **Learnings for future iterations:**
  - AI tool system uses allowlisted endpoint sets per router (`ALLOWED_ANALYTICS_ENDPOINTS`, `ALLOWED_TRADES_ENDPOINTS`) — add new routers by creating a new `ALLOWED_*_ENDPOINTS` array and `*_SET`
  - `executeCallAnalytics` creates a server-side tRPC caller via `createCaller(ctx)` — new routers are accessed as `caller.routerName[endpoint]` with the same pattern
  - `schema-context.ts` is a static string (not per-request) — describes the full database schema and available endpoints for the AI system prompt
  - `planner.ts` has a separate `AVAILABLE_ANALYTICS_ENDPOINTS` string used only for report planning — keep in sync with actual tool capabilities
  - The tool `inputSchema` `router` field uses a `z.enum()` — must be updated when adding new routers

---

## 2026-02-18 - US-021
- Added 3 prop-related queries to `SUGGESTED_CHAT_QUERIES` in `src/lib/constants/ai.ts`
- Added 1 prop-related report prompt to `SUGGESTED_REPORT_PROMPTS`
- Files changed: `src/lib/constants/ai.ts` (updated)
- Chat queries added: "How am I doing on my prop challenge?", "Will I pass this challenge at my current pace?", "What's my risk of failing the challenge?"
- Report prompt added: "Generate a prop challenge compliance report with drawdown analysis, daily loss tracking, and probability of passing"
- **Learnings for future iterations:**
  - `SUGGESTED_CHAT_QUERIES` and `SUGGESTED_REPORT_PROMPTS` are `as const` readonly tuples — just append new entries to the array

---

## 2026-02-18 - US-022
- Added prop context banner to trade detail stats panel
- Banner shows below hero P&L section, only for prop_challenge/prop_funded accounts
- For losses: "This trade used X% of Y% max drawdown limit" (red text for percentage)
- For wins: "This trade contributed X% toward Y% profit target" (green text for percentage)
- Fallback text "Prop account trade" when missing data (no initialBalance or no relevant metric)
- Uses Shield icon from lucide-react with primary (chartreuse) color
- Terminal design: font-mono text-[10px], border-white/5 container, rounded-sm
- Added `account` field to StatsPanel's Trade interface (accountType, maxDrawdown, profitTarget, initialBalance)
- Passed `trade.account` from page.tsx to StatsPanel in both mobile (Sheet) and desktop (ResizablePanel) views
- No new API calls needed — trade.account already loaded by `getById` with `account: true` relation
- All data-testid attributes added (prop-context-banner)
- Files changed: `src/components/trade-detail/stats-panel.tsx` (updated), `src/app/(protected)/journal/[id]/page.tsx` (updated)
- **Learnings for future iterations:**
  - `getById` fetches `account: true` relation — full Account object (including all prop fields) available on `trade.account`
  - `TradeForStatsPanel` is a Pick type from src/types — to pass additional data, extend the local `Trade` interface in stats-panel.tsx rather than modifying the shared type
  - `initialBalance` on Account can be `string | null` — handle null case in component
  - Biome auto-formats long arithmetic expressions inside JSX to multiline with `%` on a separate line
  - Percentage-to-dollar conversion: `initialBalance * (maxDrawdown / 100)` gives the dollar limit; `Math.abs(netPnl) / dollarLimit * 100` gives what percentage of that limit was used

---
