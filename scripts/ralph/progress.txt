# Ralph Progress Log
Started: Thu Jan 15 15:31:55 +01 2026
---

## Codebase Patterns
- P&L calculation functions are in src/lib/market-data/symbols.ts (calculateFuturesPnL, calculateForexPnL, calculatePnL)
- Running P&L utilities are in src/lib/trades/running-pnl.ts (calculateRunningPnlAtTime, generateRunningPnlSeries)
- Use toUnixTimestamp from @/lib/shared for timestamp conversion
- ChartBar type includes { time: number, open: number, high: number, low: number, close: number }
- Execution type defined in src/lib/trades/running-pnl.ts with { id, executionType, price, quantity, executedAt, realizedPnl }

---

## 2026-01-15 - US-000
- Audited existing utilities for running P&L calculation
- Confirmed: calculateFuturesPnL exists in src/lib/market-data/symbols.ts (lines 775-794)
- Confirmed: calculateForexPnL exists in src/lib/market-data/symbols.ts (lines 805-825)
- Confirmed: calculateRunningPnl is LOCAL helper in src/components/trade-detail/replay/use-replay-engine.ts (lines 273-306) - needs extraction
- **Learnings for future iterations:**
  - P&L calculation uses proper point/pip values from FUTURES_SPECS and FOREX_SPECS
  - calculatePnL is a unified wrapper that delegates to futures/forex functions based on instrumentType
  - Running P&L = unrealized P&L (current position) + realized P&L (from exits/scale-outs)
  - The local helper tracks visible executions and bars progressively for replay animation
---

## 2026-01-15 - US-001
- Extracted running P&L calculation utility from use-replay-engine.ts
- Created src/lib/trades/running-pnl.ts with two main functions:
  - calculateRunningPnlAtTime(): Calculates P&L at a specific point using current price and visible executions
  - generateRunningPnlSeries(): Returns array of { time, pnl } for charting
- Exported both functions and Execution type from src/lib/trades/index.ts
- Updated use-replay-engine.ts to import from shared utility (removed local helper)
- Files changed:
  - src/lib/trades/running-pnl.ts (new)
  - src/lib/trades/index.ts (added export)
  - src/components/trade-detail/replay/use-replay-engine.ts (refactored)
- **Learnings for future iterations:**
  - ReplayExecution is now a type alias to Execution for backwards compatibility
  - generateRunningPnlSeries filters bars starting from entry time (not before)
  - The utility handles both futures and forex via delegation to existing P&L functions
---

## 2026-01-15 - US-005
- Created comprehensive integration tests for running P&L utility
- Test file: tests/integration/running-pnl.test.ts
- Test coverage includes:
  - Long trade P&L calculations (profit/loss scenarios)
  - Short trade P&L calculations (profit/loss scenarios)
  - Partial exit handling (scale-outs with accumulated realized P&L)
  - Multiple futures symbols: ES ($50/pt), NQ ($20/pt), MES ($5/pt)
  - Forex symbols: EUR/USD, GBP/USD ($10/pip per standard lot)
  - Edge cases: empty executions, empty bars, no entry execution, zero price movement
  - generateRunningPnlSeries: series generation, bar filtering, scale-out inclusion
- Files changed:
  - tests/integration/running-pnl.test.ts (new)
- **Learnings for future iterations:**
  - Running P&L tests don't need database - they test pure functions
  - However, vitest global setup still starts Testcontainers for all integration tests
  - Test helper functions (createBar, createExecution) make test data creation clean
  - ES: $50/point, NQ: $20/point, MES: $5/point (micro)
  - EUR/USD & GBP/USD: pipSize 0.0001, $10/pip per standard lot
---

## 2026-01-15 - US-002
- Created RunningPnlChart component for displaying trade P&L over time
- File: src/components/trade-detail/running-pnl-chart.tsx
- Implementation details:
  - Uses AG Charts (ag-charts-react) following MonthlyChart pattern
  - Area chart with split coloring: green fill (profitFill) for profit, red (lossFill) for loss
  - X-axis timestamps formatted in user's timezone via useTimezone hook
  - Y-axis shows P&L in dollars with $K formatting for large values
  - Adaptive label density: shows fewer labels for larger datasets (100+ points)
  - Tooltip shows full timestamp (MMM d, HH:mm:ss) and P&L value
  - Final P&L displayed in top-right corner overlay
  - Terminal design system: transparent background, monospace fonts (JetBrains Mono)
- Files changed:
  - src/components/trade-detail/running-pnl-chart.tsx (new)
- **Learnings for future iterations:**
  - AG Charts uses `AgCartesianChartOptions<DataType>` for typed chart options
  - formatInTimeZone from date-fns-tz for timezone-aware formatting
  - ANALYTICS_COLORS.profitFill/lossFill are 20% opacity versions of profit/loss colors
  - CHART_AXIS_STYLE provides consistent axis styling across all charts
  - Chart height should use flex-1 with 100% height style for full tab fill
  - Markers can be conditionally shown based on data density (chartData.length <= 50)
---
