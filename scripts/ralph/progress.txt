# Ralph Progress Log
Started: Sat Mar 14 20:36:41 WET 2026
---

## 2026-03-14 - US-001
- Created `types/globals.d.ts` with ClerkPublicMetadata interface and Clerk type extensions (CustomJwtSessionClaims for server-side, UserPublicMetadata for client-side)
- Added `isBetaFromMetadata(metadata)` helper in `src/lib/billing/utils.ts` — safely checks nested `metadata.features.beta_access === true`
- Kept existing `isBetaAuth()` with @deprecated JSDoc tag for backward compat
- Files changed: `types/globals.d.ts` (new), `src/lib/billing/utils.ts` (modified)
- **Learnings for future iterations:**
  - Clerk type extensions: `CustomJwtSessionClaims` for server-side session claims (includes metadata from publicMetadata), `UserPublicMetadata` from `@clerk/types` for client-side useUser() typing
  - Pre-existing Biome formatting issues exist in billing/utils.ts — auto-format with `bunx biome format --write` before committing
  - The `isBetaFromMetadata` function uses defensive checks since publicMetadata is `Record<string, unknown>` at runtime
---

## 2026-03-14 - US-002
- Updated `isBetaAuth()` in `src/lib/billing/utils.ts` to use `isBetaFromMetadata(auth.sessionClaims?.metadata)` instead of `auth.has({ feature: FEATURE_BETA_ACCESS })`
- Extended `AuthWithHas` (billing/utils.ts) and `ClerkAuthLike` (trpc.ts) interfaces with `sessionClaims?: { metadata?: Record<string, unknown> }`
- Updated `getCurrentPlan` and `getUsage` in `src/server/api/routers/billing.ts` to use `isBetaFromMetadata(ctx.clerkAuth?.sessionClaims?.metadata)`
- Updated 3 beta check sites in `src/server/api/routers/ai.ts` (sendMessage, startReport, retryReport) to use `isBetaFromMetadata`
- Removed unused `FEATURE_BETA_ACCESS` import from billing/utils.ts and ai.ts, removed unused `getCurrentMonthYear` import from ai.ts
- Files changed: `src/lib/billing/utils.ts`, `src/server/api/trpc.ts`, `src/server/api/routers/billing.ts`, `src/server/api/routers/ai.ts`
- **Learnings for future iterations:**
  - Server-side beta detection path: `ctx.clerkAuth?.sessionClaims?.metadata` — Clerk's `auth()` puts publicMetadata in `sessionClaims.metadata`
  - `requireFeature` and `requirePlan` middleware work transitively through `hasFeatureAccess`/`hasPlanAccess` → `isBetaAuth` — updating `isBetaAuth` automatically fixes them
  - When removing imports, always check Biome reports unused import errors — the `getCurrentMonthYear` import was previously used alongside `FEATURE_BETA_ACCESS` checks but became unused
  - For test mocks: `clerkAuth` mock objects now need `sessionClaims: { metadata: { features: { beta_access: true } } }` to simulate beta users
---

## 2026-03-14 - US-003
- Switched all 5 client-side beta detection sites from `has({ feature: FEATURE_BETA_ACCESS })` to `isBetaFromMetadata(user?.publicMetadata)` via `useUser()` hook
- Files changed: `upgrade-prompt.tsx`, `billing-tab.tsx`, `pricing.tsx`, `usage-limit-banner.tsx`, `download-pdf-button.tsx`, `billing.ts`
- Removed `FEATURE_BETA_ACCESS` constant from `src/lib/constants/billing.ts` and all imports across the codebase
- **Learnings for future iterations:**
  - Clerk's `UserPublicMetadata` type augmentation in `types/globals.d.ts` does NOT fully propagate nested types through `useUser()` — TypeScript sees `features` as `{}` not `{ beta_access?: boolean }`. Must cast: `user?.publicMetadata as Record<string, unknown> | undefined` and use `isBetaFromMetadata()` helper instead of direct property access
  - Always use `isBetaFromMetadata()` for client-side beta checks — it handles runtime type safety and avoids TypeScript augmentation issues
  - Biome import ordering: `@/components/*` sorts before `@/lib/*` — place new imports accordingly
---

## 2026-03-14 - US-004
- Updated `billing.test.ts` beta test to use `sessionClaims: { metadata: { features: { beta_access: true } } }` with `has: () => false` instead of old `has: feature === "beta_access"` pattern
- Updated `entitlements.test.ts` beta bypass section to use `BETA_AUTH` with `sessionClaims` for beta detection and `has: () => false` to truly prove beta bypasses all gates (not just `FULL_ACCESS_AUTH`)
- Fixed pre-existing bug: `ai.sendMessage` rejection test was creating conversation with `noAccessCaller` but `createConversation` also requires `ai_chat` feature — changed to use `fullAccessCaller` for conversation creation
- Files changed: `tests/integration/billing.test.ts`, `tests/integration/entitlements.test.ts`
- **Learnings for future iterations:**
  - To mock beta in tests: pass `{ has: () => false, sessionClaims: { metadata: { features: { beta_access: true } } } }` as clerkAuth — this proves beta truly bypasses gates since `has()` denies everything
  - `createConversation` in the AI router uses `requireFeature(FEATURE_AI_CHAT)` — it's NOT just `protectedProcedure`. When testing rejection of sendMessage, create the conversation with a caller that HAS access first
  - Pre-existing test failures: `ai.test.ts` and `report-viewer.test.ts` have failures unrelated to beta detection (report limit issues)
---

## 2026-03-14 - US-005
- Created `/pricing` page at `src/app/(marketing)/pricing/page.tsx` with metadata (title: "Pricing | EdgeJournal")
- Created `PricingPageContent` client component at `src/app/(marketing)/pricing/_components/pricing-page-content.tsx`
- Renders Clerk's `<PricingTable>` with Terminal design theming via `appearance` prop (dark base theme, CLERK_THEME variables, 4px border radius)
- Page accessible to both authenticated and unauthenticated users (sits in marketing layout)
- `newSubscriptionRedirectUrl="/dashboard"` sends users to dashboard after checkout
- Added `data-testid` attributes for future E2E testing
- Files changed: `src/app/(marketing)/pricing/page.tsx` (new), `src/app/(marketing)/pricing/_components/pricing-page-content.tsx` (new)
- **Learnings for future iterations:**
  - Clerk `PricingTable` accepts `appearance` prop same as other Clerk components — use `dark` base theme + `CLERK_THEME` variables for consistency
  - PricingTable handles empty/no plans gracefully on its own — no custom fallback needed
  - The marketing layout already includes Navbar and Footer, so pricing page just needs content
---

## 2026-03-14 - US-006
- Added `PLAN_NONE` constant to replace `PLAN_FREE` in `src/lib/constants/billing.ts`
- Added deprecated `PLAN_FREE` re-export pointing to `PLAN_NONE` for backward compat (downstream files still import it until US-008/009/011)
- Removed free tier entry from `PLAN_METADATA`, added `PLAN_NONE` metadata entry (name: "No Plan", empty features)
- Updated `getEffectivePlan()` in `src/lib/billing/utils.ts` to return `PLAN_NONE` instead of `PLAN_FREE`
- Updated import in `billing/utils.ts` from `PLAN_FREE` to `PLAN_NONE`
- Files changed: `src/lib/constants/billing.ts`, `src/lib/billing/utils.ts`
- **Learnings for future iterations:**
  - When removing a widely-imported constant, add a deprecated re-export to avoid breaking the build — downstream stories will remove the import
  - `PLAN_METADATA` must have an entry for `PLAN_NONE` since billing-tab.tsx and pricing.tsx do lookups like `PLAN_METADATA[effectivePlan]` which would crash on undefined
  - The marketing landing page pre-renders at build time — any undefined metadata access crashes the build, not just at runtime
---

## 2026-03-14 - US-007
- Replaced `PLAN_FREE` import with `PLAN_NONE` in `src/server/api/routers/billing.ts`
- Updated `getCurrentPlan` fallback from `PLAN_FREE` to `PLAN_NONE` for users without clerkAuth
- Updated metadata fallback from `PLAN_METADATA[PLAN_FREE]` to `PLAN_METADATA[PLAN_NONE]`
- `getUsage` unchanged — it doesn't reference PLAN_FREE directly
- Files changed: `src/server/api/routers/billing.ts`
- **Learnings for future iterations:**
  - The deprecated `PLAN_FREE` re-export in billing.ts constants still exists — downstream stories (US-008/009/011) will clean up remaining references
---

## 2026-03-14 - US-008
- Removed free tier card from marketing pricing section, now shows only Starter ($10/mo) and Pro ($24/mo)
- Both plans now show "30-day free trial" badge
- Updated `PLAN_HIERARCHY` to use `PLAN_NONE` instead of `PLAN_FREE`
- Signed-out users: CTA says "Start [Plan] Trial" and redirects to `/pricing` after sign-up
- Signed-in users without plan: CTA links to `/pricing` for checkout (not `/settings?tab=billing`)
- Updated trial note copy: "All plans include a 30-day free trial" (removed "no credit card required")
- Replaced `PLAN_FREE` import with `PLAN_NONE`
- Changed grid from 3-column to 2-column (`lg:grid-cols-2`) and reduced max-width to `max-w-4xl`
- Removed `order-first` mobile reordering (no longer needed with 2 cards)
- Files changed: `src/app/(marketing)/_components/pricing.tsx`
- **Learnings for future iterations:**
  - When reducing from 3 to 2 pricing cards, update grid columns and max-width to keep cards well-proportioned
  - `SignUpButton`'s `forceRedirectUrl` should point to `/pricing` (Clerk checkout page) for paid plans, not `/settings?tab=billing`
---

## 2026-03-14 - US-009
- Removed free tier from billing settings plan comparison grid — now shows only Starter and Pro (2-column grid)
- Replaced `PLAN_FREE` import with `PLAN_NONE` throughout billing-tab.tsx
- Updated Current Plan card: shows "No active plan" with "Subscribe to get started" for users without a subscription
- Updated upgrade logic: uses `PLAN_NONE` instead of `PLAN_FREE` to determine if upgrade button should show
- Removed the `planSlug !== PLAN_FREE` guard in comparison cards since free tier card is gone
- Files changed: `src/app/(protected)/settings/_components/billing-tab.tsx`
- **Learnings for future iterations:**
  - The `hasPaidPlan` boolean (isProUser || isStarterUser) is useful for conditional display — differentiates between users who have a subscription vs those with PLAN_NONE
  - When removing a plan from comparison grid, also update `sm:grid-cols-N` to match the new count
---

## 2026-03-14 - US-010
- Verified upgrade-prompt.tsx already has no PLAN_FREE references (was never imported)
- Beta detection already uses `isBetaFromMetadata(user?.publicMetadata)` (done in US-003)
- Links already point to `/pricing` (was updated prior to this story)
- No code changes needed — all acceptance criteria already met by prior stories
- Files changed: none (only PRD updated)
- **Learnings for future iterations:**
  - Some stories may be no-ops if prior stories already addressed all concerns — still verify and mark as passing
  - Pre-existing Biome lint issues exist (CSS class sorting in settings-content.tsx, non-null assertion in billing.test.ts) — these are unrelated to billing cleanup
---

## 2026-03-14 - US-011
- Removed deprecated `PLAN_FREE` re-export from `src/lib/constants/billing.ts`
- Updated 3 comments in `src/server/api/routers/strategies.ts`: "free plan" → "regardless of plan"
- Updated comment in `src/server/api/routers/billing.ts`: "free/starter" → "no plan/starter"
- Updated `tests/integration/billing.test.ts`: test description and assertions from "free" to "none" plan
- Updated `tests/e2e/billing.spec.ts`: removed free card references, updated plan comparison assertions, fixed unauthenticated CTA assertion from "Start Free Trial" to "Start Starter Trial"
- Verified `src/lib/constants/errors.ts` has no free tier references
- Verified `usage-limit-banner.tsx`, `download-pdf-button.tsx`, AI router have no free tier references (only legitimate "free" usage like "free trial", "risk-free rate")
- AGENTS.md files only reference "port 3000 is free" (not plan-related) — no changes needed
- Files changed: `src/lib/constants/billing.ts`, `src/server/api/routers/strategies.ts`, `src/server/api/routers/billing.ts`, `tests/integration/billing.test.ts`, `tests/e2e/billing.spec.ts`
- **Learnings for future iterations:**
  - "Free" in cta.tsx/hero.tsx refers to the 30-day free trial, NOT the free tier plan — don't change these
  - Pre-existing Biome issues (CSS class sorting, non-null assertion, archive JSON formatting) are unrelated to billing cleanup and should not block commits
---

## 2026-03-14 - US-012
- Added plan-level entitlement test sections to `tests/integration/entitlements.test.ts`: PLAN_NONE denied, Starter features, Pro features
- Created `STARTER_FEATURES` and `PRO_FEATURES` arrays mapping plan tiers to feature sets
- PLAN_NONE tests: verified denial of trade creation, AI chat, strategy creation; confirmed read-only queries still work
- Starter plan tests: verified trade/strategy creation works, AI chat/reports denied (pro-only)
- Pro plan tests: verified all features work (trades, strategies, AI chat, AI reports)
- Confirmed billing.test.ts already fully updated (PLAN_NONE state tests from US-011)
- Confirmed zero PLAN_FREE references remain in any test files or fixtures
- Pre-existing failures in ai.test.ts and report-viewer.test.ts (report limit issues) are unrelated
- Files changed: `tests/integration/entitlements.test.ts`
- **Learnings for future iterations:**
  - Plan-level tests should use feature arrays (STARTER_FEATURES, PRO_FEATURES) with `withFeatures(...spread)` for clarity
  - entitlements.test.ts went from 17 to 29 tests with the plan-level additions
---
