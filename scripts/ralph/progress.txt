# Ralph Progress Log
Started: Fri Feb 28 2026
Feature: Admin Panel
---

## Codebase Patterns
- Bug report enums in schema: bugSeverityEnum (low, medium, high, critical), bugCategoryEnum (ui, data, performance, crash, other), bugReportStatusEnum (open, in_progress, resolved, closed)
- User role enum: userRoleEnum (user, admin)
- Constants barrel export from src/lib/constants/index.ts using `export * from "./file"`
- Biome formatter enforces single-line for short assignments - don't break lines unnecessarily
- Admin endpoints: use `adminProcedure` from `@/server/api/trpc` (extends protectedProcedure, checks role === 'admin')
- Middleware chaining: to add middleware that depends on a prior middleware's ctx additions, use `procedure.use()` inline (not standalone t.middleware)
- Admin router lives at src/server/api/routers/admin/index.ts — uses nested createTRPCRouter for sub-routers (e.g., admin.analytics.platformStats)
- For aggregating tokens from aiMessages, use `sql<string>\`coalesce(sum(...), 0)\`` since sum can return null
- Don't use `.default({})` on Zod objects with required fields that have field-level `.default()` — TypeScript rejects it
- Drizzle relational queries `findMany({ with: { user: true } })` auto-join via relations — no manual join needed
- Use correlated SQL subqueries for computed columns in list views: `sql<number>\`cast((select count(*) from "table" where ...) as integer)\``
- `ilike` from drizzle-orm for case-insensitive search; combine with `or()` for multi-field search
- Don't import schema tables only used in raw SQL strings — Biome flags unused imports
- Don't use raw `sql\`${column} < ${dateVar}\`` with Date objects — postgres driver rejects Date instances in raw SQL. Use Drizzle operators (`lt`, `gte`, etc.) instead.
- Admin layout: `(admin)` route group at app root with server-side role check via `api.settings.me()` + `redirect()`
- `settings.me` query returns current user's `{ id, role, name, email, imageUrl }` — use for role-based UI
---

## 2026-02-28 - US-001
- Created src/lib/constants/admin.ts with ADMIN_NAV_ITEMS, BUG_REPORT_STATUS_LABELS, BUG_REPORT_SEVERITY_LABELS, BUG_REPORT_CATEGORY_LABELS, status/severity color maps, ROLE_LABELS, ADMIN_TABLE_PAGE_SIZE, ADMIN_TABLE_PAGE_SIZE_OPTIONS
- Added admin error constants to src/lib/constants/errors.ts: ERR_ADMIN_FORBIDDEN, ERR_ADMIN_USER_NOT_FOUND, ERR_ADMIN_INVALID_STATUS_TRANSITION, ERR_ADMIN_BUG_REPORT_NOT_FOUND, ERR_ADMIN_ROLE_UPDATE_FAILED
- Exported admin constants from src/lib/constants/index.ts
- **Learnings for future iterations:**
  - Biome formatter wants short assignments on a single line - don't manually wrap
  - Bug report schema fields: title, description, severity, category, screenshotKey, pageUrl, userAgent, metadata (jsonb), status, createdAt
  - Nav items use icon names as strings (e.g., "LayoutDashboard") for flexibility — actual icon import happens in the component
---

## 2026-02-28 - US-002
- Created adminProcedure in src/server/api/trpc.ts extending protectedProcedure
- adminProcedure checks ctx.user.role === 'admin', throws FORBIDDEN with ERR_ADMIN_FORBIDDEN
- Files changed: src/server/api/trpc.ts
- **Learnings for future iterations:**
  - Cannot define adminMiddleware as standalone t.middleware because ctx.user doesn't exist in base context type. Must use protectedProcedure.use() inline to get proper ctx.user typing from authMiddleware.
  - adminProcedure is exported alongside publicProcedure and protectedProcedure for use in admin routers
---

## 2026-02-28 - US-003
- Created admin router directory: src/server/api/routers/admin/index.ts
- Created admin.analytics.platformStats query using adminProcedure
- Returns: totalUsers, activeUsersLast7d, totalTrades, openBugReports, aiConversationsLast7d, totalTokensUsed
- Active users computed by combining distinct userIds from trades and aiConversations in last 7 days
- Registered admin router in src/server/api/root.ts
- Files changed: src/server/api/routers/admin/index.ts (new), src/server/api/root.ts
- **Learnings for future iterations:**
  - Admin router uses nested createTRPCRouter pattern: `admin: createTRPCRouter({ analytics: createTRPCRouter({ ... }) })` for clean namespacing
  - Use `sql<string>\`coalesce(sum(col), 0)\`` for nullable aggregates — sum() returns null when no rows match
  - Drizzle `selectDistinct` works well for getting unique userIds without needing GROUP BY
  - Token counts stored as integer in aiMessages.tokensUsed — sum and convert to Number for the response
---

## 2026-02-28 - US-004
- Created tests/integration/admin.test.ts with 10 tests covering:
  - Admin middleware: FORBIDDEN for non-admin, UNAUTHORIZED for unauthenticated, allows admin access
  - Platform stats: all expected fields present, correct user count, trade count + active users from trades, open bug report count (excludes resolved), AI conversation + token counts, deduplication of active users across trades and AI, zero counts on empty database
- Files changed: tests/integration/admin.test.ts (new)
- **Learnings for future iterations:**
  - Use `createTestUser({ role: "admin" })` to create admin users — the fixture supports role option
  - Store user references from `createTestUser()` in `let adminUser: User` variables to avoid re-querying the DB in each test
  - Tests that insert data into related tables (trades, bugReports, aiConversations, aiMessages) can use `getTestDb()` and `schema` directly — no need for tRPC callers for raw inserts
  - Bug reports with status "open" are counted; "resolved" and "closed" are not — matches the `eq(bugReports.status, "open")` filter in platformStats
  - Biome auto-formats short function calls to single line — run `bunx biome check --write` to fix formatting
---

## 2026-02-28 - US-005
- Added bugReports sub-router to admin router with 3 endpoints:
  - `admin.bugReports.list` — offset pagination, filtering by status/category/severity, returns user info, sorted newest first
  - `admin.bugReports.getById` — single bug report with full details and user info
  - `admin.bugReports.updateStatus` — validates status transitions (open→in_progress→resolved→closed with allowed shortcuts)
- Files changed: src/server/api/routers/admin/index.ts
- **Learnings for future iterations:**
  - Don't use `.default({})` on a Zod object that has required fields with `.default()` — TypeScript will reject it. Just use the object schema directly, and callers must pass at least `{}`.
  - Drizzle `query.bugReports.findMany({ with: { user: true } })` automatically joins via the relations defined in schema.ts — no need for manual joins.
  - Status transition validation: store valid transitions as a record map and check `allowed.includes(input.status)` — clean and extensible.
  - Bug report list endpoint maps user info to a subset (id, name, email, imageUrl) to avoid leaking full user data.
---

## 2026-02-28 - US-006
- Created tests/integration/admin-bug-reports.test.ts with 18 tests covering:
  - List: returns bug reports with user info, sorts newest first, pagination, combined filters
  - Filter by: status, category, severity
  - getById: returns full details with user info, NOT_FOUND for non-existent
  - updateStatus: open→in_progress→resolved→closed transitions, reopen closed→open, rejects invalid transitions (open→resolved), NOT_FOUND for non-existent
  - Auth: non-admin rejected on list, getById, updateStatus
- Files changed: tests/integration/admin-bug-reports.test.ts (new)
- **Learnings for future iterations:**
  - Tests that build on each other (sequential status transitions) work well within a single describe block — Vitest runs tests in order within a describe
  - Use `db.insert().returning()` to capture inserted IDs for later assertions — store in a shared array variable
  - Biome enforces single-line for `new Date(value).getTime()` — don't break across lines
---

## 2026-02-28 - US-007
- Added users sub-router to admin router with 3 endpoints:
  - `admin.users.list` — offset pagination, search by name/email (ilike), returns account count, trade count, last active date via correlated subqueries, sorted newest first
  - `admin.users.getById` — single user with accounts, recent trades (last 10, soft-delete filtered), AI conversation count, bug report count
  - `admin.users.updateRole` — validates user exists, updates role (user ↔ admin), returns updated user subset
- Files changed: src/server/api/routers/admin/index.ts
- **Learnings for future iterations:**
  - Use correlated SQL subqueries for computed columns (account count, trade count, last active) — avoids N+1 and is efficient for paginated list views
  - `sql<number>\`cast((select count(*) from "table" where ...) as integer)\`` for inline counts in select
  - `sql<Date | null>\`greatest(...)\`` works for computing last active across multiple tables
  - `ilike` from drizzle-orm for case-insensitive search — combine with `or()` for multi-field search
  - Always include `isNull(trades.deletedAt)` when counting or listing trades (soft-delete pattern)
  - Don't import schema tables you only reference in raw SQL strings — Biome flags unused imports
---

## 2026-02-28 - US-008
- Created tests/integration/admin-users.test.ts with 16 tests covering:
  - List: returns users with account/trade counts, correct counts for data-rich user, search by email, search by name (case insensitive), empty search results, pagination, sort by newest first
  - getById: returns full user details with accounts, recent trades, AI conversation count, bug report count, zero counts for user without data, NOT_FOUND for non-existent user
  - updateRole: promote user to admin, demote admin to user, NOT_FOUND for non-existent user
  - Auth: non-admin rejected on list, getById, updateRole
- Files changed: tests/integration/admin-users.test.ts (new)
- **Learnings for future iterations:**
  - Biome enforces single-line for short `.find()` callbacks — don't break across lines
  - When seeding test data with multiple accounts, use `.returning()` to capture the first account ID for trade inserts
  - Test user with rich data (accounts, trades, AI convos, bug reports) should be created in beforeAll to test both getById detail views and list computed counts
---

## 2026-02-28 - US-009
- Added ai sub-router to admin router with 3 endpoints:
  - `admin.ai.listConversations` — offset pagination, filtering by mode/status, returns user info + message count + token count via correlated subqueries, innerJoin with users for user info, sorted newest first
  - `admin.ai.getConversation` — single conversation with all messages (ordered asc by createdAt), user info via relational query
  - `admin.ai.usageStats` — total tokens, tokens by model, conversations by mode, daily usage for last 30 days using date_trunc
- Added ERR_ADMIN_CONVERSATION_NOT_FOUND to src/lib/constants/errors.ts
- Files changed: src/server/api/routers/admin/index.ts, src/lib/constants/errors.ts
- **Learnings for future iterations:**
  - For list views needing computed counts + joined user info, use `ctx.db.select()` with `.innerJoin()` for user data + correlated SQL subqueries for counts — more efficient than relational queries for paginated lists
  - `date_trunc('day', timestamp)::date::text` converts timestamps to date strings for daily grouping in PostgreSQL
  - Biome enforces single-line assignment for `messageCount: sql<number>\`...\`` — don't break the sql tagged template onto a new line
  - `sql\`${column} is not null\`` for null filtering in raw SQL within drizzle select
---

## 2026-02-28 - US-010
- Created tests/integration/admin-ai.test.ts with 19 tests covering:
  - listConversations: returns conversations with user info, includes message count and token count, filter by mode (chat/report), filter by status, pagination, sort by newest first
  - getConversation: returns conversation with messages, messages ordered by createdAt ascending, NOT_FOUND for non-existent
  - usageStats: total tokens used, tokens by model, conversations by mode, daily usage for last 30 days, zero tokens when no messages exist
  - Auth: non-admin rejected on listConversations, getConversation, usageStats; unauthenticated rejected
- Files changed: tests/integration/admin-ai.test.ts (new)
- **Learnings for future iterations:**
  - When tests truncate tables mid-file (e.g., "zero counts" test), subsequent tests in the same file need to re-create users — don't rely on beforeAll users after a truncation
  - Biome flags unused variables even in test setup — if a caller is only used for seeding data, don't assign it to a variable
  - Token counts from correlated subqueries match exactly when seeded with specific values — good for precise assertions
---

## 2026-02-28 - US-011
- Added 4 analytics endpoints to admin.analytics sub-router:
  - `growth` — daily new users + cumulative total for last 30 days using date_trunc and a base count query for users before the window
  - `tradingActivity` — daily trade count, total P&L, avg trade size for last 30 days with soft-delete filtering
  - `topTraders` — top 10 users by total P&L using innerJoin with users, grouped by userId/name/email, ordered desc by sum(netPnl)
  - `accountBreakdown` — count of accounts grouped by accountType
- Files changed: src/server/api/routers/admin/index.ts
- **Learnings for future iterations:**
  - For cumulative totals in growth charts, first query the base count before the time window, then accumulate over daily rows — avoids window functions
  - `avg(abs(column))` works well for avg trade size — using abs ensures both winning and losing trades contribute positively
  - Biome formatting for `.where(and(...))`: if args fit on one line Biome wraps `.where(\n  and(...),\n)` — use `bunx biome check --write` to auto-fix
  - `trades.netPnl` is the column to use for P&L aggregations (includes fees), not `realizedPnl`
  - Always include `isNull(trades.deletedAt)` when aggregating trade data
---

## 2026-02-28 - US-012
- Created tests/integration/admin-analytics.test.ts with 22 tests covering:
  - growth: daily new user counts, newUsers + cumulativeTotal fields, correct cumulative total
  - tradingActivity: daily trade activity, tradeCount/totalPnl/avgTradeSize fields, correct trade count, correct total P&L, soft-deleted trades excluded
  - topTraders: ranked by P&L, correct fields, correct ranking order, limit to 10
  - accountBreakdown: counts by type, correct fields, correct per-type counts, reflects new accounts
  - Auth: non-admin rejected on all 4 endpoints, unauthenticated rejected
- Fixed bug in admin.analytics.growth: raw `sql\`${users.createdAt} < ${thirtyDaysAgo}\`` passed Date object to postgres driver which expects strings. Changed to `lt(users.createdAt, thirtyDaysAgo)` using Drizzle operator.
- Files changed: tests/integration/admin-analytics.test.ts (new), src/server/api/routers/admin/index.ts (bug fix)
- **Learnings for future iterations:**
  - Never use raw `sql` template with Date objects as parameters — postgres driver rejects them. Use Drizzle operators (`lt`, `gte`, `eq`) which handle type conversion.
  - Import `eq` from drizzle-orm in test files when you need to query tables directly with `.where(eq(...))`.
---

## 2026-02-28 - US-013
- Added system sub-router to admin router with health endpoint
- `admin.system.health` returns: databaseStatus, appVersion, tableCounts (users, trades, accounts, aiConversations, aiMessages, bugReports), lastActivity (lastSignup, lastTrade, lastAiConversation)
- Database connection checked via `sql\`select 1\`` with try/catch
- App version read from package.json using `readFileSync` at `process.cwd()`
- Row counts use `count()` from drizzle-orm, last activity dates use `max()` from drizzle-orm
- Files changed: src/server/api/routers/admin/index.ts
- **Learnings for future iterations:**
  - Use `max()` from drizzle-orm for last activity date queries — returns the most recent date from a column
  - Biome enforces line breaks on chained `.select().from()` when the select object is long — split across lines
  - For database health checks, `ctx.db.execute(sql\`select 1\`)` is a lightweight connectivity test
  - `readFileSync` from `node:fs` with `resolve(process.cwd(), "package.json")` works reliably for reading app version in tRPC server context
---

## 2026-02-28 - US-014
- Created tests/integration/admin-system.test.ts with 14 tests covering:
  - Auth: non-admin rejected with FORBIDDEN, unauthenticated rejected with UNAUTHORIZED
  - Response fields: all top-level fields present, databaseStatus is "connected", appVersion is a non-"unknown" string, all tableCounts fields, all lastActivity fields
  - Database counts: correct user count (2 from beforeAll), zero for empty tables, accurate counts after inserting accounts/trades/bugReports/aiConversations/aiMessages
  - Last activity: lastSignup present, lastTrade present after data insertion, lastAiConversation present after data insertion, null for trade/AI when no data exists
- Files changed: tests/integration/admin-system.test.ts (new)
- **Learnings for future iterations:**
  - System health tests build on each other within describes — data inserted in "accurate counts" test is visible in subsequent "last activity" tests since Vitest runs tests in order within a describe
  - The health endpoint returns `databaseStatus` as a union type "connected" | "error" — test the happy path (connected) since we can't easily break the DB connection in tests
  - `appVersion` comes from package.json at process.cwd() — always a valid string in the test environment
---

## 2026-02-28 - US-015
- Created admin route group `(admin)` with layout, loading state, sidebar, and placeholder page
- Added `settings.me` tRPC query returning `{ id, role, name, email, imageUrl }` for client-side role access
- Admin layout (server component) checks user role via `api.settings.me()` and redirects non-admins to `/dashboard`
- Admin sidebar uses `ADMIN_NAV_ITEMS` from constants with icon map pattern (string icon names → Lucide components)
- Loading state shows spinner with "Verifying admin access..." while role is being checked
- Terminal design: monospace labels, chartreuse accent for "Admin" badge and active nav items, muted nav items
- Files changed: src/server/api/routers/settings.ts, src/app/(admin)/layout.tsx (new), src/app/(admin)/loading.tsx (new), src/app/(admin)/_components/admin-sidebar.tsx (new), src/app/(admin)/admin/page.tsx (new)
- **Learnings for future iterations:**
  - Admin layout is a separate route group `(admin)` at app root, parallel to `(protected)` — gives completely independent layout
  - `settings.me` query returns current user info including role — use this for any client-side role checks
  - Admin layout uses server-side role check with `redirect()` from next/navigation — no client-side loading flash for non-admins
  - Icon map pattern: ADMIN_NAV_ITEMS stores icon names as strings, sidebar maps them to Lucide components via `ICON_MAP` record
  - Active nav detection: exact match for `/admin` (overview), `startsWith` for sub-pages like `/admin/bug-reports`
---

## 2026-02-28 - US-016
- Created `AdminGuard` client component for belt-and-suspenders admin route protection
- Guard takes `initialRole` from server to avoid loading flash, then validates client-side via `settings.me` query
- Redirects non-admins to `/dashboard` via `useRouter().replace()` if role changes mid-session
- Updated admin layout to wrap children in `AdminGuard` alongside existing server-side redirect
- Files changed: src/app/(admin)/_components/admin-guard.tsx (new), src/app/(admin)/layout.tsx
- **Learnings for future iterations:**
  - Server-side layout `redirect()` is the primary guard; client-side `AdminGuard` is secondary for mid-session role changes
  - Pass `initialRole` from server component to client guard to avoid double-fetch and loading flash
  - Use `staleTime: 30_000` on the `settings.me` query to avoid excessive refetching during admin navigation
---

## 2026-02-28 - US-017
- Added conditional Admin navigation link to app-sidebar.tsx footer
- Uses existing `settings.me` tRPC query to check `me?.role === "admin"`
- Shield icon (already imported) with link to /admin
- Active state uses `pathname.startsWith("/admin")` for sub-page highlighting
- Hidden for non-admin users (conditional rendering with `&&`)
- Files changed: src/app/(protected)/_components/app-sidebar.tsx
- **Learnings for future iterations:**
  - `settings.me` query is already available in the sidebar — no need to create a new endpoint for role checking
  - Shield icon was already imported in app-sidebar.tsx (used for Prop nav item)
  - Admin link placed in footer (after Settings, before UserButton) to distinguish it from main navigation items
  - Use `pathname.startsWith("/admin")` for active state since admin has sub-routes
---

## 2026-02-28 - US-018
- Replaced placeholder admin overview page with full dashboard using `admin.analytics.platformStats` endpoint
- 6 stat cards: Total Users, Active Users (7d), Total Trades, Open Bug Reports, AI Conversations (7d), Total AI Tokens
- Each card links to its detailed admin panel page
- Compact number formatting for large values (K/M suffixes for tokens)
- Loading skeleton, error state, and hover effects on cards
- Terminal design: monospace labels, chartreuse accent values, card hover with primary border
- Files changed: src/app/(admin)/admin/page.tsx
- **Learnings for future iterations:**
  - Admin overview is a client component ("use client") since it fetches data via tRPC React hooks
  - Biome auto-formats long `.useQuery()` chains to multi-line — let it handle the formatting
  - Card config array with `as const` gives type-safe key access to the data object
  - Use `Link` from next/link for card navigation — whole card is clickable
---

## 2026-02-28 - US-019
- Created /admin/bug-reports page with full list view
- Table with columns: title, category, severity, status, reporter, date
- Filter dropdowns for status, category, and severity using Select components
- Severity badges with color coding from BUG_REPORT_SEVERITY_COLORS
- Status badges with dropdown for inline status updates via updateStatus mutation
- Click row to expand/collapse detail view showing: description, page URL, user agent, screenshot indicator, raw metadata (JSON)
- Pagination with prev/next buttons and page counter
- Loading skeleton rows, empty state, and error state
- Terminal design: monospace table, data-dense layout, chartreuse accents
- Files changed: src/app/(admin)/admin/bug-reports/page.tsx (new)
- **Learnings for future iterations:**
  - Use `Fragment (<>)` wrapper for expandable table rows — two adjacent `<tr>` elements (main row + detail row) need a common parent inside `.map()`
  - Status transition dropdown: duplicate STATUS_TRANSITIONS map on client matching server validation — keeps UX in sync with backend constraints
  - `e.stopPropagation()` on DropdownMenuTrigger onClick prevents row expand/collapse when clicking the status dropdown
  - Biome auto-formats single-line ternaries and nullish coalescing — run `bunx biome check --write` after writing JSX
---

## 2026-02-28 - US-020
- Created /admin/users page with full list view
- Table with columns: name, email, role, accounts count, trades count, joined date, last active
- Search bar with Enter key support and Clear button for name/email filtering
- Role badges with color coding: admin=chartreuse (primary), user=gray
- Click row navigates to /admin/users/[userId] detail page via `router.push()`
- Pagination with prev/next buttons and page counter
- Loading skeleton rows, empty state, and error state
- Added ROLE_COLORS constant to src/lib/constants/admin.ts
- Terminal design: monospace table, data-dense layout
- Files changed: src/app/(admin)/admin/users/page.tsx (new), src/lib/constants/admin.ts
- **Learnings for future iterations:**
  - Biome formats short lucide-react imports onto a single line — always run `bunx biome check --write` after creating new files
  - Search pattern: use separate `searchInput` (for controlled input) and `search` (for query) state to avoid re-fetching on every keystroke
  - `router.push()` for row click navigation to detail pages — more appropriate than `Link` wrapping table rows
  - ROLE_COLORS constant follows same pattern as BUG_REPORT_STATUS_COLORS: `"text-{color} bg-{color}/10"`
---

## 2026-02-28 - US-021
- Created /admin/users/[userId] user detail page
- Header: avatar (Next.js Image), name, email, role badge, joined date
- Role toggle button with AlertDialog confirmation dialog (promote to admin / demote to user)
- Summary stat cards: accounts, recent trades, AI conversations, bug reports
- Accounts table: name, type (with color badges), broker, active status, created date
- Recent trades table: symbol, direction (with trending icons), status, entry/exit dates, net P&L (color-coded)
- AI Conversations and Bug Reports summary sections with counts
- Loading skeleton, error state, back-to-users link
- Terminal design: monospace values, data-dense layout, chartreuse accents
- Files changed: src/app/(admin)/admin/users/[userId]/page.tsx (new)
- **Learnings for future iterations:**
  - Use Next.js `Image` component instead of `<img>` — Biome warns about LCP/bandwidth for raw img elements
  - `useParams()` returns `Record<string, string | string[]>` — cast `params.userId as string` for dynamic route params
  - AlertDialog from Radix: control with `open`/`onOpenChange` state, use `AlertDialogAction` onClick for the confirm action
  - ACCOUNT_TYPE_LABELS/COLORS not centralized in constants — duplicated locally (also in app-sidebar and settings). Consider centralizing if it comes up again.
  - `admin.users.getById` returns `recentTrades` with string P&L values — parse with `Number.parseFloat()` for color logic
---

## 2026-02-28 - US-022
- Created /admin/ai page with full AI usage monitoring
- Top stats: total conversations, total reports, total tokens, avg tokens per conversation (4 stat cards)
- Token usage bar chart using AG Charts (ag-charts-react) showing daily token consumption for last 30 days
- Conversations table: user, title, mode (chat/report), status, messages count, tokens, date
- Filter dropdowns for mode (chat/report) and status (active/generating/complete/failed)
- Click row to expand and view full conversation messages inline with role coloring
- Ice blue (#00d4ff) accent for AI section per design system
- Files changed: src/app/(admin)/admin/ai/page.tsx (new)
- **Learnings for future iterations:**
  - AG Charts pattern: use `AgCartesianChartOptions<DataType>` with `useMemo`, `AgCharts` component with `style={{ height: N }}`
  - Chart theme: transparent background, `#64748b` label color, `JetBrains Mono` font, `#1e293b` line stroke, `#ffffff08` gridline
  - AI conversation mode can be null — use nullish coalescing for MODE_LABELS/MODE_COLORS lookup: `convo.mode ?? ""`
  - `getConversation` query with `enabled: !!expandedId` prevents fetching until a row is expanded
  - Biome flags unused computed variables even if derived from used data — remove or prefix with `_`
---

## 2026-02-28 - US-023
- Created /admin/analytics page with full platform analytics dashboard
- User Growth chart: bar chart for daily new users + line overlay for cumulative total (dual Y-axes)
- Trading Activity chart: bar chart for daily trade count + line overlay for total P&L (dual Y-axes)
- Top 10 Traders leaderboard table: ranked by total P&L with color-coded profit/loss values
- Account Type Breakdown donut chart: demo, live, challenge, funded with distinct colors
- All charts use AG Charts (ag-charts-community) with Terminal design theming
- Loading skeletons, error state, empty state for each section
- Terminal design: chartreuse accent for positive metrics, red for losses, monospace throughout
- Files changed: src/app/(admin)/admin/analytics/page.tsx (new)
- **Learnings for future iterations:**
  - AG Charts dual Y-axis pattern: use `keys: ["fieldName"]` on each number axis to bind series to specific axes
  - `AgPolarChartOptions` for donut/pie charts — import from `ag-charts-community`
  - `name` field from `users` table is `string | null` — always account for null in TopTrader interfaces
  - Biome auto-removes unused imports with `--write --unsafe` flag — run after initial file creation to clean up
  - Account type labels/colors duplicated across admin pages — consider centralizing in constants if pattern continues
---

## 2026-02-28 - US-024
- Created /admin/system page with full system health dashboard
- Database connection status indicator: green CheckCircle for connected, red XCircle for error
- App version display from package.json with "v" prefix
- 6 stat cards for database row counts: users, trades, accounts, AI conversations, AI messages, bug reports
- Last activity section: last signup, last trade, last AI conversation with formatted timestamps
- Loading skeletons, error state
- Terminal design: green (#00ff88) for healthy, red (#ff3b3b) for issues, chartreuse primary accent, monospace throughout
- Files changed: src/app/(admin)/admin/system/page.tsx (new)
- **Learnings for future iterations:**
  - System health page is simpler than analytics pages — single endpoint, no charts, just stat cards and status indicators
  - `CheckCircle`/`XCircle` from lucide-react work well for binary status indicators
  - `toLocaleString()` with date options gives clean timestamp formatting for "last activity" displays
  - Divide-y on a container with child rows gives clean separator lines without manual borders
---
