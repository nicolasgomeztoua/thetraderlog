# Ralph Progress Log
Started: Wed Jan 14 22:34:43 +01 2026
---

## Codebase Patterns
- Use `getDateStringInTimezone(trade.entryTime, userTimezone)` for date grouping
- Always group trades by ENTRY TIME, not exit time
- Frontend sends date strings via `toDateString()`, backend converts via `getDayBoundsInTimezone()`
- Timezone utilities are in `src/lib/shared/timezone.ts`
- User timezone fetched via `getUserTimezone(ctx.db, ctx.user.id)` helper

---

## 2026-01-14 - US-001
- **What was implemented:** Created comprehensive timezone system documentation
- **Files changed:**
  - Created `docs/TIMEZONE_SYSTEM.md`
- **Learnings for future iterations:**
  - Documentation should include a decision tree for quick reference
  - Include both correct and incorrect code examples to make patterns clear
  - The codebase has two different date handling patterns: trade timestamps (UTC converted to user TZ) vs calendar dates (stored as UTC midnight, displayed as-is)
---

## 2026-01-14 - US-006
- **What was implemented:** Created timezone-aware date string generator function
- **Files changed:**
  - Modified `src/lib/shared/timezone.ts` - added `generateDateStringsInTimezone()`
- **Learnings for future iterations:**
  - Use UTC arithmetic to avoid Date object timezone ambiguity when generating date strings
  - Pattern: get "today" in user timezone via formatInTimeZone, then do all arithmetic in UTC milliseconds
  - Auto-exports work via `export * from "./timezone"` in index.ts - no need to manually add new exports
  - DST handling: using Date.UTC + ms arithmetic sidesteps DST issues because we never touch local time
---

## 2026-01-14 - US-007
- **What was implemented:** Created date string display utilities for calendar rendering
- **Files changed:**
  - Modified `src/lib/shared/timezone.ts` - added `getDayOfWeekFromDateString()`, `getMonthFromDateString()`, `formatDateString()`
- **Learnings for future iterations:**
  - Pattern for day-of-week from date string: parse to UTC Date, use getUTCDay() - avoids timezone shifting
  - Pattern for month from date string: just parse the string directly with split("-") - no Date object needed
  - Pattern for formatting date strings: create UTC Date at noon (12:00) and use formatInTimeZone with "UTC" timezone
  - Setting noon (12:00) UTC avoids edge cases where midnight might shift to adjacent day in some implementations
---

## 2026-01-15 - US-003
- **What was implemented:** Fixed getOvertradingAnalysis to group trades by entry time instead of exit time
- **Files changed:**
  - Modified `src/server/api/routers/analytics.ts` - changed date grouping from `trade.exitTime` to `trade.entryTime`
- **Learnings for future iterations:**
  - PRD procedure names may not exactly match actual codebase names (PRD said "getOvertradeMetrics" but actual is "getOvertradingAnalysis")
  - userTimezone is typically fetched alongside beThreshold in analytics procedures - check for existing fetches before adding new ones
  - When changing exitTime to entryTime for grouping, also update the null check condition
---

## 2026-01-15 - US-004
- **What was implemented:** Fixed getBehavioralPatterns to group trades by entry time instead of exit time
- **Files changed:**
  - Modified `src/server/api/routers/analytics.ts` - changed date grouping in "Overtrading Tendency" from `trade.exitTime` to `trade.entryTime`
- **Learnings for future iterations:**
  - PRD procedure names may not match actual codebase names (PRD said "getPsychometrics" but actual is "getBehavioralPatterns")
  - Similar pattern to US-003: search for `exitTime` usages when looking for date grouping issues
  - The "Overtrading Tendency" calculation in getBehavioralPatterns is the only place in that procedure that groups by date
---

## 2026-01-15 - US-005
- **What was implemented:** Audited all analytics date grouping procedures
- **Files changed:**
  - Modified `src/server/api/routers/analytics.ts` - fixed `getSymbolTrend` to use entryTime instead of exitTime for month grouping
- **Audit results:**
  - ✅ getCalendarData - uses `getDateStringInTimezone(trade.entryTime, userTimezone)`
  - ✅ getPerformanceByDayOfWeek - uses `getDayOfWeekInTimezone(trade.entryTime, userTimezone)`
  - ✅ getPerformanceByHour - uses `getHourInTimezone(trade.entryTime, userTimezone)`
  - ✅ getPerformanceBySession - uses `getHourInTimezone(trade.entryTime, userTimezone)`
  - ✅ getPerformanceByMonth - uses `getMonthStringInTimezone(trade.entryTime, userTimezone)`
  - ℹ️ getEquityCurve - uses exitTime intentionally (plots cumulative P&L at trade close time, not date grouping)
  - 🔧 getSymbolTrend - FIXED: was using exitTime for month grouping, changed to entryTime
- **Learnings for future iterations:**
  - Equity curve is an exception to the "entry time for grouping" rule - it plots P&L at the moment of trade realization (exit)
  - When auditing for date grouping issues, search for all uses of timezone utility functions (getMonthStringInTimezone, getDateStringInTimezone, etc.)
  - The `exitTime` null check was redundant in getSymbolTrend since we already filter for closed trades with netPnl
---

## 2026-01-15 - US-010
- **What was implemented:** Created integration tests for timezone date grouping
- **Files changed:**
  - Created `tests/integration/analytics/timezone-grouping.test.ts`
- **Test coverage:**
  - Trade entered 11 PM EST closed next day appears on entry date (EST)
  - getCalendarData returns correct dates for user timezone
  - getOvertradingAnalysis matches calendar data for same days
  - Daily journal trade grouping respects user timezone
  - Pacific timezone (UTC-8) edge cases
  - Pacific/Auckland (UTC+13) ahead-of-UTC edge cases
  - Cross-procedure consistency (calendar vs overtrading analysis)
- **Learnings for future iterations:**
  - getOvertradingAnalysis returns `byTradeCount` (array of buckets with tradeCount, days, etc.) not `totalTrades` - derive totals from buckets
  - Use existing `timezone.test.ts` as pattern reference - it has similar setup patterns
  - Docker must be running for integration tests (Testcontainers PostgreSQL)
  - Pattern: use `createTestUser()` + `createTestAccount()` + insert `userSettings` to set specific timezone
  - Testing "11 PM EST closed next day" scenario: use UTC timestamp that's next day in UTC but same day in user TZ
---
