# Ralph Progress Log
Started: Sat Mar 14 16:20:39 WET 2026
---

## 2026-03-14 - US-001
- Added `lastBarAt` column (timestamp with timezone, nullable) to `candle_cache` table in `src/server/db/schema.ts`
- Removed outdated `// No expiresAt` comment
- Schema pushed successfully with `bun run db:push`
- Files changed: `src/server/db/schema.ts`
- **Learnings for future iterations:**
  - The `candle_cache` table uses a unique index on `(symbol, interval, date)` as the cache key — US-002 will need to use `onConflictDoUpdate` targeting this constraint
  - `lastBarAt` is nullable for backward compat with existing rows — US-002 needs backfill logic for null values
---

## 2026-03-14 - US-002
- Implemented smart append logic in `getOHLCBars` in `src/lib/market-data/service.ts`
- Added helper functions: `isToday()`, `isCacheStale()`, `extractLastBarAt()`
- On cache hit for today's date, checks if lastBarAt > 5min old → re-fetches from Databento
- Historical dates return cached data immediately (never stale)
- Changed `onConflictDoNothing()` to `onConflictDoUpdate()` targeting `[symbol, interval, date]` unique index
- Populates `lastBarAt` from last bar's timestamp on all writes (inserts and updates)
- Backfill: if existing row has null lastBarAt, extracts it from parsed bars JSON before staleness check
- Files changed: `src/lib/market-data/service.ts`
- **Learnings for future iterations:**
  - `onConflictDoUpdate` needs `target` array matching the unique index columns, and `set` object for columns to update
  - The `extractLastBarAt` and `isCacheStale` functions are exported-friendly for unit testing (US-007)
  - Staleness threshold is 5 minutes (STALENESS_THRESHOLD_MS constant) — easily adjustable
---

## 2026-03-14 - US-003
- Eliminated redundant derived-interval cache storage in `getOHLCBars`
- Added `BaseInterval` type (`'1min' | '1h'`) and `getBaseInterval()` mapping function
- `getOHLCBars` now always caches using base interval, then aggregates to requested interval before returning
- Updated `aggregateBars` to short-circuit for both `1min` and `1h` (base intervals = no-op)
- Files changed: `src/lib/market-data/service.ts`
- **Learnings for future iterations:**
  - `getOHLCBars` handles the base→derived aggregation internally now — callers like `getOHLCForTimeRange` and `getOHLCForChart` don't need changes since they pass intervals through to `getOHLCBars` which handles everything
  - `fetchFromDatabento` still has its own `aggregateBars` call but it's harmless since it only receives base intervals now
  - US-004 may be largely satisfied already since `getOHLCBars` handles aggregation — verify before making changes
---

## 2026-03-14 - US-004
- Verified that `getOHLCForChart`, `getOHLCForTimeRange`, and `getFullDayBars` all correctly use base interval fetch + aggregation
- `getOHLCForChart` → `getOHLCForTimeRange` → `getOHLCBars` (which maps to base interval internally) — no code changes needed
- `getFullDayBars` already hardcodes `"1min"` at line 610 — confirmed no changes needed
- No files changed (US-003 already satisfied all acceptance criteria)
- Build passes, typecheck passes
- **Learnings for future iterations:**
  - When `getOHLCBars` handles aggregation internally, downstream callers automatically benefit — no need to duplicate logic
  - Pre-existing Biome lint errors exist (unused import in ai.ts, non-null assertion in billing test, formatting in archive JSON) — these are unrelated to market data work
---

## 2026-03-14 - US-005
- Verified that the marketData tRPC router works seamlessly with the progressive cache and base-interval-only storage
- All 4 endpoints confirmed working correctly:
  - `getChartData` → `getOHLCForChart` → `getOHLCForTimeRange` → `getOHLCBars` (handles base interval mapping + aggregation)
  - `getFullDayChartData` → `getFullDayBars` → `getOHLCBars("1min")` (already base interval)
  - `analyzePriceAction` → `getOHLCForTimeRange("1h")` → `getOHLCBars` (1h is base interval, no mapping needed)
  - `getOHLC` (deprecated) → `getOHLCForTimeRange` → `getOHLCBars` (handles base interval mapping)
- `CacheInterval` type still includes all user-requestable intervals (1min, 5min, 15min, 30min, 1h, 4h)
- No code changes needed — US-003's internal base interval mapping in `getOHLCBars` handles everything
- Build passes, typecheck passes
- **Learnings for future iterations:**
  - The router is a thin layer — it just validates input and delegates to service functions. When the service layer handles complexity internally (like base interval mapping), the router needs no changes.
  - Pre-existing Biome errors (4 errors, 2 warnings) are unrelated to market data work and exist in ai.ts, billing test, and archive JSON files
---

## 2026-03-14 - US-006
- Added structured console.info logging with `[market-data]` prefix to `getOHLCBars` in `src/lib/market-data/service.ts`
- Four log types implemented:
  - Cache hit (historical): `[market-data] cache hit: ES 1min 2024-12-15 (387 bars)`
  - Cache miss (new fetch): `[market-data] cache miss: ES 1min 2024-12-15 → fetching from Databento`
  - Smart refresh (same-day re-fetch): `[market-data] cache refresh: ES 1min 2024-03-14 (was 240 bars → now 387 bars)`
  - Derived interval aggregation: `[market-data] aggregating: ES 1min → 5min (387 → 78 bars)`
- No external dependencies — plain console.info with greppable prefix
- Files changed: `src/lib/market-data/service.ts`
- **Learnings for future iterations:**
  - Use `cached.barCount` from DB instead of re-parsing `cached.bars` JSON to get bar count — avoids double parse and handles corrupted entries safely
  - Aggregation logging fires on both cache hit and API fetch paths when interval !== baseInterval
---

## 2026-03-14 - US-007
- Created unit tests for market data aggregation and staleness detection: `tests/unit/market-data-aggregation.test.ts`
- Exported `aggregateBars`, `extractLastBarAt`, `isCacheStale`, `isToday`, `STALENESS_THRESHOLD_MS` from `src/lib/market-data/service.ts`
- 22 tests covering:
  - `aggregateBars`: 1min→5min, 1min→15min, 1min→30min, 1h→4h, multiple buckets, empty/single bar, volume summing, base interval passthrough
  - `extractLastBarAt`: normal, empty, single bar cases
  - `isToday`: today, yesterday, historical dates
  - `isCacheStale`: historical (never stale), today+null, today+old, today+recent, exact threshold boundary
- Files changed: `src/lib/market-data/service.ts`, `tests/unit/market-data-aggregation.test.ts`
- **Learnings for future iterations:**
  - service.ts imports `@/env` and `@/server/db` which trigger env validation — unit tests must mock these modules with `vi.mock()` before importing
  - Unit tests run with `bunx vitest run --config vitest.config.unit.ts` (no Docker needed)
  - Biome enforces import ordering — `type` imports must be sorted alphabetically with non-type imports
---

## 2026-03-14 - US-008
- Created integration tests for progressive cache behavior: `tests/integration/market-data-cache.test.ts`
- 6 tests covering: cache entry creation with lastBarAt, historical cache hit (no re-fetch), onConflictDoUpdate upsert, base-interval-only storage, 5min aggregation from 1min, 4h aggregation from 1h
- Mocked `@/server/db` to use test DB via `getTestDb()`, mocked `@/env` for API key, mocked global `fetch` for Databento API
- Files changed: `tests/integration/market-data-cache.test.ts`
- **Learnings for future iterations:**
  - To test service.ts functions that import `db` from `@/server/db`, mock the module with a lazy getter: `vi.mock("@/server/db", () => ({ get db() { return getTestDb(); } }))`
  - `vi.spyOn(globalThis, "fetch")` is cleaner than mocking the whole module for HTTP mocking
  - Biome requires chained method calls to break across lines: `vi.spyOn(...).mockResolvedValue(...)` must use `.` on new lines
  - Pre-existing failure in `entitlements.test.ts` (1 test) is unrelated to market data work
---
