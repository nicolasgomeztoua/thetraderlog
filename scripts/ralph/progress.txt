# Ralph Progress Log
Started: Wed Feb 11 18:51:59 WET 2026
---

## Codebase Patterns
- Use Drizzle ORM for all database operations
- All constants go in src/lib/constants/ (never hardcode)
- Terminal design: monospace fonts, chartreuse accent (#d4ff00), ice blue (#00d4ff) for AI
- Protected routes use tRPC protectedProcedure
- P&L stored as decimal strings, parse with parseFloat()
- All timestamps use withTimezone: true
- ID generators in src/lib/shared/id.ts use `ids.entityName()` format
- User ownership validated via eq(table.userId, ctx.user.id) in all queries
- Active accounts filtered via getActiveAccountsSubquery helper
- User timezone fetched via getUserTimezone helper, date bounds via getDayBoundsInTimezone
- Existing AI tables: aiConversations (basic), aiMessages (basic) - need schema updates for US-001
- No existing AI utilities in src/lib/ - everything needs to be built from scratch
- Existing constants pattern: src/lib/constants/analytics.ts, src/lib/constants/trade-log.ts
- Router registration: create file in routers/, export router, import in root.ts, add to appRouter

## 2026-02-11 - US-000: Audit Existing Utilities for AI Analytics
- **What was implemented:** Comprehensive audit of all existing code relevant to AI analytics

### Existing src/lib/ Utilities (No AI-specific code exists)

**Analytics (src/lib/analytics/):**
- calculations.ts: getTiltScoreLevel, getDisciplineScoreLevel, getOvertradingLevel, getRiskOfRuinLevel, getKellyLevel, getEfficiencyRatingLevel, getHeatmapIntensity, getPnlColor, formatHour12/24, formatMonth/Full, formatCompactNumber
- stats.ts: calculateAggregateStats, calculateWinRate, calculateProfitFactor, calculateRMultipleFromTrade
- risk.ts: buildEquityCurve, findDrawdownPeriods, calculateCalmarRatio, calculateRecoveryFactor, calculateUlcerIndex, calculateKellyPercent, calculateRiskOfRuin, calculateRiskMetrics
- constants.ts: ANALYTICS_COLORS, DAYS_OF_WEEK, MONTHS, TRADING_SESSIONS, EMOTIONAL_STATES, HOLDING_TIME_BUCKETS
- thresholds.ts: Numeric thresholds for behavioral/risk analysis

**Trades (src/lib/trades/):**
- calculations.ts: calculatePoints, calculateTicks, calculatePips, calculateActualRMultiple, calculatePlannedRR, calculateDuration, calculateGrossPnl, calculateROI, calculateAllStats, calculateMAEMFE, analyzePostExit
- hash.ts: Trade identification hashing
- sort.ts: Trade sorting utilities
- running-pnl.ts: Cumulative P&L calculations

**Shared (src/lib/shared/):**
- utils.ts: cn, formatCurrency, formatPnL, formatPercent, formatNumber, formatDate, formatTime, formatDateTime, getPnLColorClass
- timezone.ts: toDateString, toUserTimezone, getHourInTimezone, getDayOfWeekInTimezone, getDateStringInTimezone, getMonthStringInTimezone, formatDateInTimezone, formatTimeInTimezone, getDayBoundsInTimezone, utcHourToLocalHour, generateDateStringsInTimezone
- id.ts: createId, ids.* (user, account, trade, tag, conversation, message, strategy, etc. — NO aiReport yet)
- schemas.ts: Zod validation schemas for trade/account/strategy enums
- time.ts: toUnixTimestamp, roundToCandle, getLastNDaysRange
- colors.ts: COLORS, PRESET_COLORS, CHART_COLORS, RISK_COLORS

**Constants (src/lib/constants/):**
- analytics.ts: DAY_LABELS, QUICK_DATE_PRESETS, HOURS, OUTCOME_OPTIONS, REVIEW_OPTIONS, TRADING_SESSIONS, DEFAULT_SESSIONS
- trade-log.ts: Trade sorting configuration

**Market Data (src/lib/market-data/):**
- service.ts: Server-only market data service (for futures/forex OHLC data)
- symbols.ts: Symbol specifications and point values
- candle-aggregation.ts: OHLC candle aggregation
- maemfe.ts: MAE/MFE calculations from bars

**Storage (src/lib/storage/):**
- s3.ts: S3 client, presigned URLs, transform functions (transformHtmlToS3Keys, transformHtmlWithPresignedUrls)

**Strategy (src/lib/strategy/):**
- evaluation.ts: Auto-condition evaluation engine for strategy rules

### Analytics Router Endpoints (28 total)

1. **getOverview** (query): {accountId?, filters?} → {totalTrades, wins, losses, breakevens, winRate, totalPnl, avgPnl, grossProfit, grossLoss, profitFactor, avgWin, avgLoss, avgRMultiple, expectancy, payoffRatio, sharpeRatio, largestWin, largestLoss, currentStreak, currentStreakType}
2. **getCalendarData** (query): {accountId?, filters?} → [{date, pnl, trades, wins, losses, winRate, avgPnl}] (365 days)
3. **getPerformanceByDayOfWeek** (query): {accountId?, filters?} → [{day, pnl, trades, wins, losses, winRate, avgPnl}] (7 items)
4. **getPerformanceByHour** (query): {accountId?, filters?} → [{hour, pnl, trades, wins, losses, winRate, avgPnl}] (24 items)
5. **getPerformanceBySession** (query): {accountId?, filters?} → [{name, start, end, color, pnl, trades, wins, losses, winRate, avgPnl}]
6. **getPerformanceByMonth** (query): {accountId?, months?(1-24), filters?} → [{month, pnl, trades, wins, losses, winRate, avgPnl}]
7. **getRiskMetrics** (query): {accountId?, filters?} → {maxDrawdown, drawdownFrom, drawdownEnd, maxConsecutiveLosses, consecutiveWins, kellyPercentage, riskOfRuin, recoveryFactor, ...}
8. **getEquityCurve** (query): {accountId?, filters?} → [{date, cumulativePnl, drawdownPercent, tradeId, symbol}]
9. **getDrawdownHistory** (query): {accountId?, limit?(1-20), filters?} → [{magnitude, startDate, troughDate, recoveryDate, troughValue, recoveryValue}]
10. **getRMultipleDistribution** (query): {accountId?, filters?} → {buckets: [{label, min, max, count, totalPnl, avgR}], stats: {total, avg, median, min, max}}
11. **getRiskRewardAnalysis** (query): {accountId?, filters?} → {tradesWithStopLoss, rMultiples, plannedRRs, efficiencies, rStats, rrStats, performanceByPlannedRR, wins, losses, avgPnl}
12. **getPositionSizeAnalysis** (query): {accountId?, filters?} → {buckets: [{label, min, max, trades, wins, losses, totalPnl, winRate, avgPnl}], stats: {totalTrades, avgSize, minSize, maxSize}}
13. **getPerformanceBySymbol** (query): {accountId?, filters?} → [{symbol, trades, wins, losses, winRate, pnl, avgPnl, grossProfit, grossLoss, profitFactor, stdDev}]
14. **getSymbolTrend** (query): {accountId?, months?, filters?} → [{symbol, months: {[key]: number}}]
15. **getStreakAnalysis** (query): {accountId?, filters?} → {currentStreak, maxWinStreak, maxLossStreak, streakDistribution, performanceDuringStreaks}
16. **getRevengeTrading** (query): {accountId?, filters?} → {afterWin, afterLoss, afterConsecutiveLosses, revengeIndicator}
17. **getOvertradingAnalysis** (query): {accountId?, filters?} → {byTradeCount, optimalRange, overtradingThreshold, correlationScore}
18. **getHoldingTimeAnalysis** (query): {accountId?, filters?} → [{label, minMinutes, maxMinutes, trades, wins, losses, totalPnl, winRate, avgPnl}]
19. **getBehavioralPatterns** (query): {accountId?, filters?} → {tiltScore, disciplineScore, overtradingTendency, emotionalStateBreakdown, totalTrades}
20. **getMonteCarloSimulation** (query): {accountId?, iterations?(100-10000), filters?} → {hasEnoughData, percentiles, probabilityOfProfit, expectedValue, standardDeviation, actualOutcome}
21. **getFilterPresets** (query): {} → [{id, name, description, filters, isDefault}]
22. **getDefaultPreset** (query): {} → preset | null
23. **createFilterPreset** (mutation): {name, description?, filters, isDefault?} → preset
24. **updateFilterPreset** (mutation): {id, name?, description?, filters?} → preset
25. **deleteFilterPreset** (mutation): {id} → {success}
26. **setDefaultPreset** (mutation): {id | null} → preset | null
27. **exportFilteredTrades** (query): {accountId?, filters?} → [{id, symbol, direction, quantity, entryPrice, exitPrice, entryTime, exitTime, realizedPnl, netPnl, fees, stopLoss, instrumentType, strategyName, rating, isReviewed, notes}]
28. **getFilteredTradeCount** (query): {accountId?, filters?} → {count}

Shared input: analyticsFilterInput = {symbols?, dateRange?: {start?, end?}, daysOfWeek?, hours?, sessions?, strategies?, tags?, rMultipleRange?, positionSizeRange?, outcome?, reviewed?}

### Trades Router Endpoints (30 total)

**Queries:**
1. **getAll**: {limit, cursor, status, symbol, tradeDirection, startDate, endDate, accountId, search, includeDeleted, result, pnl range, rating, isReviewed, setupType, dayOfWeek, tagIds, exitReason, strategyId, rMultiple range, sortField, sortDirection} → {items: Trade[], nextCursor}
2. **getById**: {id} → Trade (full with executions, tags, account, strategy, ruleChecks, attachments)
3. **getStats**: {startDate?, endDate?, accountId?} → {totalTrades, wins, losses, breakevens, winRate, totalPnl, avgPnl, avgWin, avgLoss, profitFactor, avgRMultiple, beThreshold}
4. **getExecutions**: {tradeId} → TradeExecution[]
5. **getUnreviewedCount**: {accountId?} → number
6. **getDeleted**: {accountId?, limit} → Trade[] (soft-deleted)
7. **getTradesNeedingMAEMFE**: {limit, accountId?} → Trade[] (needing MAE/MFE calc)
8. **getImportProgress**: {tradeIds} → {total, processed, isComplete, progress}

**Mutations:**
9. **create**: {symbol, instrumentType, direction, entryPrice, entryTime, quantity, exitPrice?, exitTime?, realizedPnl?, stopLoss?, takeProfit?, fees?, setupType?, emotionalState?, notes?, tagIds?, accountId, strategyId?} → Trade
10. **batchImport**: {accountId, trades[]} → {imported, skipped, total, tradeIds, processingCount}
11. **update**: {id, ...fields} → Trade
12. **close**: {id, exitPrice, exitTime, fees?, realizedPnl} → Trade
13. **delete** (soft): {id} → {success}
14. **deleteMany** (soft): {ids[]} → {success, deleted}
15. **restore**: {id} → {success}
16. **permanentDelete**: {id} → {success}
17. **emptyTrash**: {accountId?} → {success, deleted}
18. **addExecution**: {tradeId, executionType, price, quantity, executedAt, fees?, notes?, realizedPnl?} → Execution
19. **deleteExecution**: {executionId} → {success}
20. **updateTrailingStop**: {tradeId, trailedStopLoss} → Trade
21. **updateRating**: {id, rating} → Trade
22. **bulkUpdateRating**: {ids[], rating} → {success, updated}
23. **markReviewed**: {id, isReviewed} → Trade
24. **bulkMarkReviewed**: {ids[], isReviewed} → {success, updated}
25. **updateStrategy**: {id, strategyId} → Trade
26. **calculateMAEMFE**: {tradeId} → {success, dataQuality, metrics, trade}
27. **bulkCalculateMAEMFE**: {tradeIds[]} → {processed, success, failed, skipped}
28. **getUploadUrl**: {tradeId, filename, mimeType, size} → {presignedUrl, key}
29. **confirmUpload**: {tradeId, key, filename, mimeType, size, caption?} → Attachment
30. **deleteAttachment**: {id} → {success}

User-scoping: All queries use eq(trades.userId, ctx.user.id). Ownership validated through parent for child entities.

### Database Schema (20 tables, 16 enums)

**Enums:** user_role, instrument_type, trade_direction, trade_status, execution_type, emotional_state, import_source, exit_reason, account_type, drawdown_type, payout_frequency, challenge_status, trading_platform, strategy_rule_category, rule_type, data_quality

**Tables:**
1. **user**: id, clerkId, email, name, imageUrl, role, createdAt, updatedAt
2. **account_group**: id, userId(FK→user), name, description, color, createdAt, updatedAt
3. **account**: id, userId(FK→user), name, broker, platform, accountType, initialBalance, currency, accountNumber, isActive, isDefault, notes, color, maxDrawdown, drawdownType, dailyLossLimit, profitTarget, consistencyRule, minTradingDays, challengeStartDate, challengeEndDate, challengeStatus, profitSplit, payoutFrequency, linkedAccountId(FK→account), groupId(FK→account_group), createdAt, updatedAt
4. **trade**: id, userId(FK→user), accountId(FK→account), strategyId(FK→strategy), symbol, instrumentType, direction, status, entryPrice, entryTime, exitPrice, exitTime, quantity, stopLoss, takeProfit, stopLossHit, takeProfitHit, trailedStopLoss, wasTrailed, exitReason, isPartiallyExited, remainingQuantity, realizedPnl, fees, netPnl, maePrice, mfePrice, maeAmount, mfeAmount, marketDataQuality, setupType, emotionalState, notes, rating, isReviewed, importSource, externalId, tradeHash, deletedAt, createdAt, updatedAt
5. **trade_execution**: id, tradeId(FK→trade), executionType, price, quantity, executedAt, fees, realizedPnl, notes, createdAt
6. **tag**: id, userId(FK→user), name, color, createdAt
7. **trade_tag**: tradeId(FK→trade) + tagId(FK→tag) — junction table
8. **user_settings**: id, userId(FK→user, unique), openaiApiKey, anthropicApiKey, googleApiKey, preferredAiProvider, defaultInstrumentType, timezone, breakevenThreshold, currency, theme, tradeLogColumns, tradeLogSort, tradingSessions, createdAt, updatedAt
9. **filter_preset**: id, userId(FK→user), name, description, filters(JSON), isDefault, createdAt, updatedAt
10. **ai_conversation**: id, userId(FK→user), title, createdAt, updatedAt
11. **ai_message**: id, conversationId(FK→ai_conversation), role, content, createdAt
12. **strategy**: id, userId(FK→user), name, description, color, entryCriteria, exitRules, riskParameters(JSON), scalingRules(JSON), trailingRules(JSON), isActive, createdAt, updatedAt
13. **strategy_rule**: id, strategyId(FK→strategy), text, category, order, ruleType, configSource, autoCondition(JSON), isGenerated, sourceConfigHash, createdAt
14. **trade_rule_check**: tradeId(FK→trade) + ruleId(FK→strategy_rule) — junction, checked, checkedAt, evaluationResult(JSON), wasAutoEvaluated, userOverride
15. **daily_journal**: id, userId(FK→user), date, content(HTML), contentFormat, dayStartedAt, createdAt, updatedAt
16. **daily_checklist_template**: id, userId(FK→user), text, order, isActive, createdAt
17. **daily_checklist_check**: id, journalId(FK→daily_journal), templateId(FK→daily_checklist_template), forcedItemId, checked, checkedAt
18. **journal_attachment**: id, journalId(FK→daily_journal), url, key, filename, mimeType, size, caption, createdAt
19. **trade_attachment**: id, tradeId(FK→trade), url, key, filename, mimeType, size, caption, createdAt
20. **candle_cache**: id, symbol, interval, date, bars(JSON), barCount, source, fetchedAt

**Key Relationships:**
- user → accounts, trades, tags, strategies, journals, conversations, settings, filter_presets
- account → trades (one-to-many), account_group (many-to-one), linked_account (self-ref)
- trade → executions, tags(via junction), rule_checks(via junction), attachments, account, strategy, user
- ai_conversation → ai_messages (one-to-many), user (many-to-one)
- strategy → strategy_rules, trades
- daily_journal → checklist_checks, attachments

### Existing AI Page (src/app/(protected)/ai/page.tsx)
- Client-side only with fake pattern-matching AI (generateLocalInsight function)
- EXAMPLE_QUERIES hardcoded array (6 queries)
- hasApiKey state with API key notice banner
- Simple message rendering with inline markdown parsing
- Uses Key and Settings icons for API key links
- Fetches trades.getStats and trades.getAll for local analysis

### Other Routers (registered in root.ts)
- accounts, analytics, dailyJournal, filterPresets, marketData, settings, storage, strategies, tags, trades
- No AI router registered yet

### Files Changed
- scripts/ralph/progress.txt (this file)

### Learnings for future iterations:
- There are 28 analytics endpoints and 30 trades endpoints — comprehensive coverage exists
- The analyticsFilterInput schema is shared across most analytics queries
- Existing aiConversations and aiMessages tables are minimal — need significant updates for US-001
- No aiReport ID generator exists in src/lib/shared/id.ts — needs to be added
- The existing AI page is 100% client-side fake logic — entire thing needs replacing
- createCallerFactory is already exported from root.ts for server-side caller creation
- New AI enums: aiConversationStatusEnum, aiConversationModeEnum, aiReportStatusEnum
- aiReports table uses ids.aiReport() prefix "ar"
- aiConversations now has: status, mode, initialPrompt, dateRangeStart, dateRangeEnd, model
- aiMessages now has: model, tokensUsed, toolCalls (JSON string)
- S3 storage patterns exist in src/lib/storage/s3.ts — reuse for PDF uploads
- Market data service exists at src/lib/market-data/service.ts — reuse for get_market_data tool
---

## 2026-02-11 - US-001: Update AI Schema — Conversations, Messages, Reports
- What was implemented: Updated database schema for AI analytics system
- Files changed:
  - src/server/db/schema.ts — Added 3 new enums, updated aiConversations & aiMessages tables, created aiReports table, added relations and type exports
  - src/lib/shared/id.ts — Added ids.aiReport() generator with "ar" prefix
- **Learnings for future iterations:**
  - Drizzle pgEnum values must be plain strings — no inline comments inside the array
  - When adding new tables, remember to update ALL relevant relations (users, parent table, etc.)
  - The `bun run check` has a pre-existing formatting issue in scripts/ralph/prd.command-center-dashboard.json — this is not a blocker for our changes
  - db:push NOTICE about truncated FK names is expected (daily_checklist_check_template_id...) — safe to ignore
---

## 2026-02-11 - US-003: AI Constants and Model Configuration
- What was implemented: Centralized AI constants file with model configuration, modes, limits, and suggested prompts
- Files changed:
  - src/lib/constants/ai.ts — New file with all AI constants
  - src/lib/constants/index.ts — Added exports for AI constants
- **Learnings for future iterations:**
  - Constants index re-exports everything — add new constant files here for barrel import support
  - AI_MODELS array has a `mode` field to filter models by chat/report/both in UI selectors
  - DEFAULT_CHAT_MODEL and DEFAULT_REPORT_MODEL are separate strings used by the OpenRouter client
  - SUGGESTED_CHAT_QUERIES and SUGGESTED_REPORT_PROMPTS are `as const` tuples for type safety
---

## 2026-02-11 - US-002: OpenRouter Client Service
- What was implemented: OpenRouter API client with streaming and non-streaming support
- Files changed:
  - src/lib/ai/client.ts — New file with chatCompletion, chatCompletionStream, types, retry logic
  - src/env.js — Added OPENROUTER_API_KEY to server schema and runtimeEnv
- **Learnings for future iterations:**
  - Environment variables use @t3-oss/env-nextjs via src/env.js — always add new server vars there with z.string().min(1)
  - OpenRouter API is OpenAI-compatible — same message format, tool_calls, and SSE streaming
  - chatCompletion returns full response (for tool loops), chatCompletionStream yields SSE chunks (for UI streaming)
  - Exported types: ChatMessage, ToolCall, ToolDefinition, ChatCompletionOptions, TokenUsage, ChatCompletionResponse, StreamChunk, OpenRouterError
  - fetchWithRetry handles 429 (rate limit) and 5xx with exponential backoff (1s, 2s, 4s)
  - Streaming includes stream_options: { include_usage: true } to get token usage in final chunk
---

## 2026-02-11 - US-004: Schema Context Generator
- What was implemented: Static schema context string for AI system prompts
- Files changed:
  - src/lib/ai/schema-context.ts — New file with generateSchemaContext() function
- **Learnings for future iterations:**
  - The schema context is a single static string (not generated dynamically from Drizzle schema introspection) — simpler and avoids runtime dependency on schema objects
  - All 19 enums, 20 tables, relationships, 7 example SQL queries, and 20+ tRPC endpoints documented in markdown table format
  - P&L columns are decimal strings — SQL queries must use CAST(net_pnl AS NUMERIC) for aggregation
  - Soft delete filter (deleted_at IS NULL) must be included in all example queries
  - The context includes analytics filter schema so AI knows what filters are available
---

## 2026-02-11 - US-005: AI Context Builder — User-Specific Context
- What was implemented: User-specific context builder for AI system prompts
- Files changed:
  - src/lib/ai/context-builder.ts — New file with buildUserContext() async function
- **Learnings for future iterations:**
  - Drizzle `with` relations work for loading nested data (strategies → rules) in a single query
  - Use `Promise.all` for parallel loading of independent user data (strategies, tags, accounts, settings, journals)
  - Database type pattern: `import type { db as DbType } from "@/server/db"; type Db = typeof DbType;`
  - `userSettings` can be null for new users — always provide defaults
  - `tradingSessions` is stored as JSON string in userSettings — parse with try/catch
  - Strategy `entryCriteria` and `exitRules` are HTML (from Tiptap editor) — strip tags for plain text context
  - Journal `content` is also HTML — strip and truncate for context (300 chars per entry)
  - `dailyJournals.date` is a timestamp normalized to midnight — use `gte` for 7-day lookback
---

## 2026-02-11 - US-006: AI System Prompt — Trading Analyst Persona
- What was implemented: System prompt builder with trading analyst persona and mode-specific instructions
- Files changed:
  - src/lib/ai/prompts/trading-analyst.ts — New file with buildSystemPrompt() function
- **Learnings for future iterations:**
  - System prompt is composed of 6 sections: persona, tool instructions, data handling notes, mode instructions, schema context, user context
  - Chat mode instructions emphasize conciseness, prefer call_analytics over raw SQL
  - Report mode instructions emphasize thoroughness, multi-tool usage, chart generation
  - Tool instructions tell AI that SQL is auto-scoped via CTEs — no need for user_id WHERE clauses
  - The buildSystemPrompt function takes {schemaContext, userContext, mode} — call generateSchemaContext() and buildUserContext() separately and pass results in
---

## 2026-02-11 - US-007: AI Tool — run_query (Read-Only SQL Execution)
- What was implemented: Read-only SQL execution tool with user-scoped CTEs and validation
- Files changed:
  - src/lib/ai/tools/run-query.ts — New file with tool definition, executor, validation, and CTE builder
- **Learnings for future iterations:**
  - `db.execute(sql.raw(queryString))` works for raw SQL with the Drizzle postgres-js driver
  - Do NOT use simple word-boundary matching for "DELETE" — it matches "deleted_at". Use phrase patterns like `DELETE FROM` instead
  - Wrapping user query in `SELECT * FROM (...) AS _result LIMIT N` safely caps rows even when user query has its own LIMIT
  - User-scoped CTEs cover 15 tables: user_trades, user_accounts, user_account_groups, user_tags, user_strategies, user_strategy_rules, user_trade_tags, user_executions, user_journals, user_settings, user_conversations, user_reports, user_trade_rule_checks, user_trade_attachments, user_filter_presets
  - Tool definitions use OpenAI function-calling format: `{ type: "function", function: { name, description, parameters } }`
  - The `executeRunQuery` function takes `db` as a parameter (not importing it directly) for testability
  - String literal stripping (`replace(/'[^']*'/g, "''")`) prevents false positives in SQL validation
---
