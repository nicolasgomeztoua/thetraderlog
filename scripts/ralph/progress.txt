# Ralph Progress Log
Started: Sun Jan 18 19:17:56 WET 2026
---

## Codebase Patterns
- Use Drizzle ORM for all database operations in `src/server/db/schema.ts`
- All constants go in `src/lib/constants/` (never hardcode)
- Terminal design: monospace fonts, chartreuse accent (#d4ff00)
- Protected routes use tRPC protectedProcedure
- ID generation uses `src/lib/shared/id.ts` with `ids.entityName()` pattern
- Array columns in PostgreSQL use `.array()` modifier on text columns
- Schema changes: Edit schema.ts then run `bun run db:push --force` to skip data-loss prompts

---

## 2026-01-18 19:XX - US-001
- What was implemented: Extended strategies table with marketplace-related columns
- Files changed:
  - `src/server/db/schema.ts` - Added coverImageUrl, coverImageKey, isPublic, isAnonymous, instruments, categoryTags columns
- **Learnings for future iterations:**
  - Use `--force` flag with `bun run db:push` to auto-approve data loss statements
  - Text array columns in Drizzle: `text("column_name").array()`
  - Existing DB columns not in schema will be dropped during push (watch for data loss warnings)
---

## 2026-01-18 20:XX - US-002
- What was implemented: Created strategyVotes table for marketplace voting functionality
- Files changed:
  - `src/server/db/schema.ts` - Added strategyVotes table with id, strategyId, userId, vote, createdAt, updatedAt columns; added unique index on (strategyId, userId); added relations to users and strategies; exported StrategyVote and NewStrategyVote types
  - `src/lib/shared/id.ts` - Added strategyVote ID generator with "sv" prefix
- **Learnings for future iterations:**
  - For junction tables with unique constraints, use `uniqueIndex()` function from drizzle-orm/pg-core
  - Add relation updates in both parent tables (users.strategyVotes, strategies.votes)
  - Remember to add ID generator when creating new tables
---

## 2026-01-18 21:XX - US-003
- What was implemented: Created strategyDownloads table for tracking marketplace strategy copies
- Files changed:
  - `src/server/db/schema.ts` - Added strategyDownloads table with id, originalStrategyId (FK), copiedStrategyId (FK), userId (FK), createdAt; added unique index on (originalStrategyId, userId); added index on originalStrategyId for counting; added relations to strategies and users; added sourceStrategyId and cachedStats columns to strategies table; exported StrategyDownload and NewStrategyDownload types
  - `src/lib/shared/id.ts` - Added strategyDownload ID generator with "sd" prefix
- **Learnings for future iterations:**
  - Self-referential relations in Drizzle require relationName to distinguish directions (e.g., "sourceStrategies")
  - When a table references another table twice (e.g., originalStrategy and copiedStrategy), use relationName in both the relation definition and the many() call
---

## 2026-01-18 22:XX - US-004
- What was implemented: Created strategyReports table for marketplace content moderation
- Files changed:
  - `src/server/db/schema.ts` - Added strategyReportReasonEnum (misleading_stats, inappropriate_content, spam, other) and strategyReportStatusEnum (pending, reviewed, dismissed, actioned); created strategyReports table with id, strategyId (FK), reporterId (FK), reason, details, status, createdAt, reviewedAt; added indexes on strategyId, status; added unique index on (strategyId, reporterId); added relations to strategies and users; exported StrategyReport and NewStrategyReport types
  - `src/lib/shared/id.ts` - Added strategyReport ID generator with "rp" prefix
- **Learnings for future iterations:**
  - When adding new enums, add them to the ENUMS section near other enums for consistency
  - Report tables typically have two foreign keys: one for the reported entity, one for the reporter user
  - Use "reporter" vs "user" naming to distinguish between the report owner and the entity owner
---

## 2026-01-18 23:XX - US-005
- What was implemented: Added centralized marketplace constants
- Files changed:
  - `src/lib/constants/marketplace.ts` - Created new file with STRATEGY_INSTRUMENTS, STRATEGY_CATEGORIES, MARKETPLACE_SORT_OPTIONS, MARKETPLACE_PAGE_SIZE, MIN_TRADES_TO_PUBLISH, VERIFIED_TRACK_RECORD_THRESHOLD, LIMITED_DATA_THRESHOLD, STRATEGY_REPORT_REASONS; includes TypeScript types for each constant array
  - `src/lib/constants/index.ts` - Re-exported all marketplace constants and types
- **Learnings for future iterations:**
  - Follow existing constant file patterns (analytics.ts): use `as const` for type safety, add JSDoc comments explaining each constant
  - Biome sorts type exports after constant exports in the same export block
  - Use `bun run check --write` to auto-fix Biome formatting issues
- S3 utilities exist in `src/lib/storage/s3.ts` with `getPresignedUploadUrl`, `getS3Bucket`, `isS3Configured`
- Generic image upload hook in `src/hooks/use-image-upload.ts` uses storage router
---

## 2026-01-19 - US-006
- What was implemented: Added strategy cover image upload endpoint to strategies router
- Files changed:
  - `src/server/api/routers/strategies.ts` - Added `getImageUploadUrl` mutation with:
    - Input validation: strategyId, filename, mimeType, size
    - Strategy ownership validation
    - Image type validation (jpeg, png, webp, gif)
    - File size validation (< 5MB)
    - S3 key generation: `strategies/{userId}/{strategyId}/cover-{timestamp}.{ext}`
    - Returns presignedUrl, publicUrl, and key
- **Learnings for future iterations:**
  - Existing S3 upload pattern: `src/lib/storage/s3.ts` provides `getPresignedUploadUrl`, `getS3Bucket`, `isS3Configured`
  - Storage router (`src/server/api/routers/storage.ts`) has generic `getImageUploadUrl` - use for non-entity-specific uploads
  - For entity-specific uploads (strategy covers), add mutations to the entity router with ownership validation
  - Helper functions like `getPublicUrl` can be duplicated across routers (or extracted to shared util if needed frequently)
---

## 2026-01-19 - US-007
- What was implemented: Extended strategy update mutation to handle cover image fields
- Files changed:
  - `src/server/api/routers/strategies.ts` - Extended updateStrategySchema with coverImageUrl and coverImageKey fields; modified update mutation to:
    - Extract coverImageKey from input for special handling
    - Delete old S3 object when cover image key changes (using deleteObject from s3.ts)
    - Gracefully handle S3 deletion failures (log but don't fail the update)
- **Learnings for future iterations:**
  - When updating fields that reference S3 objects, check if the key is changing to clean up old objects
  - Use try/catch around S3 operations that shouldn't fail the main operation
  - Fields extracted from input spread must be explicitly added back to updateData
---

## 2026-01-19 - US-008
- What was implemented: Added autosave mutation to strategies router for silent auto-save without cache invalidation
- Files changed:
  - `src/server/api/routers/strategies.ts` - Added `autosaveStrategySchema` and `autosave` mutation with:
    - Same validation as update mutation but accepts partial fields
    - Strategy ownership validation
    - Cover image key handling with S3 cleanup
    - Support for instruments and categoryTags array fields
    - Returns only `{ updatedAt: Date }` for UI confirmation (silent save)
- **Learnings for future iterations:**
  - For auto-save endpoints, return minimal data (just timestamp) to avoid triggering cache invalidation
  - When creating a variant of an existing mutation (autosave vs update), extract shared logic or duplicate carefully
  - Include marketplace fields (instruments, categoryTags) in autosave schema to support edit page forms
---

## 2026-01-19 - US-009
- What was implemented: Integration tests for cover image upload and auto-save endpoints
- Files changed:
  - `tests/integration/strategies.test.ts` - Created new test file with:
    - Tests for getImageUploadUrl: S3 not configured behavior, valid mime types, Zod schema validation
    - Tests for autosave: field updates, ownership validation, risk parameters JSON, arrays
    - Tests for update mutation cover image fields: accept URL/key, clear to null, combined updates
    - Authentication tests for all endpoints
- **Learnings for future iterations:**
  - In test environment (vitest/Node.js), `isS3Configured()` returns false - tests must account for this
  - The S3 config check happens FIRST in router handlers, before other validations
  - Can only test Zod schema validation and behavior that reaches the S3 check in test env
  - For full S3 endpoint testing, would need to mock `isS3Configured()` or test with S3 configured
  - Test patterns: `setupTrader()` creates user + account, `createTestUser()` for additional users
  - Use `createTestCaller(clerkId, user)` for authenticated callers
---

## 2026-01-19 - US-010
- What was implemented: Added publish and unpublish mutations for marketplace visibility
- Files changed:
  - `src/server/api/routers/strategies.ts` - Added `publish` mutation with:
    - Strategy ownership validation
    - Required fields validation (name, description)
    - Minimum trade count enforcement (MIN_TRADES_TO_PUBLISH = 20)
    - Stats computation using shared `calculateAggregateStats` from `@/lib/analytics`
    - Cached stats stored as JSON (totalTrades, wins, losses, winRate, profitFactor, avgR, avgWin, avgLoss, computedAt)
  - `src/server/api/routers/strategies.ts` - Added `unpublish` mutation with:
    - Strategy ownership validation
    - Sets isPublic to false
- **Learnings for future iterations:**
  - Reuse existing analytics functions: `calculateAggregateStats` from `@/lib/analytics/stats.ts` computes all needed stats
  - Use `getUserBreakevenThreshold` from `@/server/api/helpers.ts` for consistent breakeven classification
  - Import constants from `@/lib/constants` (e.g., MIN_TRADES_TO_PUBLISH)
  - Handle Infinity values in profitFactor when storing as JSON (set to null)
  - JSON fields: strategy riskParameters, scalingRules, trailingRules need JSON.parse when returning
---

## 2026-01-18 - US-011
- What was implemented: Added marketplace listing endpoint for browsing public strategies
- Files changed:
  - `src/server/api/routers/marketplace.ts` - Created new marketplace router with `list` query:
    - Uses `publicProcedure` for optional auth (listing doesn't require auth, but hasVoted does)
    - Input: cursor, limit, search, instruments, categories, sort
    - Cursor-based pagination with configurable limit (default MARKETPLACE_PAGE_SIZE = 20)
    - Search in name and description using `ilike`
    - Filter by instruments and categories using PostgreSQL array overlap (`&&`)
    - Sort by votes, downloads, or recent
    - Returns: strategy fields, creator info (null if anonymous), cachedStats, engagement (voteScore, downloadCount), hasVoted, trackRecordStatus
  - `src/server/api/root.ts` - Registered marketplaceRouter in appRouter
- **Learnings for future iterations:**
  - Use `publicProcedure` for endpoints that don't require auth but benefit from optional user context
  - For array overlap queries in PostgreSQL, use raw SQL: `strategies.instruments && ARRAY[...]::text[]`
  - When fetching aggregate data (votes, downloads), use separate queries with `inArray` for efficiency
  - Build lookup Maps from arrays for O(1) access when transforming results
  - To get user from clerkId in publicProcedure, query users table: `ctx.db.query.users.findFirst({ where: eq(users.clerkId, ctx.userId) })`
---

## 2026-01-19 - US-012
- What was implemented: Added strategy voting endpoints with rate limiting
- Files changed:
  - `src/server/api/routers/marketplace.ts` - Added `vote` and `removeVote` mutations:
    - `vote`: Accepts strategyId and vote (1 or -1), uses upsert pattern to replace existing vote
    - `removeVote`: Accepts strategyId, deletes user's vote
    - Both validate strategy is public and return updated vote score
    - Cannot vote on own strategy
  - `src/lib/rate-limit.ts` - Created new rate limiting utility:
    - Uses Upstash Redis with sliding window algorithm
    - Configurable: 20 votes per user per hour
    - Graceful fallback if Redis not configured
  - `src/env.js` - Added UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN env vars
  - `package.json` - Added @upstash/ratelimit and @upstash/redis dependencies
- **Learnings for future iterations:**
  - Upstash rate limiting setup: Redis client + Ratelimit with sliding window
  - Use `isRateLimitConfigured()` check before creating rate limiter (graceful fallback)
  - For upsert with Drizzle: `.onConflictDoUpdate({ target: [...columns], set: {...} })`
  - Biome disallows non-null assertions - use nullish coalescing with empty string fallback after config check
  - computeVoteScore helper: aggregates all votes for a strategy using SUM
---

## 2026-01-19 - US-013
- What was implemented: Added strategy download/copy endpoint to marketplace router
- Files changed:
  - `src/server/api/routers/marketplace.ts` - Added `download` mutation:
    - Validates strategy exists and is public
    - Prevents downloading own strategy
    - Prevents duplicate downloads (unique constraint on originalStrategyId + userId)
    - Creates new strategy copy with "(Copy)" suffix
    - Copies: name, description, color, coverImageUrl (but NOT coverImageKey), entryCriteria, exitRules, riskParameters, scalingRules, trailingRules
    - Sets sourceStrategyId to link copy to original
    - Creates strategyDownload record to track the download
    - Returns copied strategy with parsed JSON fields
- **Learnings for future iterations:**
  - When copying resources with S3 references, copy the public URL (coverImageUrl) but NOT the S3 key (coverImageKey) - user doesn't own the original S3 object
  - Use sourceStrategyId to track copy-to-original relationships for attribution UI
  - strategyDownloads table has unique index on (originalStrategyId, userId) to prevent duplicate downloads
  - JSON fields (riskParameters, scalingRules, trailingRules) need JSON.parse when returning from database
---

## 2026-01-18 - US-014
- What was implemented: Added strategy report endpoint to marketplace router
- Files changed:
  - `src/server/api/routers/marketplace.ts` - Added `report` mutation:
    - Input: strategyId, reason (from STRATEGY_REPORT_REASONS enum), optional details (max 1000 chars)
    - Validates strategy exists and is public
    - Prevents reporting own strategy
    - Prevents duplicate reports (checks existing report before insert)
    - Creates strategyReport record with status "pending"
    - Returns { success: true }
- **Learnings for future iterations:**
  - For report-type features, always check both "cannot report own" and "cannot report twice" before insert
  - Use constants from `@/lib/constants` for enum values in Zod schema (e.g., `z.enum(STRATEGY_REPORT_REASONS)`)
  - The strategyReports table has a unique index on (strategyId, reporterId) enforced at DB level, but checking first gives better error messages
---

## 2026-01-18 - US-015
- What was implemented: Added marketplace.getById query for public strategy details
- Files changed:
  - `src/server/api/routers/marketplace.ts` - Added `getById` query:
    - Uses `publicProcedure` for optional auth (no auth required, but vote status available when authenticated)
    - Fetches strategy with rules relation (ordered by rule order)
    - Only returns public strategies (isPublic = true)
    - Includes creator info (respects isAnonymous flag)
    - Includes computed stats from cachedStats
    - Includes engagement data (voteScore, downloadCount)
    - Includes current user's vote status (hasVoted)
    - Returns all strategy fields including entryCriteria, exitRules, rules array
    - Parses JSON fields (riskParameters, scalingRules, trailingRules)
- **Learnings for future iterations:**
  - For detail views, include all related data (rules, stats, engagement) in a single query response
  - publicProcedure works well for endpoints that benefit from optional user context (vote status, ownership checks)
  - The strategyRules relation needs explicit import from schema when used in orderBy
---

## 2026-01-18 - US-016
- What was implemented: Added cached stats refresh logic when trades are closed/modified
- Files changed:
  - `src/server/api/helpers/strategy-stats.ts` - Created new helper module with:
    - `CachedStrategyStats` interface matching cachedStats column structure
    - `computeAndCacheStrategyStats(db, strategyId, userId)` - computes and caches stats for public strategies
    - `refreshStrategyStatsIfPublic(db, strategyId, userId)` - convenience wrapper that skips if no strategy assigned
  - `src/server/api/routers/trades.ts` - Integrated stats refresh into multiple mutations:
    - `close`: refreshes after closing a trade
    - `update`: refreshes when status changes to "closed"
    - `delete`: refreshes when soft-deleting a closed trade (excluded from stats)
    - `restore`: refreshes when restoring a closed trade (included back in stats)
    - `updateStrategy`: refreshes both old and new strategies when assignment changes on closed trades
- **Learnings for future iterations:**
  - Use `import type { db as DbType } from "@/server/db"` then `type Db = typeof DbType` for database client typing (matches helpers.ts pattern)
  - Stats refresh should happen in all trade mutations that affect closed trades: close, update-to-closed, delete, restore, strategy reassignment
  - When reassigning a closed trade's strategy, refresh BOTH old and new strategies (one loses a trade, one gains a trade)
  - The refresh function checks isPublic internally, so callers don't need to check first
---

## 2026-01-18 - US-017
- What was implemented: Comprehensive integration tests for marketplace backend
- Files changed:
  - `tests/integration/marketplace.test.ts` - Created new test file with:
    - Tests for strategies.publish: validates name/description required, minimum 20 trades, ownership, stats computation
    - Tests for strategies.unpublish: sets isPublic false, ownership validation
    - Tests for marketplace.list: paginated results, filters (search, instruments, categories), trackRecordStatus
    - Tests for marketplace.getById: returns public strategy details, 404 for private, anonymous creator handling
    - Tests for marketplace.vote/removeVote: upvote/downvote, vote replacement, cannot vote own strategy
    - Tests for marketplace.download: creates strategy copy, records download, cannot download twice or own strategy
    - Tests for marketplace.report: creates report with pending status, cannot report own or twice
    - Authentication tests for all protected endpoints
- **Learnings for future iterations:**
  - Tests require Docker to run (testcontainers for PostgreSQL)
  - When testing marketplace features, create strategies with enough trades (MIN_TRADES_TO_PUBLISH = 20)
  - Use `createTestTrades` to create trades, then update them with `db.update` to link to strategy
  - The marketplace router uses `publicProcedure` for list/getById (optional auth for vote status)
  - Test multiple users for ownership validation: main user owns resources, other users test access restrictions
---

## 2026-01-18 - US-018
- What was implemented: Created dedicated strategy edit page route at /strategies/[id]/edit
- Files changed:
  - `src/app/(protected)/strategies/[id]/edit/page.tsx` - New edit page with:
    - Loading skeleton showing cover image, header, tabs, and content placeholders
    - 404 page if strategy not found or user doesn't have access
    - useEffect redirect to /strategies if not owner (strategy null after load)
    - Cover image placeholder using strategy color gradient
    - Terminal design styling with monospace fonts
    - data-testid attributes for E2E testing (strategy-edit-loading, strategy-edit-not-found, strategy-edit-page, strategy-edit-cover, strategy-edit-back, strategy-edit-heading)
- **Learnings for future iterations:**
  - The strategies.getById query already validates ownership (returns null for non-owner)
  - For edit pages, use useEffect to redirect after load completes if no data
  - Cover image placeholders can use linear-gradient with strategy color
  - Placeholder content helps visualize where future components will go
---

## 2026-01-18 - US-019
- What was implemented: Created cover image upload component for strategy edit page
- Files changed:
  - `src/components/strategy/cover-image-upload.tsx` - New component with:
    - Shows current cover image if exists, gradient placeholder if not
    - File picker for image upload (accepts image/*)
    - Uses strategies.getImageUploadUrl mutation for presigned S3 URLs
    - Upload progress via XMLHttpRequest with toast notifications (0-100%)
    - Updates strategy via strategies.update mutation on completion
    - Delete button to remove cover image (sets to null)
    - Responsive 3:1 aspect ratio banner
    - Terminal design styling with hover states
    - data-testid attributes for E2E testing
  - `src/app/(protected)/strategies/[id]/edit/page.tsx` - Replaced placeholder with CoverImageUpload component
- **Learnings for future iterations:**
  - For strategy-specific uploads, use strategies.getImageUploadUrl (validates ownership) instead of generic storage hook
  - XMLHttpRequest for upload progress tracking: use xhr.upload.addEventListener("progress", ...)
  - After S3 upload completes, remember to update the entity (strategy) with the new URL and key
  - When deleting cover images, set both coverImageUrl and coverImageKey to null
  - Use separate isUploading and isDeleting states for distinct loading indicators
---

## 2026-01-18 - US-020
- What was implemented: Built strategy edit form with auto-save functionality
- Files changed:
  - `src/components/strategy/strategy-edit-form.tsx` - New component with:
    - Form fields: name (required), description (textarea), color picker
    - Debounced auto-save (500ms) using strategies.autosave mutation
    - Save status indicator: "Saving...", "All changes saved at HH:MM", "Unsaved changes", error state
    - Uses useRef for debounce timer and tracking last saved data
    - Invalidates strategy query cache on save completion
    - Terminal design styling with monospace fonts, spacious inputs
    - data-testid attributes for E2E testing
  - `src/app/(protected)/strategies/[id]/edit/page.tsx` - Integrated StrategyEditForm component, replaced placeholder
- **Learnings for future iterations:**
  - Debounced auto-save pattern: use useRef for timer + last saved data, compare before saving to avoid redundant API calls
  - For auto-save status UI: track both status (saving/saved/error) AND whether data differs from last saved state
  - Don't save if required field (name) is empty - silently skip
  - Invalidate query cache after autosave to keep UI in sync with server state
---

## 2026-01-18 - US-021
- What was implemented: Built Rules Editor Section with drag-and-drop, inline editing, delete confirmation
- Files changed:
  - `src/server/api/routers/strategies.ts` - Added addRule, updateRule, deleteRule, reorderRules mutations for individual rule CRUD operations
  - `src/components/strategy/rules-editor.tsx` - New component with dnd-kit drag-and-drop, grouped by category, inline text editing, delete confirmation, optimistic updates
  - `src/app/(protected)/strategies/[id]/edit/page.tsx` - Integrated RulesEditor component
- **Learnings for future iterations:**
  - For Record<string, T[]> that should only have known keys, use Record<SpecificUnion, T[]> to avoid TypeScript undefined warnings
  - dnd-kit: Use SortableContext with verticalListSortingStrategy for list reordering
  - Inline editing pattern: track editingId + editingText state, update on Enter, cancel on Escape
  - Delete confirmation pattern: track deletingId state, show confirm/cancel buttons inline
  - When removing unused function params, remember to also remove from JSX props and interface
---

## 2026-01-18 - US-022
- What was implemented: Built Risk Parameters Editor Section for strategy edit page
- Files changed:
  - `src/components/strategy/risk-parameters-editor.tsx` - New component with:
    - Position sizing: method dropdown (fixed, risk_percent, kelly) with conditional inputs for fixedSize, riskPercent, kellyFraction
    - Max risk per trade: type dropdown (dollars/percent) + value input
    - Daily loss limit: type dropdown (dollars/percent) + value input
    - Max concurrent positions: number input
    - Min R:R ratio: number input
    - Debounced auto-save (500ms) using strategies.autosave mutation
    - Save status indicator (saving/saved/error/unsaved)
    - Terminal design styling with grouped sections
    - data-testid attributes for E2E testing
  - `src/app/(protected)/strategies/[id]/edit/page.tsx` - Integrated RiskParametersEditor component
- **Learnings for future iterations:**
  - For complex forms with multiple fields that auto-save, use JSON.stringify comparison of state object vs lastSavedDataRef to detect changes
  - Build a helper function (buildRiskParameters) that assembles the data shape from state, use it in both change detection and save mutation
  - When form has conditional fields (e.g., different inputs per position sizing method), only include the relevant field in the saved object
  - Biome lint: for useEffect that depends on a callback function, only include the callback in dependencies (not all the state it uses internally)
---

## 2026-01-18 - US-023
- What was implemented: Assembled strategy edit page layout with tabbed sections
- Files changed:
  - `src/app/(protected)/strategies/[id]/edit/page.tsx` - Restructured to use tabbed layout with 4 tabs:
    - Overview tab: contains StrategyEditForm with name, description, color, instruments, categories
    - Rules tab: contains RulesEditor component
    - Risk Management tab: contains RiskParametersEditor component
    - Advanced tab: placeholder for future scaling and trailing rules
  - `src/components/strategy/strategy-edit-form.tsx` - Extended with:
    - instruments multi-select using Popover + Checkbox pattern
    - categoryTags multi-select using same pattern
    - Auto-save extended to include instruments and categoryTags arrays
    - arraysEqual helper moved outside component to avoid dependency warnings
- **Learnings for future iterations:**
  - For multi-select with checkboxes in a popover, use controlled Popover with Button trigger styled to show selected items as chips
  - Move pure helper functions (like array comparison) outside React components to avoid useCallback dependency issues
  - Tabs component uses defaultValue for initial tab, each TabsTrigger needs matching value prop
  - Responsive tab labels: use hidden sm:inline pattern to show only icons on mobile
  - The strategies autosave mutation already accepts instruments and categoryTags as optional arrays (from US-008)
---

## 2026-01-18 - US-024
- What was implemented: Updated strategy detail page with cover image hero banner
- Files changed:
  - `src/app/(protected)/strategies/[id]/page.tsx` - Major updates:
    - Added hero banner with cover image (using Next.js Image) or gradient placeholder using strategy color
    - Strategy name overlays cover image with text shadow for readability
    - Added Edit button linking to /strategies/[id]/edit with strategy color accent
    - Strategy color used as accent on stats cards (colored top border)
    - Added data-testid attributes for E2E testing (strategy-detail-loading, strategy-detail-not-found, strategy-detail-page, strategy-detail-hero, strategy-detail-cover-image, strategy-detail-cover-placeholder, strategy-detail-heading, strategy-detail-actions, strategy-detail-edit-button, strategy-detail-stats, strategy-detail-form)
    - Updated loading skeleton to include hero banner skeleton
    - Terminal design styling with monospace fonts
- **Learnings for future iterations:**
  - For hero banners with cover images, use Next.js Image with fill and object-cover for responsive sizing
  - Gradient placeholder pattern: `linear-gradient(135deg, ${color}20 0%, ${color}05 50%, transparent 100%)`
  - Text readability on images: use gradient overlay (from-background/90 via-background/30 to-transparent) + text-shadow
  - Strategy color can be used as subtle accent on UI elements via inline styles (borderTopColor, boxShadow)
---

## 2026-01-18 - US-025
- What was implemented: Added "Share to Marketplace" section on strategy detail page
- Files changed:
  - `src/components/strategy/marketplace-section.tsx` - New component with three states:
    - Disabled state: shows progress bar (x/20 trades) when < MIN_TRADES_TO_PUBLISH
    - Publish form: anonymous toggle (custom switch), instrument/category multi-select popovers
    - Published state: Published badge, track record badge (verified/limited/normal), vote score, download count, marketplace link, unpublish button
  - `src/components/strategy/index.ts` - Exported MarketplaceSection
  - `src/app/(protected)/strategies/[id]/page.tsx` - Integrated MarketplaceSection between stats and form sections
- **Learnings for future iterations:**
  - For toggle switches without Shadcn Switch component, use a button with relative positioning and translate-x for the thumb
  - Track record status: verified (100+ trades), normal (30-99), limited (<30 trades) using constants from marketplace.ts
  - When fetching engagement data (votes, downloads) for published strategies, use marketplace.getById query with enabled: isPublic
  - cachedStats is stored as JSON string in DB, needs JSON.parse when using
---

## 2026-01-18 - US-026
- What was implemented: Created marketplace page route at /marketplace
- Files changed:
  - `src/app/(protected)/marketplace/page.tsx` - New page with:
    - Hero section with gradient orbs, Store icon, title "Strategy Marketplace", and subtitle
    - Infinite scroll using api.marketplace.list.useInfiniteQuery
    - useInfiniteScroll hook with sentinel ref for automatic loading
    - Loading skeleton with 6 placeholder cards
    - Empty state when no strategies available
    - Placeholder strategy card showing: cover image (Next.js Image), track record badge, creator info, stats row (win rate, profit factor, trades), engagement row (vote score, downloads), instrument badges
    - Terminal design styling (monospace fonts, chartreuse accent)
    - data-testid attributes: marketplace-page, marketplace-heading, marketplace-loading, marketplace-empty, marketplace-grid, marketplace-filter-placeholder, marketplace-strategy-card-{id}
- **Learnings for future iterations:**
  - For infinite scroll pages, use api.router.query.useInfiniteQuery with getNextPageParam
  - The useInfiniteScroll hook provides sentinelRef - place a div with ref={sentinelRef} after the grid
  - Use Next.js Image component with fill and sizes for responsive cover images
  - Placeholder components can be typed with interfaces that match the API response shape
  - Track record status badges: "verified" (100+), "limited" (<30), "normal" (30-99)
---

## 2026-01-18 - US-027
- What was implemented: Built marketplace filter bar component with search, filters, and sorting
- Files changed:
  - `src/components/marketplace/filter-bar.tsx` - New component with:
    - Search input with debounced search (300ms) using useRef timer
    - Instrument filter (multi-select Popover with Checkbox pattern)
    - Category filter (same multi-select pattern)
    - Sort dropdown (Most Voted, Most Downloaded, Most Recent)
    - Clear filters button (shows count when filters active)
    - URL persistence via useSearchParams, router.replace
    - Terminal design styling with monospace fonts
    - data-testid attributes for E2E testing
  - `src/components/marketplace/index.ts` - Exports FilterBar, MarketplaceFilters type, DEFAULT_MARKETPLACE_FILTERS
  - `src/app/(protected)/marketplace/page.tsx` - Integrated FilterBar, added filter state, connected to marketplace.list query params, wrapped in Suspense for useSearchParams
- **Learnings for future iterations:**
  - When using useSearchParams in a component, wrap it in Suspense at the page level
  - For URL sync useEffect that should only run once, use a ref (initializedRef) to prevent re-runs on filter state changes
  - Multi-select filter pattern: Popover with checkbox buttons, show count badge on trigger, "Clear Selection" button inside
  - Debounced search: use useRef for timer, handleSearchChange updates local input immediately, debounces filter/URL update
  - Keep sort preference when clearing filters (better UX)
---

## 2026-01-18 - US-028
- What was implemented: Built marketplace strategy card component with full voting UI
- Files changed:
  - `src/components/marketplace/strategy-card.tsx` - New component with:
    - Cover image with Next.js Image or gradient placeholder using strategy color
    - Track record badges: verified (100+ trades with ShieldCheck icon), limited (<30 trades with TriangleAlert icon)
    - Strategy name and truncated description (line-clamp-2)
    - Creator info with avatar or initial/Anonymous for anonymous strategies
    - Stats row: win rate (colored), profit factor (colored), total trades
    - Category badges (showing first 3 with +N overflow)
    - Vote controls with up/down arrows, optimistic updates, score display
    - Download count badge
    - Instrument badges (showing first 2 with +N overflow)
    - Hover effects and Terminal design styling
    - data-testid attributes for E2E testing
  - `src/components/marketplace/index.ts` - Added StrategyCard and MarketplaceStrategyData exports
  - `src/app/(protected)/marketplace/page.tsx` - Replaced PlaceholderStrategyCard with new StrategyCard component
- **Learnings for future iterations:**
  - Use `@/lib/shared` for cn utility, not `@/lib/utils`
  - For voting with optimistic updates: calculate score delta based on old vote vs new vote
  - If changing vote: delta = newVote - oldVote; If removing vote: newScore = oldScore - oldVote
  - Use onClick with e.preventDefault() and e.stopPropagation() on vote buttons to prevent card link navigation
  - Track record status is determined by totalTrades from cachedStats (verified >= 100, limited < 30, normal 30-99)
---

## 2026-01-18 - US-029
- What was implemented: Implemented voting UI with ownership detection and disabled state for own strategies
- Files changed:
  - `src/components/marketplace/strategy-card.tsx` - Extended VoteControls with isOwner prop (disables voting), size prop (sm/md for different contexts), exported as public for reuse
  - `src/components/marketplace/index.ts` - Exported VoteControls and VoteControlsProps
  - `src/server/api/routers/marketplace.ts` - Added currentUserId to list response for ownership detection
  - `src/app/(protected)/marketplace/page.tsx` - Pass currentUserId to StrategyCard for isOwner calculation
- **Learnings for future iterations:**
  - For ownership detection in marketplace cards, return currentUserId from API and compare with strategy.creator.id
  - Protected routes (/marketplace under (protected) layout) require authentication via Clerk middleware, so "login required" handling is not needed
  - Export reusable components (VoteControls) and their props interfaces for use in other pages (detail page)
  - Use size prop pattern (sm/md) for components that appear in multiple contexts with different visual needs
---

## 2026-01-18 - US-030
- What was implemented: Built marketplace strategy detail page at /marketplace/[id]
- Files changed:
  - `src/app/(protected)/marketplace/[id]/page.tsx` - New detail page with:
    - Hero section with cover image (or gradient placeholder), strategy name, creator info overlay
    - Track record badge (verified/limited/normal) on hero image
    - Action bar with back link, vote controls (reusing VoteControls), download count, download/view downloaded button
    - Instrument and category badges section
    - Performance stats grid with limited data warning banner
    - Description section
    - Strategy rules display (grouped by category: entry, exit, risk, management)
    - Risk parameters display (position sizing, max risk, daily loss, etc.)
    - Legacy entry/exit criteria display (if present)
    - Report dialog with reason dropdown and details textarea
  - `src/server/api/routers/marketplace.ts` - Added currentUserId to getById response for ownership detection
- **Learnings for future iterations:**
  - tRPC v11+ doesn't support onSuccess callback in useQuery options - use conditional state initialization in render instead
  - For vote state initialization: use a separate `voteInitialized` flag and set state in render if strategy data arrives
  - Reuse VoteControls component from marketplace/strategy-card.tsx with size="md" for larger contexts
  - For "Already Downloaded" detection, use strategies.getAll with select to find strategy with matching sourceStrategyId
  - For ownership check on detail pages, add currentUserId to the API response (consistent with list endpoint pattern)
---

## 2026-01-18 - US-031
- What was implemented: Added download confirmation dialog to marketplace detail page
- Files changed:
  - `src/app/(protected)/marketplace/[id]/page.tsx` - Added DownloadConfirmDialog component with:
    - Confirmation message explaining what will be copied
    - Checklist of benefits (rules copied, edits isolated, attribution preserved)
    - Cancel and Confirm Download buttons
    - Loading state with spinner during download
    - Strategy color accent on confirm button
    - Replaced direct download button with dialog trigger
- **Learnings for future iterations:**
  - Dialog components with mutation can stay open during action, then close on navigation (parent handles redirect)
  - Use CheckCircle icon for positive confirmation items in dialogs
  - When extending existing functionality, check what's already implemented (redirect and toast were already in place)
---

## 2026-01-18 - US-032
- What was implemented: Added Marketplace to sidebar navigation
- Files changed:
  - `src/app/(protected)/_components/app-sidebar.tsx` - Added Store icon import, added Marketplace nav item after Strategies, fixed isActive to use `pathname.startsWith()` for sub-route detection
- **Learnings for future iterations:**
  - Sidebar navigation uses `mainNavItems` array for nav items
  - For nested routes (e.g., /marketplace/[id]), use startsWith for active state detection
  - lucide-react has Store icon suitable for marketplace features
---

## 2026-01-18 - US-033
- What was implemented: Updated strategy cards to show marketplace status and engagement
- Files changed:
  - `src/components/strategy/strategy-card.tsx` - Added Public badge (Globe icon), Downloaded badge (Download icon), engagement stats section with votes and downloads
  - `src/server/api/routers/strategies.ts` - Extended getAll query to fetch engagement data (vote score, download count) for public strategies from strategyVotes and strategyDownloads tables
  - `src/app/(protected)/strategies/page.tsx` - Pass engagement prop to StrategyCard
- **Learnings for future iterations:**
  - Use `COALESCE(SUM(...), 0)` for vote aggregations to handle empty results
  - Conditional data fetching in map: only query engagement data if strategy.isPublic
  - Badge styling with chartreuse: bg-primary/20 text-primary for consistent accent
---

## 2026-01-18 - US-034
- What was implemented: Added source strategy attribution on strategy detail page
- Files changed:
  - `src/app/(protected)/strategies/[id]/page.tsx` - Added attribution section showing "Downloaded from Marketplace" badge, link to original strategy, creator info with avatar
- **Learnings for future iterations:**
  - Use marketplace.getById to fetch source strategy info (returns null if no longer public)
  - Handle both cases: original still public (show link + creator) vs no longer public (show message)
  - The "Downloaded" badge on cards was already implemented in US-033 via sourceStrategyId check
---

## 2026-01-19 - US-035
- What was implemented: Created reusable NullableNumberInput component for proper number input handling
- Files changed:
  - `src/components/ui/nullable-number-input.tsx` - New component with:
    - Internal string state while typing (allows intermediate states like "", "-", ".")
    - Validation on blur (not every keystroke) for intuitive editing
    - Properly handles 0, negative numbers, and empty (null) values
    - Toast errors with label context for invalid, min, max violations
    - Reverts to last valid value on validation error
    - Uses type="text" with inputMode="decimal" for mobile keyboards
    - Monospace font, placeholder styled as muted/50 for distinction
    - Syncs with external value changes via ref comparison
  - `src/components/ui/index.ts` - Created new index file exporting NullableNumberInput and NullableNumberInputProps
- **Learnings for future iterations:**
  - For number inputs that need intuitive editing, use internal string state + validate on blur pattern
  - type="text" + inputMode="decimal" gives better UX than type="number" for nullable number fields
  - Track lastValidValue via ref to revert on validation errors without re-renders
  - Track lastExternalValue via ref to sync internal state only when external value actually changes
  - Biome sorts type exports before value exports in export blocks
---

## 2026-01-19 - US-036
- What was implemented: Refactored RiskParametersEditor to use NullableNumberInput component
- Files changed:
  - `src/components/strategy/risk-parameters-editor.tsx` - Replaced all Input type=number with NullableNumberInput:
    - Changed state types from string to number|null for: fixedSize, riskPercent, kellyFraction, maxRiskValue, dailyLossValue, maxConcurrentPositions, minRRRatio
    - Simplified buildRiskParameters() to use direct null checks instead of parsing strings
    - Updated all 7 numeric inputs to use NullableNumberInput with proper label, min, max, step props
    - Autosave triggers on blur through NullableNumberInput's onChange callback
- **Learnings for future iterations:**
  - When refactoring from string state to number|null state, the buildRiskParameters() logic simplifies dramatically (no need for !Number.isNaN(Number(value)) checks)
  - NullableNumberInput handles all the edge cases internally (0, negatives, empty), so form code becomes cleaner
  - NullableNumberInput already has monospace font styling built in, so className="font-mono" not needed
  - The flex-1 className works with NullableNumberInput for inputs in flex containers (e.g., alongside a Select dropdown)
---

## 2026-01-19 - US-037
- What was implemented: Converted strategy detail page from editable form to read-only view
- Files changed:
  - `src/app/(protected)/strategies/[id]/page.tsx` - Major refactor:
    - Removed StrategyForm component and associated mutations (update, handleSubmit)
    - Added RulesDisplay component: groups rules by category (entry, exit, risk, management), displays ordered bullet points
    - Added RiskParametersDisplay component: shows position sizing, max risk, daily loss limit, max positions, min R:R as key-value pairs
    - Added Description section with whitespace-pre-wrap for proper formatting
    - Added instrument/category badges matching marketplace design pattern
    - Added legacy entry/exit criteria display for backwards compatibility
    - Edit button already existed in action bar linking to /strategies/[id]/edit
    - All display sections use terminal-wrapped styling (rounded border border-white/5 bg-white/2)
- **Learnings for future iterations:**
  - Reuse display component patterns from similar pages (marketplace detail has RulesDisplay and RiskParametersDisplay)
  - Keep the RulesDisplay and RiskParametersDisplay as local components in each page rather than extracting to shared components (matches current pattern)
  - For read-only views, conditional rendering with `{data && (...)}` keeps UI clean when sections are empty
  - The StrategyForm is still used in the edit page (/strategies/[id]/edit) - only removed from detail page
---

## 2026-01-19 - US-038
- What was implemented: Added duplicate confirmation modal with rename functionality
- Files changed:
  - `src/server/api/routers/strategies.ts` - Extended duplicate mutation input schema:
    - Added optional `name` parameter (z.string().min(1).max(100).optional())
    - If name provided, uses it; otherwise defaults to "[name] (Copy)"
  - `src/app/(protected)/strategies/[id]/page.tsx` - Added duplicate confirmation dialog:
    - Added Dialog, Input imports for modal components
    - Added duplicateOpen state and duplicateName state
    - Added useEffect to initialize name to "[strategy.name] (Copy)" when dialog opens
    - Changed Duplicate button to open dialog instead of immediately calling mutation
    - Created Dialog with name input, Cancel/Duplicate buttons
    - Added data-testid attributes: strategy-detail-duplicate-button, strategy-duplicate-dialog, strategy-duplicate-name-input, strategy-duplicate-confirm-button
    - Enter key support for quick submission
    - Strategy color accent on confirm button
- **Learnings for future iterations:**
  - For confirmation dialogs that need to pre-fill values, use useEffect with dialog open state as dependency
  - Dialog component from Shadcn provides controlled open/close behavior via open and onOpenChange props
  - Use onKeyDown on Input to support Enter key submission for better UX
  - Adding data-testid to all interactive elements enables E2E testing
---

## 2026-01-19 - US-039
- What was implemented: Added optimistic updates for strategy color changes
- Files changed:
  - `src/components/strategy/strategy-edit-form.tsx` - Enhanced autosave mutation with optimistic updates:
    - Added previousColorRef to track color for rollback
    - onMutate: cancel outgoing refetches, snapshot current color, update cache immediately with new color
    - onSuccess: clear previousColorRef after successful save
    - onError: revert cache to previous color, revert local state, clear ref
    - Maintains existing behavior for other fields (name, description, instruments, categories)
- **Learnings for future iterations:**
  - Optimistic updates follow pattern: cancel → snapshot → update → (rollback on error)
  - Use utils.strategies.getById.cancel() before optimistic updates to prevent race conditions
  - Use refs (not state) for rollback values to avoid triggering re-renders during mutation
  - Only apply optimistic updates when the specific field is being changed (check input.field !== undefined)
  - Invalidate cache on settled to ensure eventual consistency with server
---

## 2026-01-19 - US-040
- What was implemented: Fixed button nesting hydration error in StrategyEditForm
- Files changed:
  - `src/components/strategy/strategy-edit-form.tsx` - Refactored instrument/category selection:
    - Replaced button wrapping Checkbox with proper Checkbox + label pattern
    - Checkbox uses onCheckedChange handler (native Radix pattern)
    - Label uses htmlFor with unique ID for each checkbox
    - Biome-compliant: no role="button" on div, no button nesting
    - Maintains same UX: clicking label toggles checkbox
- **Learnings for future iterations:**
  - Radix Checkbox component renders as a <button>, so never wrap in another <button>
  - For multi-select with checkboxes, use Checkbox with id + label with htmlFor pattern
  - Checkbox onCheckedChange is the proper callback (not onClick on wrapper)
  - Biome lint/a11y/useSemanticElements rejects role="button" on div, use proper elements
---

## 2026-01-19 - US-041
- What was implemented: Redesigned strategies list page following marketplace design patterns
- Files changed:
  - `src/app/(protected)/strategies/page.tsx` - Complete hero section redesign:
    - Added hero section with gradient orbs (bg-primary/10, bg-accent/10 with blur-3xl)
    - Added icon badge (BookMarked + "Trading Playbook" label)
    - Moved "New Strategy" button into hero alongside heading
    - Added data-testid attributes: strategies-page, strategies-hero, strategies-heading, strategies-new-button, strategies-loading, strategies-empty, strategies-grid
    - Changed "Your Strategies" section heading to "All Strategies"
    - Fixed Infinity comparison to use Number.POSITIVE_INFINITY
    - Maintained responsive 1/2/3 column grid layout
- **Learnings for future iterations:**
  - Hero section pattern: relative container with overflow-hidden, absolute positioned gradient orbs, relative content wrapper
  - Gradient orb pattern: absolute positioned div with bg-color/10 blur-3xl rounded-full
  - Icon badge pattern: icon + uppercase tracking-wider text-xs span in flex row
  - Use Number.POSITIVE_INFINITY instead of Infinity for Biome compliance
---
