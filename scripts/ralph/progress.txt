# Ralph Progress Log - Daily Journal Feature
# Started: 2026-01-11
---

## Codebase Patterns
- Database schema: src/server/db/schema.ts (single source of truth)
- Constants: src/lib/constants/ (never hardcode values)
- tRPC routers: src/server/api/routers/
- UI components: src/components/ui/ (Shadcn + Terminal design)
- Colors: Chartreuse #d4ff00 (primary), Ice Blue #00d4ff (AI), Green #00ff88 (profit), Red #ff3b3b (loss)
- Always run `bun run check` and `bun run build` before committing

---

## Learnings

(Ralph will add learnings here as stories are completed)

---

## 2026-01-11 - US-001
- Added ID generators for daily journal entities (dj, ct, ja prefixes)
- Files changed: src/lib/shared/id.ts
- **Learnings for future iterations:**
  - ID generators follow pattern: `createId("prefix")` returning `prefix-{nanoid}`
  - All entity IDs have 2-character prefixes (us=user, ac=account, dj=dailyJournal, etc.)
---

## 2026-01-11 - US-002
- Added dailyJournals table schema with all required fields
- Files changed: src/server/db/schema.ts
- **Learnings for future iterations:**
  - Table pattern: use createTable() with third argument for indexes
  - User FK with cascade delete: `.references(() => users.id, { onDelete: "cascade" })`
  - Unique index uses uniqueIndex(), regular index uses index()
  - Relations added to usersRelations (many) and new entity relations (one back to user)
  - Type exports follow pattern: `export type Entity = typeof table.$inferSelect`
---

## 2026-01-11 - US-003, US-004, US-005
- Verified checklist templates, checklist checks, and journal attachments schemas already implemented
- US-003: dailyChecklistTemplates table with id, userId, text, order, isActive, createdAt fields
- US-004: dailyChecklistChecks table with journalId, templateId, checked, checkedAt (composite PK)
- US-005: journalAttachments table with id, journalId, url, key, filename, mimeType, size, caption, createdAt
- All relations properly configured in schema
- Files verified: src/server/db/schema.ts (already committed in previous iteration)
- **Learnings for future iterations:**
  - Composite primary key pattern: `primaryKey({ columns: [t.col1, t.col2] })`
  - Junction/join tables reference both parent tables with cascade delete
  - All tables need corresponding relations defined for Drizzle ORM
  - Type exports for both Select and Insert: `Entity` and `NewEntity`
---

## 2026-01-11 - US-006
- Pushed schema changes to database with `bun run db:push`
- All four tables created: dailyJournals, dailyChecklistTemplates, dailyChecklistChecks, journalAttachments
- Fixed biome config to exclude example/config files from linting
- Files changed: biome.jsonc
- **Learnings for future iterations:**
  - Biome exclude pattern: `"includes": ["**", "!folder", "!file.json"]` (add `**` first, then negations)
  - Long PostgreSQL identifier names are automatically truncated (notice, not error)
  - db:push is safe for new tables - no destructive changes
---

## 2026-01-11 - US-007
- Created dailyJournal router with getByDate query
- Files changed: src/server/api/routers/dailyJournal.ts, src/server/api/root.ts
- **Learnings for future iterations:**
  - tRPC router pattern: create file in routers/, export router, register in root.ts
  - Date normalization: use helper function to normalize to midnight UTC for date-based lookups
  - Auto-create pattern: check if exists, if not insert and re-fetch with relations
  - TypeScript: Drizzle returning() can return undefined, always guard with `if (!created)`
  - Queries can include nested relations with `with: { relation: { with: { nested: true } } }`
---

## 2026-01-11 - US-008
- Added updateContent mutation for upsert journal content
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - Upsert pattern: query first to check if exists, then update or insert accordingly
  - Mutations return the updated/created entity for optimistic updates on frontend
  - Remember to set updatedAt when updating existing records
---

## 2026-01-11 - US-009
- Added getRange query for fetching journals in a date range
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - Date range queries: use `gte` and `lte` from drizzle-orm for inclusive date ranges
  - For calendar display, transform data to include computed flags like `hasContent` to avoid sending full content
  - Use `columns` option in findMany to limit returned fields when full data isn't needed
---

## 2026-01-11 - US-010
- Added getWithTrades query that returns journal and trades for a specific date
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - For "within a day" queries, use `gte(field, normalizedDate)` AND `lt(field, nextDay)` instead of lte(endOfDay)
  - Combine auto-create pattern with related entity fetching in a single query method
  - Filter soft-deleted trades with `isNull(trades.deletedAt)`
  - Use `orderBy: (table, { asc }) => [asc(table.field)]` for query ordering
---

## 2026-01-11 - US-011
- Added checklist template CRUD operations: getTemplates, createTemplate, updateTemplate, deleteTemplate
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - User ownership validation pattern: query first to verify user owns entity before update/delete
  - Auto-order calculation: when order not provided, query existing items and use max(order) + 1
  - Partial update pattern: build updateData object with only provided fields, skip update if empty
  - All mutations should throw errors for "not found" cases rather than returning null
---

## 2026-01-11 - US-012
- Added reorderTemplates mutation for bulk order updates with transaction
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - Bulk validation pattern: query all owned items once, build Set of ids, check each input id against Set
  - Use db.transaction() for multi-row updates to ensure atomicity
  - Return updated data after bulk operations for frontend sync
---

## 2026-01-11 - US-013
- Added daily check operations: getChecks, toggleCheck, bulkUpdateChecks
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - Toggle pattern: if record exists toggle value, if not create with checked=true
  - Upsert pattern: check existence first, then update or insert
  - Track checkedAt timestamp - set to new Date() when checked, null when unchecked
  - Composite primary key access requires both columns in where clause
---

## 2026-01-11 - US-014
- Created integration tests for dailyJournal router core operations
- Files changed: tests/integration/daily-journal/journal.test.ts
- **Learnings for future iterations:**
  - Test file location: tests/integration/{feature}/{domain}.test.ts
  - setupTrader() provides user + account for basic test setup
  - Test date normalization by using different times on same day
  - Test user isolation by creating second user and verifying they can't access first user's data
  - Use createTestUser() directly for creating additional test users
---

## 2026-01-11 - US-015
- Created integration tests for checklist template and check operations
- Files changed: tests/integration/daily-journal/checklist.test.ts
- **Learnings for future iterations:**
  - Test toggle behavior across multiple calls (on -> off -> on)
  - Test ownership by having user2 try to access user1's templates
  - Use beforeAll in nested describe to set up fixtures for specific test groups
  - Test bulk operations by creating multiple entities and verifying each
---

## 2026-01-11 - US-016
- Set up S3 client utility using Bun's built-in S3Client
- Files changed: src/env.js, src/lib/storage/s3.ts
- **Learnings for future iterations:**
  - Use optional() for env vars that not all users will have configured
  - Lazy initialization pattern: store client in module-level variable, create on first getS3Client() call
  - Bun S3Client uses presign() for presigned URLs, supports PUT/GET methods
  - Export isS3Configured() helper for graceful degradation when S3 not set up
---

## 2026-01-11 - US-017
- Added getUploadUrl mutation for presigned PUT URL generation
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - Use nanoid for unique keys in S3 paths
  - Key format pattern: `{feature}/{userId}/{date}/{uuid}-{filename}`
  - Check isS3Configured() at start of mutation and throw descriptive error if not
  - Return both presignedUrl and key so frontend can use key for confirmUpload
---

## 2026-01-11 - US-018
- Added confirmUpload mutation to create journalAttachments records after S3 upload
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - Validate user ownership of parent entity (journal) before creating child records (attachment)
  - Use getPresignedDownloadUrl for URL stored in database - URLs expire, so frontend should regenerate as needed
  - Schema uses integer for size field, so z.number().int().positive() for validation
  - caption is nullable in schema, handle optional input with ?? null
---

## 2026-01-11 - US-019
- Added deleteAttachment mutation for removing journal attachments
- Files changed: src/server/api/routers/dailyJournal.ts
- **Learnings for future iterations:**
  - Validate child ownership through parent: query attachment with { with: { journal: true } }, then check journal.userId
  - Use same error message for "not found" and "not authorized" to avoid information leakage
  - Graceful S3 deletion: try/catch around deleteObject, log error but continue with DB deletion
  - S3 file may have already been deleted or never existed, so don't block database cleanup
---

## 2026-01-11 - US-020
- Installed Tiptap dependencies for rich text editor
- Files changed: package.json, bun.lock
- **Learnings for future iterations:**
  - Tiptap v3 packages: @tiptap/react, @tiptap/pm, @tiptap/starter-kit for core functionality
  - Extensions for specific features: @tiptap/extension-link, @tiptap/extension-image, @tiptap/extension-placeholder
  - Use `bun add` for installing multiple packages in one command
---

## 2026-01-11 - US-021
- Created daily-journal page layout with resizable panels
- Files changed: src/app/(protected)/daily-journal/page.tsx
- **Learnings for future iterations:**
  - ResizablePanelGroup pattern: use horizontal direction, set defaultSize on each panel
  - Panel size persistence: use localStorage with getStoredSizes/saveSizes helpers
  - Use onLayout callback to persist sizes, not direct state updates
  - Full height layout: use `h-[calc(100vh-4rem)]` to account for nav header
  - Panel constraints: use minSize/maxSize to prevent unusable layouts
  - Placeholder pattern: use placeholder cards for components not yet implemented
---

## 2026-01-11 - US-022
- Created DateNavigation component with prev/next day buttons, Today button, and date picker popover
- Files changed: src/components/daily-journal/date-navigation.tsx, src/components/ui/calendar.tsx, src/components/ui/popover.tsx, src/app/(protected)/daily-journal/page.tsx, package.json, bun.lock
- **Learnings for future iterations:**
  - react-day-picker v9 is used for calendar, integrates with Radix popover
  - Calendar component uses DayPicker with customized classNames for Terminal styling
  - Date navigation pattern: prev/next buttons + popover picker + Today shortcut
  - Use isToday() from date-fns to check if selected date is today
  - addDays() from date-fns for prev/next day navigation
  - format() from date-fns for date display formatting
---

## 2026-01-11 - US-023
- Created CalendarSidebar component with month grid view, P&L colors, and journal indicators
- Files changed: src/components/daily-journal/calendar-sidebar.tsx, src/app/(protected)/daily-journal/page.tsx
- **Learnings for future iterations:**
  - Use analytics.getCalendarData for P&L data (already includes date aggregation)
  - Use dailyJournal.getRange for journal indicators (hasContent flag)
  - Month grid pattern: use eachDayOfInterval() + getDay() to get days with padding
  - Stable React keys for weeks: use first day of week as key, not array index
  - Color intensity scaling: calculate maxAbsPnl for the visible month, then scale 0.25/0.5/0.75/1
  - Tooltip pattern: wrap day buttons in Tooltip with TooltipTrigger asChild
  - getDateStringInTimezone from lib/shared for timezone-aware date strings matching backend
---

