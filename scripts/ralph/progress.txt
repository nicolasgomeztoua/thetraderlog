# Ralph Progress Log
Started: Sat Mar 14 23:24:11 WET 2026
---

## Codebase Patterns
- Trigger.dev scheduled tasks use `schedules.task()` with `cron` property (not `task()`)
- Market data service exports `getOHLCBars()` and `hasCachedData()` from `@/lib/market-data/service`
- Base intervals stored in cache: "1min" and "1h" only — other intervals aggregate from these
- Biome requires `import type` before regular imports from the same module
- Use `db.selectDistinct()` for DISTINCT queries with Drizzle ORM

## 2026-03-14 - US-001
- Implemented daily market data pre-fetch scheduled task
- Files created:
  - `src/trigger/daily-market-data-prefetch.ts` — Trigger.dev scheduled task wrapper (cron 0 1 * * *)
  - `src/lib/market-data/prefetch.ts` — Core testable logic: `discoverSymbols()` + `prefetchMarketDataForAllSymbols()`
- Architecture: Core logic separated from Trigger.dev wrapper for testability
- Features: distinct symbol discovery, per-symbol retry with exponential backoff, concurrency batching (10), skip already-cached data, summary logging
- **Learnings for future iterations:**
  - `schedules.task()` uses `cron` property directly (not nested in schedule object)
  - `hasCachedData()` checks base interval cache — useful for skip logic
  - Biome enforces import type ordering: `import type` must come before value imports from same source
---

## 2026-03-14 - US-002
- Implemented integration tests for daily market data pre-fetch
- Files created:
  - `tests/integration/daily-market-data-prefetch.test.ts` — 7 tests covering discoverSymbols and prefetchMarketDataForAllSymbols
- Tests cover:
  - Distinct symbol extraction from trades table (with duplicates)
  - Empty symbol list (no trades) graceful handling
  - Both 1min and 1h intervals requested per symbol
  - Error handling: continues with remaining symbols when one fails
  - Skip already-cached symbols
  - Failed symbols reported in failedSymbols array
- **Learnings for future iterations:**
  - `getOHLCBars` catches fetch errors internally and returns `{ dataQuality: "unavailable" }` — to test error handling in prefetch, mock `getOHLCBars` directly rather than mocking `fetch`
  - `fetchWithRetry` uses exponential backoff with real timers — tests with retry failures take ~6s due to 1s+2s+4s backoff delays
  - Mock service functions with `vi.fn()` at module level + `vi.mock()` with lazy getters for clean reset between tests
---

## 2026-03-14 - US-003
- Implemented extended chart endpoint for 7 trading sessions of 1h bars
- Files changed:
  - `src/lib/market-data/service.ts` — Added `getExtendedDayBars()` function: fetches 1h bars for 3 days before entry through 3 days after exit (capped at today)
  - `src/server/api/routers/marketData.ts` — Added `getExtendedChartData` tRPC endpoint using the new service function
- Architecture: Same pattern as `getFullDayBars()` but with extended date range and 1h interval instead of 1min
- **Learnings for future iterations:**
  - `getFullDayBars` always fetches 1min, `getExtendedDayBars` always fetches 1h — both use `getOHLCBars()` internally
  - New service functions must be imported in the router file (add to the import block)
  - Pre-existing Biome errors in settings/import pages — don't try to fix unrelated files
---

## 2026-03-14 - US-004
- Implemented chart component 7-day view on 1h interval
- Files changed:
  - `src/components/trade-detail/tradingview-chart.tsx` — Added second tRPC query for `getExtendedChartData`, conditional on `interval === "1h"`
- Architecture: Two separate tRPC queries — `getFullDayChartData` for sub-hourly (with client-side aggregation), `getExtendedChartData` for 1h (server-side 1h bars, no aggregation needed)
- Both queries use `enabled` flag to only fetch when appropriate interval is selected
- Loading state derived from whichever query is active (`isHourly ? isLoadingHourly : isLoadingSubHourly`)
- Auto-fit recalculates on interval switch because the full useEffect re-runs when `interval` or `chartData` changes
- **Learnings for future iterations:**
  - `getExtendedChartData` returns same shape as `getFullDayChartData` — `{ bars, source, dataQuality, barCount }`
  - Use `enabled` flag on tRPC queries for conditional fetching — avoids wasted requests
  - The chart useEffect already depends on `interval` and `chartData`, so switching intervals triggers full re-render with auto-fit
---

## 2026-03-14 - US-005
- Implemented chart annotations schema and backend CRUD
- Files changed:
  - `src/lib/shared/id.ts` — Added `chartAnnotation: () => createId("ca")`
  - `src/server/db/schema.ts` — Added `annotationTypeEnum`, `lineStyleEnum`, `chartAnnotations` table with relations, type exports
  - `src/lib/constants/errors.ts` — Added `ERR_ANNOTATION_NOT_FOUND`, `ERR_ANNOTATION_CREATE_FAILED`
  - `src/server/api/routers/chartAnnotations.ts` — New router with `list`, `create`, `delete`, `clearAll` procedures
  - `src/server/api/root.ts` — Registered `chartAnnotationsRouter`
- Architecture: Ownership validation through trade ownership (user owns trade → can manage annotations on that trade). Delete validates ownership via `with: { trade: true }` pattern from AGENTS.md.
- **Learnings for future iterations:**
  - Biome requires imports in alphabetical order by path — insert new imports in the correct position
  - Validate child ownership through parent pattern: query child `with: { parent: true }`, check `child.parent.userId`
  - Use same error constant for "not found" and "not authorized" to avoid info leakage
---

## 2026-03-14 - US-006
- Implemented integration tests for chart annotations CRUD endpoints
- Files created:
  - `tests/integration/chart-annotations.test.ts` — 14 tests covering create, list, delete, clearAll with ownership validation
- Tests cover:
  - Create horizontal and vertical annotations with correct values
  - Default lineStyle and color when not provided
  - List annotations for a trade, empty result for trades with none
  - Delete single annotation by ID
  - ClearAll removes all annotations for a trade, doesn't affect other users
  - Ownership validation: rejects create/delete/list/clearAll when user does not own the trade
- **Learnings for future iterations:**
  - `setupTraderWithTrades()` returns `{ user, account, trades }` — use `trades[0].id` directly, don't go through `caller.trades.getAll()`
  - `trades.getAll({})` returns `{ items, nextCursor }` not `{ trades }` — it's a cursor-paginated endpoint
  - Tests in describe blocks within one file can build on each other (e.g., create tests populate data for list tests)
---

## 2026-03-14 - US-007
- Implemented chart drawing toolbar and horizontal line tool
- Files changed:
  - `src/components/trade-detail/tradingview-chart.tsx` — Added drawing toolbar (horizontal line, disabled vertical line placeholder, line style toggle, color cycling, clear all), annotation persistence via tRPC, keyboard shortcuts (H, Escape)
  - `src/components/trade-detail/content-panel.tsx` — Pass `tradeId` prop to TradingViewChart
- Architecture:
  - Drawing tool state tracked via React state + refs (refs for useEffect closure, state for UI re-renders)
  - `activeToolRef` used inside chart click handler to conditionally draw vs copy-to-clipboard
  - Annotations fetched via `chartAnnotations.list` query, rendered as price lines on chart mount
  - New annotations created immediately as visual price lines + persisted via `chartAnnotations.create` mutation
  - Clear all removes visual lines from `annotationPriceLinesRef` array + calls `chartAnnotations.clearAll`
  - `annotations` added to useEffect deps so chart re-renders when annotations data changes (e.g., after create/clearAll)
- **Learnings for future iterations:**
  - lightweight-charts lineStyle values: 0=Solid, 1=Dotted, 2=Dashed, 3=LargeDashed, 4=SparseDotted
  - Use refs to bridge React state into chart useEffect click handlers — state closures capture stale values
  - `biome-ignore lint/a11y/noNoninteractiveTabindex` needed for div elements that need keyboard focus for shortcuts
  - Toolbar buttons follow existing pattern: `bg-muted px-2 py-1 font-mono text-[10px]` with `min-h-[36px] min-w-[36px]` for mobile touch targets
  - Active tool highlighted with `bg-primary text-primary-foreground` matching timeframe selector pattern
---
