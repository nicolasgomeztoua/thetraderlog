# Ralph Progress Log
Started: Sun Feb 15 17:43:56 WET 2026
---

## 2026-02-15 - US-001: Define Structured Report Zod Schema
- Created `src/lib/ai/report-pipeline/report-schema.ts`
- Zod discriminated union on `type` field for 5 content block types: prose, metrics, chart, callout, image
- ChartComponent enum with all 12 chart/display components matching `src/components/mdx/components.tsx`
- MetricItem includes tooltip with what/why/benchmark, optional colorClass and description
- Top-level structuredReportSchema: executiveSummary, sections[], keyTakeaways[]
- All types exported via `z.infer<typeof schema>`
- Files changed: `src/lib/ai/report-pipeline/report-schema.ts` (new)
- **Learnings for future iterations:**
  - The ChartComponent enum must stay in sync with KNOWN_COMPONENTS in validator.ts and mdxComponents in components.tsx
  - RMultipleChart expects `{ buckets, stats }` shape, MonteCarloChart expects an object — other chart components expect arrays (see chart-wrappers.tsx)
  - Pre-existing Biome errors in prd.json and trigger.config.ts — not blocking
---

## 2026-02-15 - US-002: Add aiGenerateObject Wrapper to AI Client
- Added `aiGenerateObject<T>` wrapper function to `src/lib/ai/client.ts`
- Imported `generateObject` from `ai` and `type { z }` from `zod`
- New interfaces: `AiGenerateObjectOptions<T extends z.ZodType>` and `AiGenerateObjectResult<T>`
- Returns `{ object: z.infer<T>, totalTokens: number }` with same `mapToOpenRouterError` handling
- Files changed: `src/lib/ai/client.ts`
- **Learnings for future iterations:**
  - The AI SDK `generateObject` uses complex conditional return types (`InferSchema<T>`) that don't directly compose with `z.infer<T>` — use `as z.infer<T>` cast on `result.object`
  - `generateObject` result has `usage` (not `totalUsage` like `generateText`) — access via `result.usage?.inputTokens`
  - The wrapper follows the same pattern as `aiGenerateText` but without `tools`, `maxSteps`, `stopWhen`, or `onStepFinish` (not applicable to object generation)
---

## 2026-02-15 - US-003: Enhance store_report_data with Component Type Metadata
- Added `DataStoreEntry` interface and `DataStoreMap` type to `src/lib/ai/report-pipeline/report-schema.ts`
- Added optional `component` field to `store_report_data` tool input schema in `definitions.ts`
- Updated `executeStoreReportData` in `store-report-data.ts` to store `{ data, component?, description }` entries
- Changed `dataStore` type from `Map<string, unknown>` to `DataStoreMap` in: `definitions.ts`, `index.ts`, `gatherer.ts`, `writer-context.ts`, `generate-ai-report.ts`
- Updated `Object.fromEntries(dataStore)` serialization in trigger task to extract only the `data` field for DB persistence
- Updated `buildDataSummary` in `writer-context.ts` to show component type hints: `equity-data -> EquityCurve` instead of just `equity-data`
- Updated gatherer system prompt to instruct AI to always specify the `component` field
- Files changed: `report-schema.ts`, `definitions.ts`, `index.ts`, `store-report-data.ts`, `gatherer.ts`, `writer-context.ts`, `generate-ai-report.ts`
- **Learnings for future iterations:**
  - The `DataStoreEntry` and `DataStoreMap` types are in `report-schema.ts` — import from `@/lib/ai/report-pipeline/report-schema`
  - When accessing data from the enhanced dataStore, use `entry.data` for the actual data and `entry.component` for the component hint
  - The DB schema for `dataArtifacts` is unchanged — only the `data` field is serialized, component metadata is used during generation only
  - The `src/lib/ai/tools/index.ts` legacy dispatcher also needs updating when the dataStore type changes (easy to miss)
---

## 2026-02-15 - US-004: Update Writer Context for Structured JSON Output
- Rewrote `src/lib/ai/report-pipeline/writer-context.ts`
- Removed `MDX_CATALOG` constant entirely — the Zod schema (from US-001) defines available components, so a catalog is redundant
- Removed MDX-specific formatting rules (LaTeX escaping, pipe tables, code fences, MDX component instructions)
- Added JSON-specific formatting rules: markdown in prose blocks, no HTML tags, data citation rules, dataRef accuracy rules
- Kept Data Summary section unchanged (already enhanced with component type hints in US-003)
- Exported `COMPONENT_MINIMUMS` for reuse by the deterministic validator (US-007)
- Removed `MDX_CATALOG` from the `buildWriterContext` sections array — context is now ~60% smaller
- Files changed: `src/lib/ai/report-pipeline/writer-context.ts`
- **Learnings for future iterations:**
  - `COMPONENT_MINIMUMS` is now exported from `writer-context.ts` — the validator (US-007) should import it from there rather than duplicating thresholds
  - The writer context is now much smaller: Analysis Plan + Data Summary + Formatting Rules (no catalog)
  - Warning messages in `getDataSufficiencyWarning` were updated to say "metrics block or prose" instead of "MetricCards or prose" to match JSON schema terminology
---

## 2026-02-15 - US-005: Rewrite Writer Phase for Structured JSON Output
- Rewrote `src/lib/ai/report-pipeline/writer.ts` to use `aiGenerateObject` with `structuredReportSchema` instead of `aiGenerateText`
- `WriterResult` now returns `{ report: StructuredReport, tokensUsed }` instead of `{ content: string, tokensUsed }`
- Removed `dataStoreKeys` from `WriterOptions` — dataRef keys are now embedded in the writer context (from US-004)
- Removed `DATAREF_KEYS_PLACEHOLDER` replacement logic and the `buildWriterSystemPrompt` dataStoreKeys parameter
- Rewrote `WRITER_PERSONA` for JSON output (references "structured JSON report" not "MDX report")
- Rewrote `WRITER_INSTRUCTIONS` with: block type documentation, chart block rules, dataRef integrity rules, data accuracy rules, skip-chart guidance
- Updated `src/trigger/generate-ai-report.ts` minimally: removed `dataStoreKeys` from writer call, serialize `writerResult.report` to JSON string for downstream compatibility
- Files changed: `writer.ts`, `generate-ai-report.ts`
- **Learnings for future iterations:**
  - When changing a phase's return type, the trigger task (`generate-ai-report.ts`) is the main caller that needs updating to stay buildable
  - The `buildWriterSystemPrompt` function is now simpler: just joins persona + context + instructions + few-shot examples (no key list replacement)
  - Few-shot examples (US-006) still use MDX format — they need to be converted to JSON format to match the new schema
  - The validator (US-007) still expects `{ content: string, dataStoreKeys, model }` — will need updating when rewritten
---

## 2026-02-15 - US-006: Update Few-Shot Examples for Structured JSON Format
- Rewrote `src/lib/ai/report-pipeline/few-shot-examples.ts`
- Converted all 3 few-shot examples from MDX (`\`\`\`mdx` with `<MetricGrid>`, `<EquityCurve>`, etc.) to JSON (`\`\`\`json` with section objects)
- Performance Overview: prose → metrics (4 items) → chart (EquityCurve) → prose → callout (tip)
- Risk Analysis: prose → chart (DrawdownTable) → prose → chart (RMultipleChart) → prose → metrics (4 items) → chart (MonteCarloChart) → prose → callout (warning) → callout (tip)
- Sparse Data: prose → metrics (2 items) → prose → callout (note) → callout (warning) — demonstrates skipping chart blocks
- Updated WRITER_FEW_SHOT_EXAMPLES intro text: references JSON format, block types, section objects instead of MDX components
- Export name `WRITER_FEW_SHOT_EXAMPLES` unchanged for compatibility with writer.ts
- Files changed: `src/lib/ai/report-pipeline/few-shot-examples.ts`
- **Learnings for future iterations:**
  - The few-shot examples are string templates (not actual parsed JSON) — they're embedded in the system prompt as formatted JSON code blocks for the LLM to learn from
  - Each example shows a single section object (not a full report) — this is intentional to keep examples focused
  - The sparse data example is critical — it demonstrates the pattern for skipping chart blocks when data is insufficient (use metrics + prose instead)
  - Pre-existing Biome errors in archived PRD JSON and trigger.config.ts are not blocking — only 2 format issues in files not touched by this story
---

## 2026-02-15 - US-007: Create Deterministic Report Validator
- Rewrote `src/lib/ai/report-pipeline/validator.ts` — complete rewrite, not incremental
- Removed all AI repair logic: `attemptRepair`, `REPAIR_SYSTEM_PROMPT`, `buildRepairPrompt`, `MAX_REPAIR_ATTEMPTS`
- Removed MDX compilation check: `validateMdxCompilation`, `compileMDX` import, `sanitizeMdxProse` import
- Removed MDX regex parsing: `COMPONENT_REGEX`, `DATAREF_REGEX`, `parseComponents`, `validateComponents`, `validateDataRefs`, `KNOWN_COMPONENTS`, `DATAREF_COMPONENTS`
- New `runValidatorPhase` accepts `{ report: StructuredReport, dataStore: DataStoreMap }` — fully typed
- Returns `{ report: StructuredReport, warnings: string[] }` — always returns a valid report with inline fixes
- Function is now **synchronous** (no async needed — no LLM calls, no MDX compilation)
- For each chart block, validates 3 things in order:
  1. `dataRef` exists in `dataStore` → if not, replace with prose fallback
  2. Data shape is compatible with component (array for most, `{ buckets, stats }` for RMultipleChart, non-array object for MonteCarloChart)
  3. Data sufficiency using `COMPONENT_MINIMUMS` imported from `writer-context.ts`
- Updated `src/trigger/generate-ai-report.ts`:
  - Validator call is now synchronous (no `await`)
  - Passes `{ report: writerResult.report, dataStore }` instead of `{ content, dataStoreKeys, model }`
  - Uses `JSON.stringify(validatorResult.report)` for `finalContent`
  - Removed `writerContent` intermediate variable
  - Logs warnings instead of errors/valid status
- Files changed: `validator.ts`, `generate-ai-report.ts`
- **Learnings for future iterations:**
  - The validator is now synchronous — callers don't need `await`
  - `COMPONENT_MINIMUMS` is imported from `writer-context.ts` (shared between writer context and validator)
  - Data shape checking covers 3 categories: arrays (most charts), `{ buckets, stats }` (RMultipleChart), non-array objects (MonteCarloChart) — these match the patterns in `chart-wrappers.tsx`
  - Sufficiency checks use both dataRef patterns AND component name patterns to catch cases where naming doesn't match (e.g., custom dataRef names)
---

## 2026-02-15 - US-008: Build JSON Report Renderer Component
- Created `src/components/report/report-renderer.tsx` (client component)
- `ReportRenderer` accepts `{ report: StructuredReport, dataArtifacts: Record<string, unknown> }`
- Wraps content in `ReportDataProvider` (reuses existing provider from `src/components/mdx/provider.tsx`)
- Renders `executiveSummary` as markdown via `ReactMarkdown` with `remarkGfm` and Terminal-styled `markdownComponents`
- Each section gets an `<h2>` with `id` (from `toHeadingId`) for TOC anchor linking
- `BlockRenderer` dispatches on discriminated union `type`:
  - `prose`: ReactMarkdown with markdownComponents
  - `metrics`: MetricGrid + MetricCard (with tooltip support)
  - `chart`: CHART_COMPONENTS map reusing existing Mdx wrapper components
  - `callout`: Callout component with ReactMarkdown content
  - `image`: ChartImage component
- `keyTakeaways` rendered as numbered `<ol>` with inline markdown per item
- All rendered elements include `data-testid` attributes
- Stable keys derived from block content fields (no array index keys — Biome lint compliance)
- Files changed: `src/components/report/report-renderer.tsx` (new)
- **Learnings for future iterations:**
  - Biome strictly enforces `noArrayIndexKey` — derive keys from content fields (e.g., `block.type + block.dataRef` for chart blocks, `takeaway` string itself for list items)
  - The `CHART_COMPONENTS` map mirrors `mdxComponents` but maps `ChartComponentName` → Mdx wrapper components directly (no `as never` needed since types match `{ dataRef: string; className?: string }`)
  - `DataTable` from `src/components/mdx/data-table.tsx` uses `dataRef` prop (same interface as chart wrappers), so it fits in the CHART_COMPONENTS map
  - For inline markdown in list items (keyTakeaways), use `p: ({ children }) => <span>{children}</span>` to avoid block-level `<p>` inside `<li>`
---

## 2026-02-15 - US-009: Update Report Viewer Page for JSON Rendering
- Updated `src/app/(protected)/ai/reports/[reportId]/page.tsx`:
  - Removed `compileMDX` import from `next-mdx-remote/rsc`
  - Removed `sanitizeMdxProse` import
  - Removed `mdxComponents` import
  - Added JSON.parse with try/catch + basic shape check (sections array, executiveSummary string)
  - Falls back to ReactMarkdown for legacy MDX/markdown content
  - Passes parsed `StructuredReport` to `ReportViewerContent`
- Updated `src/app/(protected)/ai/reports/[reportId]/_components/report-viewer-content.tsx`:
  - Removed `mdxFailed`, `children`, `content` props
  - Added `report: StructuredReport` prop
  - Replaced regex-based `extractHeadings(content)` with `extractHeadingsFromSections(sections)` that reads from `report.sections[].heading`
  - Removed `ReactMarkdown` and `ReportDataProvider` imports (delegated to `ReportRenderer`)
  - Renders via `<ReportRenderer>` instead of inline MDX/markdown
  - TOC and IntersectionObserver heading tracking preserved
- Files changed: `page.tsx`, `report-viewer-content.tsx`
- **Learnings for future iterations:**
  - When migrating from MDX to JSON rendering, always add a legacy fallback (JSON.parse try/catch → ReactMarkdown) since existing reports in the DB are still MDX strings
  - The `toHeadingId` function is duplicated in `report-renderer.tsx`, `report-viewer-content.tsx`, and `markdown-components.tsx` — each needs to stay in sync
  - The shape check (`Array.isArray(sections) && typeof executiveSummary === 'string'`) is a quick guard against non-report JSON content; full Zod parsing would be heavier but more correct
---
