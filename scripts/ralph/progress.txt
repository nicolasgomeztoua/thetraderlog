# Ralph Progress Log
Started: Sat Mar  7 18:39:16 WET 2026
---

## Codebase Patterns
- Use Drizzle ORM for all database operations
- All constants go in src/lib/constants/ (never hardcode)
- Terminal design: monospace fonts, chartreuse accent (#d4ff00)
- Protected routes use tRPC protectedProcedure
- ID prefixes defined in src/lib/shared/id.ts — add new prefix when creating tables
- Pre-existing Biome errors in prd.json, pricing.tsx, settings-content.tsx, ai.ts — ignore these
- Use `date` type (mode: "string") for date-only fields (returns YYYY-MM-DD strings)

## 2026-03-07 - US-001
- Implemented aiUsage table in src/server/db/schema.ts
- Files changed: src/server/db/schema.ts, src/lib/shared/id.ts
- **Learnings for future iterations:**
  - Added `date` import from drizzle-orm/pg-core for date-only columns
  - Biome formatting: keep uniqueIndex args on one line when they fit (2 args)
  - Schema pushed with `bun run db:push` — clean apply
  - Must add many() reference in usersRelations when adding user-owned tables
---

## 2026-03-07 - US-002
- Created src/lib/constants/billing.ts with plan slugs, feature slugs, usage limits, plan metadata
- Added billing error messages to src/lib/constants/errors.ts
- Re-exported from src/lib/constants/index.ts
- Files changed: src/lib/constants/billing.ts (new), src/lib/constants/errors.ts, src/lib/constants/index.ts
- **Learnings for future iterations:**
  - Use `export *` in index.ts for domain-specific constant files (billing, sharing, etc.)
  - Biome auto-formats long string constants to multi-line — run format after adding
  - PlanMetadata interface + Record pattern works well for plan display data
---

## 2026-03-07 - US-003
- Created src/lib/billing/utils.ts with isBetaUser, hasFeatureAccess, hasPlanAccess, getEffectivePlan
- Files changed: src/lib/billing/utils.ts (new)
- **Learnings for future iterations:**
  - Clerk's auth().has() accepts { feature: string } or { plan: string } for billing checks
  - Used minimal interfaces (UserWithMetadata, AuthWithHas) instead of importing Clerk types directly — more testable
  - Beta bypass pattern: check isBetaUser() first, then fall through to Clerk's has()
  - getEffectivePlan checks plans in order: pro → starter → free (most privileged first)
---

## 2026-03-07 - US-004
- Added requireFeature() and requirePlan() middleware factories to src/server/api/trpc.ts
- Added starterProcedure and proProcedure as convenience exports
- Added ClerkAuthLike interface and clerkAuth to tRPC context
- Added clerkAuth override to TRPCContextOverrides for test mocking
- Files changed: src/server/api/trpc.ts
- **Learnings for future iterations:**
  - Clerk auth() session stored in context as `clerkAuth` (typed as ClerkAuthLike | null)
  - Tests without Clerk: pass `clerkAuth` override with mock `has()` to test entitlements
  - Tests without clerkAuth override: entitlement middleware denies access (safe default)
  - requireFeature/requirePlan return full procedures (protectedProcedure + entitlement check), use directly in router definitions
  - When userId override is provided, Clerk auth() call is skipped entirely to avoid errors in test env
---

## 2026-03-07 - US-005
- Created billing router src/server/api/routers/billing.ts with 4 endpoints: getCurrentPlan, getUsage, incrementChatUsage, incrementReportUsage
- Registered billing router in src/server/api/root.ts
- Files changed: src/server/api/routers/billing.ts (new), src/server/api/root.ts, src/lib/constants/index.ts (Biome auto-fix)
- **Learnings for future iterations:**
  - DB User type doesn't have Clerk's publicMetadata — cast `ctx.user as unknown as UserWithMetadata` when calling isBetaUser/getEffectivePlan
  - Drizzle upsert pattern: `.insert().values().onConflictDoUpdate({ target: [...], set: {} }).returning()`
  - For atomic increment + limit check: increment first, then check, rollback if over limit
  - `sql` template literal for atomic increment: `sql\`${table.column} + 1\``
  - Biome import ordering: `@/lib/billing/*` before `@/lib/constants/*` before `@/server/*`
---

## 2026-03-07 - US-006
- Gated AI router endpoints with entitlement checks and usage tracking
- sendMessage: requireFeature(FEATURE_AI_CHAT) + inline daily chat usage increment with rollback on limit exceeded
- startReport: requireFeature(FEATURE_AI_REPORTS) + inline monthly report usage increment with rollback on limit exceeded
- generatePdf: requireFeature(FEATURE_PDF_EXPORT) — no usage tracking needed
- Files changed: src/server/api/routers/ai.ts
- **Learnings for future iterations:**
  - `requireFeature(FEATURE_SLUG)` replaces `protectedProcedure` directly — it's a full procedure, not just middleware
  - Inline usage tracking pattern: same upsert+increment+rollback as billing router, avoids cross-router calls
  - Biome requires multi-line type casts when the inline type is long: `as unknown as {\n  publicMetadata?: ...\n}`
  - Use `bunx biome check --fix <file>` to auto-fix import ordering issues
---

## 2026-03-07 - US-007
- Gated trade, tag, and strategy router mutations with entitlement middleware
- Trade mutations (create, update, close, delete, deleteMany, restore, permanentDelete, emptyTrash, addExecution, deleteExecution, updateTrailingStop, updateRating, bulkUpdateRating, markReviewed, updateStrategy, bulkMarkReviewed, calculateMAEMFE, bulkCalculateMAEMFE, getUploadUrl, confirmUpload, deleteAttachment) → requireFeature(FEATURE_TRADE_MANAGEMENT)
- CSV import (batchImport) → requireFeature(FEATURE_CSV_IMPORT_EXPORT)
- Tag mutations (create, update, delete, addToTrade, removeFromTrade, bulkAddToTrades, bulkRemoveFromTrades) → requireFeature(FEATURE_CUSTOM_TAGS)
- Strategy mutations (create, update, delete, duplicate, checkRule, bulkCheckRules, syncGeneratedRules, updateRiskParameters, updateScalingRules, updateTrailingRules, evaluateTradeRules) → requireFeature(FEATURE_CUSTOM_STRATEGIES)
- All read-only queries remain as protectedProcedure (accessible to all authenticated users)
- Files changed: src/server/api/routers/trades.ts, src/server/api/routers/tags.ts, src/server/api/routers/strategies.ts
- **Learnings for future iterations:**
  - `requireFeature(FEATURE_SLUG)` is a drop-in replacement for `protectedProcedure` — same chaining API
  - Biome auto-fix needed after adding imports from `@/lib/constants/billing` (import ordering)
  - Keep queries ungated so free users retain read access to their data
---

## 2026-03-07 - US-008
- Created integration tests for billing router: tests/integration/billing.test.ts (15 tests)
- Extended test utils to support clerkAuth overrides: tests/utils/context.ts, tests/utils/caller.ts
- Tests cover: getCurrentPlan (free/pro/starter), getUsage (zero counts, beta null limits), incrementChatUsage (increment, daily limit, beta bypass), incrementReportUsage (increment, monthly limit, beta bypass), unauthenticated access rejection
- Files changed: tests/integration/billing.test.ts (new), tests/utils/context.ts, tests/utils/caller.ts
- **Learnings for future iterations:**
  - TRPCError thrown with `code: "FORBIDDEN"` — but `rejects.toThrow()` matches the *message*, not the code. Use the error constant (e.g., ERR_AI_CHAT_LIMIT_REACHED) instead of "FORBIDDEN"
  - Test utils now accept optional `clerkAuth` param: `createTestCaller(clerkId, user, { has: () => true })` — use this for entitlement testing
  - Beta user simulation: spread user object with `publicMetadata: { beta: true }` and cast `as unknown as User`
  - Pre-existing test failures: AI and trade tests fail because US-006/US-007 added entitlement gates but existing tests don't pass clerkAuth — US-009 should fix these
---

## 2026-03-07 - US-009
- Created tests/integration/entitlements.test.ts with 14 tests covering AI, trade, and strategy entitlement gates
- Added FULL_ACCESS_AUTH and NO_ACCESS_AUTH constants to tests/utils/caller.ts for convenience
- Fixed 7 existing test files broken by US-006/US-007 entitlement gates by adding FULL_ACCESS_AUTH:
  - ai.test.ts, report-viewer.test.ts, trades/batch-import.test.ts
  - strategy-rules-sync.test.ts, strategy-evaluation.test.ts, rule-relevance.test.ts, rule-compliance.test.ts
- Fixed ai.test.ts and report-viewer.test.ts: added beta metadata to bypass usage limits (these tests create 6+ reports exceeding the monthly limit)
- Fixed ai.test.ts hardcoded model string: replaced `"moonshotai/kimi-k2"` with `DEFAULT_CHAT_MODEL` constant
- Files changed: tests/integration/entitlements.test.ts (new), tests/utils/caller.ts, tests/utils/index.ts, plus 7 existing test files
- **Learnings for future iterations:**
  - Tests calling gated mutations MUST pass FULL_ACCESS_AUTH as 3rd arg to createTestCaller
  - Tests creating 6+ AI reports need beta metadata on user to bypass monthly usage limit
  - Platform enum values are: projectx, topstepx, ninjatrader, tradovate, rithmic, apex, other (NOT mt4/mt5)
  - requireFeature middleware doesn't pass user to hasFeatureAccess, so beta bypass via publicMetadata only works in billing/AI routers, not through middleware — tests simulate beta by passing FULL_ACCESS_AUTH
---

## 2026-03-07 - US-010
- Updated pricing page to wire Clerk checkout with plan-aware CTAs
- Updated PLAN_METADATA prices to match PRD ($10/mo Starter, $24/mo Pro)
- CTA logic: unauthenticated → SignUpButton with forceRedirectUrl, authenticated without plan → link to settings billing tab, authenticated with plan → "Current Plan" badge
- Used useAuth().has() for client-side plan detection
- Added "30-day free trial" badge on Pro plan
- Features list sourced from PLAN_METADATA constants
- Files changed: src/app/(marketing)/_components/pricing.tsx, src/lib/constants/billing.ts
- **Learnings for future iterations:**
  - PLAN_METADATA is typed as Record<string, PlanMetadata> — indexing returns `PlanMetadata | undefined`. Cast with `as PlanMetadata` when key is guaranteed
  - Clerk's `useAuth().has()` returns `HasFunction | undefined` — use optional chaining: `has?.({ plan: PLAN_PRO })`
  - Plan hierarchy pattern: define `const PLAN_HIERARCHY = [PLAN_FREE, PLAN_STARTER, PLAN_PRO] as const` and use indexOf for comparison
  - For checkout wiring without known Clerk checkout URLs, redirect to `/settings?tab=billing` as interim
---

## 2026-03-07 - US-011
- Created new BillingTab component at src/app/(protected)/settings/_components/billing-tab.tsx
- Added "Billing" tab to settings page (5th tab) with URL param support (?tab=billing)
- Current Plan card: shows plan name, price, beta badge, manage/upgrade CTA
- Usage meters: AI Chat (daily) and AI Reports (monthly) with color-coded progress bars (green < 50%, amber 50-80%, red > 80%)
- Plan comparison card: side-by-side view of Free/Starter/Pro with "Current" badge and upgrade CTAs
- Uses api.billing.getCurrentPlan and api.billing.getUsage queries
- Files changed: src/app/(protected)/settings/_components/billing-tab.tsx (new), src/app/(protected)/settings/_components/settings-content.tsx
- **Learnings for future iterations:**
  - Extracted billing tab as separate component to avoid bloating the 2170+ line settings-content.tsx
  - TabsList already had `sm:grid-cols-5` despite only having 4 tabs — adding 5th tab made it correct
  - useAuth().has() from Clerk returns `HasFunction | undefined` — use optional chaining `has?.({ plan: PLAN_PRO })`
  - Biome auto-reorders data-testid attributes alphabetically within JSX elements
  - UsageMeter component handles null limit (beta/unlimited) as a separate render path
---

## 2026-03-07 - US-012
- Created src/components/billing/upgrade-prompt.tsx with UpgradePrompt (wrapper) and UpgradeCardInline components
- Applied upgrade gates to: AI page (chat + reports), trade creation form, CSV import page, PDF export button
- UpgradePrompt wraps children and conditionally renders an upgrade card when access denied
- PDF export button uses inline access check (after all hooks) since hooks must not be conditional
- Files changed: src/components/billing/upgrade-prompt.tsx (new), src/app/(protected)/ai/page.tsx, src/app/(protected)/trade/new/page.tsx, src/app/(protected)/import/page.tsx, src/app/(protected)/ai/reports/[reportId]/_components/download-pdf-button.tsx
- **Learnings for future iterations:**
  - UpgradePrompt uses useAuth().has() + useUser().publicMetadata.beta for client-side gating
  - Cannot use early returns before hooks in React — place access checks after all hooks are called
  - FEATURE_CONFIG map centralizes feature metadata (title, description, icon, planRequired, isAiFeature)
  - Ice blue (#00d4ff) for AI features, chartreuse (#d4ff00) for core features in upgrade cards
  - Biome auto-fixes needed after adding imports (import ordering + formatting)
---

## 2026-03-07 - US-013
- Created src/components/billing/usage-limit-banner.tsx with UsageLimitBanner component and useChatLimitReached/useReportLimitReached hooks
- Integrated into chat-interface.tsx: banner above input + disabled send when daily limit reached
- Integrated into report-interface.tsx: banner above generate button + disabled button when monthly limit reached
- Files changed: src/components/billing/usage-limit-banner.tsx (new), src/app/(protected)/ai/_components/chat-interface.tsx, src/app/(protected)/ai/_components/report-interface.tsx
- **Learnings for future iterations:**
  - Custom hooks (useChatLimitReached, useReportLimitReached) wrap the billing.getUsage query for reuse across components
  - Amber-500 color for usage limit warnings (non-blocking, informational tone)
  - ChatInput accepts `disabled` prop — use it to prevent sending when limit reached
  - Biome auto-formats JSX short-circuit expressions: `{condition && (<Component />)}` becomes `{condition && <Component />}`
---

## 2026-03-07 - US-014
- Created tests/e2e/billing.spec.ts with 15 E2E tests covering billing UI flows
- Added data-testid attributes to pricing.tsx (pricing-section, pricing-card-{slug}, pricing-cta-{slug})
- Added data-testid="settings-tab-billing" to settings-content.tsx TabsTrigger
- Test coverage: settings billing tab (5 tests), pricing page (4 tests + 1 unauth), upgrade prompts (1 test), usage limit banners (2 tests)
- Files changed: tests/e2e/billing.spec.ts (new), src/app/(marketing)/_components/pricing.tsx, src/app/(protected)/settings/_components/settings-content.tsx
- **Learnings for future iterations:**
  - E2E tests dependent on tRPC data must wait for a data-dependent element before asserting on derived UI
  - beforeEach should wait for a content indicator (e.g., plan name text) not just container visibility
  - Pricing page CTAs vary by user state — test for any valid state rather than a specific one
  - Global setup can fail intermittently with ERR_ABORTED — this is a dev server startup flakiness, not a test issue
  - Playwright strict mode: use data-testid attributes, not CSS classes
---
