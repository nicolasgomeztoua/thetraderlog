# Ralph Progress Log
Started: Sat Jan 31 13:26:41 WET 2026
---

## Codebase Patterns
- Strategy types go in src/lib/strategy/ module
- Use discriminated unions with `type` field for condition types
- Export from index.ts barrel files
- Unit tests (no DB needed): `bunx vitest run --config vitest.config.unit.ts`
- Integration tests (needs Docker): `bun run test`

---

## 2026-01-31 - US-002
- What was implemented: Auto-evaluation type system for strategy rules
- Files changed:
  - src/lib/strategy/types.ts (created)
  - src/lib/strategy/index.ts (created)
- **Learnings for future iterations:**
  - Created discriminated union pattern for AutoCondition types
  - DataQuality enum matches schema's dataQualityEnum concept
  - All condition interfaces have a `type` discriminator field
---

## 2026-01-31 - US-003
- What was implemented: Rule generation service that transforms strategy config into checklist rules
- Files changed:
  - src/lib/strategy/rule-generator.ts (created)
  - src/lib/strategy/index.ts (updated barrel export)
- **Learnings for future iterations:**
  - Use `node:crypto` protocol for Node.js imports (Biome enforces this)
  - parseRLevelFromTrigger regex: `/\+?(\d+(?:\.\d+)?)\s*R\b/i` handles +1R, +1.5R, 2R patterns
  - Config interfaces (RiskParameters, ScalingRules, TrailingRules) are in component files, not lib
  - hashConfig uses MD5 first 16 chars - consistent format for change detection
  - Rule types: risk params → auto, scale-in → manual, scale-out → auto if R-level parseable, trailing fixed_ticks → semi_auto, ATR/swing → manual
---

## 2026-01-31 - US-004
- What was implemented: Unit tests for the rule generator service
- Files changed:
  - tests/unit/rule-generator.test.ts (created) - 32 tests covering all generator functionality
  - vitest.config.unit.ts (created) - separate config for unit tests without database
  - vitest.config.ts (updated) - added unit test path to includes
- **Learnings for future iterations:**
  - Unit tests don't need database - use separate vitest.config.unit.ts without globalSetup
  - Run unit tests with: `bunx vitest run --config vitest.config.unit.ts`
  - Integration tests still use: `bun run test` (requires Docker for PostgreSQL container)
  - Biome auto-organizes imports when running `bunx biome check --write`
---

## 2026-01-31 - US-005
- What was implemented: Added enabled toggle flags to RiskParameters interface
- Files changed:
  - src/components/strategy/risk-config.tsx (updated) - added enabled flags to interface
  - src/server/api/routers/strategies.ts (updated) - updated Zod schema
- **Learnings for future iterations:**
  - RiskParameters interface is in src/components/strategy/risk-config.tsx
  - Nested objects like positionSizing, maxRiskPerTrade, dailyLossLimit get `.enabled` property
  - Standalone fields like maxConcurrentPositions and minRRRatio use separate `*Enabled` boolean
  - Zod schema in strategies router must mirror the TypeScript interface exactly
---

## 2026-01-31 - US-006
- What was implemented: Toggle switches for "Track as rule" in Risk Config UI
- Files changed:
  - src/components/ui/switch.tsx (created) - Radix Switch component following Terminal design
  - src/components/strategy/risk-config.tsx (updated) - Added toggle switches to all field sections
  - package.json (updated) - Added @radix-ui/react-switch dependency
- **Learnings for future iterations:**
  - Switch component uses @radix-ui/react-switch - must install package first
  - Terminal design for Switch: border-white/20, bg-white/5, data-[state=checked]:bg-primary
  - When updating nested objects (positionSizing, maxRiskPerTrade, etc.), use spread operator to preserve existing fields like `enabled`
  - Biome enforces CSS class sorting - run check to see suggested fixes
---

## 2026-01-31 - US-007
- What was implemented: Added enabled toggle flags to ScalingRules interface
- Files changed:
  - src/components/strategy/scaling-config.tsx (updated) - added enabled?: boolean to scaleIn and scaleOut array items
  - src/server/api/routers/strategies.ts (updated) - updated Zod scalingRulesSchema
- **Learnings for future iterations:**
  - ScalingRules interface is in src/components/strategy/scaling-config.tsx
  - Array items (scaleIn/scaleOut rules) get their own `.enabled` property per item
  - This is different from RiskParameters where enabled flags were on parent objects or as separate booleans
---

## 2026-01-31 - US-008
- What was implemented: Toggle switches for each scale-in and scale-out rule row
- Files changed:
  - src/components/strategy/scaling-config.tsx (updated) - added Switch import and compact toggle to each rule row
- **Learnings for future iterations:**
  - For array item toggles, add a compact vertical layout: "Track" label + Switch below Size % input
  - Update the field union type to include "enabled" in update functions (updateScaleIn, updateScaleOut)
  - Reuse the existing Switch component from ui/switch.tsx
---

## 2026-01-31 - US-009
- What was implemented: Added enabled toggle flags to TrailingRules interface
- Files changed:
  - src/components/strategy/trailing-config.tsx (updated) - added enabled?: boolean to moveToBreakeven and trailStops array items
  - src/server/api/routers/strategies.ts (updated) - updated Zod trailingRulesSchema
- **Learnings for future iterations:**
  - TrailingRules interface is in src/components/strategy/trailing-config.tsx
  - moveToBreakeven is an optional object → add `.enabled` property inside it
  - trailStops is an array → each item gets its own `.enabled` property
  - Pattern matches ScalingRules: parent objects/arrays get enabled at the item level
---

## 2026-01-31 - US-010
- What was implemented: Toggle switches for "Track" in Trailing Config UI
- Files changed:
  - src/components/strategy/trailing-config.tsx (updated) - Added Switch import, toggles to breakeven and trail stop rows
- **Learnings for future iterations:**
  - Pattern for adding toggles matches scaling-config.tsx exactly: flex col with "Track" label above Switch
  - For move-to-breakeven: changed grid from 2 cols to 3 cols to accommodate toggle
  - For trail stop rules: add toggle between Method select and Delete button
  - Update function union type must include "enabled" (e.g., `field: "triggerR" | "method" | "value" | "enabled"`)
  - Update function value type must include boolean for enabled field
---

## 2026-01-31 - US-011
- What was implemented: tRPC endpoint to sync auto-generated rules with strategy config
- Files changed:
  - src/server/api/routers/strategies.ts (updated) - added syncGeneratedRules mutation
  - src/lib/strategy/rule-generator.ts (updated) - rule generator now respects enabled flags
  - tests/unit/rule-generator.test.ts (updated) - added enabled flags to test data, new tests for enabled behavior
- **Learnings for future iterations:**
  - Rule generator must check enabled flags before generating rules (e.g., `riskParams.maxRiskPerTrade?.enabled`)
  - For risk params: nested objects have `.enabled` inside, standalone fields use separate `*Enabled` boolean
  - For scaling/trailing: each array item has its own `.enabled` flag
  - syncGeneratedRules compares by configSource key and sourceConfigHash for change detection
  - Only isGenerated: true rules are synced; manual rules (isGenerated: false) are preserved
  - Return { added, updated, deleted } counts for visibility into sync operations
---

## 2026-01-31 - US-012
- What was implemented: Integrated rule sync into strategy create and update mutations
- Files changed:
  - src/server/api/routers/strategies.ts (updated) - added syncGeneratedRulesInternal helper, integrated into create/update/duplicate
- **Learnings for future iterations:**
  - Extract shared logic into internal helper functions for reuse across mutations
  - syncGeneratedRulesInternal takes db, strategyId, and config objects directly (no need to fetch strategy again)
  - When updating, preserve manual rules by only deleting isGenerated: false rules
  - Duplicate mutation must copy ALL rule properties including ruleType, configSource, autoCondition, isGenerated, sourceConfigHash
  - Import `PostgresJsDatabase` type from "drizzle-orm/postgres-js" for db parameter typing
  - Import schema types with `import type * as schema from "@/server/db/schema"` for generic typing
---

## 2026-01-31 - US-013
- What was implemented: Integration tests for rule syncing functionality
- Files changed:
  - tests/integration/strategy-rules-sync.test.ts (created) - 17 test cases covering all sync scenarios
- **Learnings for future iterations:**
  - Integration test pattern: beforeAll/afterAll with truncateAllTables(), setupTrader() or createTestUser() + createTestCaller()
  - Test file structure: describe blocks for each acceptance criterion, it() for specific test cases
  - Strategies router tests can use caller.strategies.create/update/getById to test full flow
  - Manual rule insertion for testing orphaned rules: use getTestDb() to insert directly via db.insert(schema.strategyRules)
  - Docker is REQUIRED for integration tests - testcontainers needs a running container runtime
  - Test that hash prevents updates by creating strategy, getting hash, updating with same values, verifying hash unchanged
  - Test config sections: riskParameters (nested .enabled), scalingRules (array item .enabled), trailingRules (mixed)
---

## 2026-01-31 - US-014
- What was implemented: Auto-Evaluation Engine with 6 core evaluators for strategy rules
- Files changed:
  - src/lib/strategy/evaluation.ts (created) - 548 lines with all evaluators and context interface
  - src/lib/strategy/index.ts (updated) - added evaluation export
- **Learnings for future iterations:**
  - Evaluation pattern: each evaluator returns AutoEvaluationResult with passed, actual, expected, details, dataQuality
  - Three result helpers: passedResult(), failedResult(), unavailableResult() for consistent response format
  - EvaluationContext provides: executions, dayTrades, concurrentTradesAtEntry, mfeR, accountBalance
  - TradeForEvaluation is a minimal interface with only fields needed for evaluation
  - calculatePlannedRR from src/lib/trades/calculations.ts is reusable for R:R calculation
  - getPointValue from src/lib/market-data handles both futures and forex point values
  - For breakeven trigger: if MFE doesn't reach trigger, rule passes as "not applicable"
  - For scale out: allow 10% price tolerance and 20% quantity tolerance for matching executions
  - trailingStopTrigger returns unavailable - it's semi_auto requiring manual verification
  - Exhaustive switch statement requires block scope: `default: { const _exhaustive: never = ... }`
---

## 2026-01-31 - US-015
- What was implemented: buildEvaluationContext helper to fetch all data required for auto-evaluation
- Files changed:
  - src/lib/strategy/evaluation.ts (updated) - added buildEvaluationContext and 4 helper functions
- **Learnings for future iterations:**
  - Use `typeof db from "@/server/db"` pattern for db type (same as helpers.ts uses)
  - Promise.all for parallel queries minimizes DB round trips
  - TradeWithAccount interface includes all fields needed for evaluation context building
  - Same-day trades query uses getDayBoundsInTimezone with user's timezone setting
  - Concurrent trades counted as: trades entered before this trade's entry AND (still open OR exited after entry)
  - Account balance comes from account.initialBalance - may want current balance in future
  - MFE in R already implemented as calculateMfeInR - just reused it
---

## 2026-01-31 - US-016
- What was implemented: evaluateTradeRules tRPC endpoint for auto-evaluating strategy rules against trade data
- Files changed:
  - src/server/api/routers/strategies.ts (updated) - added evaluateTradeRules mutation
- **Learnings for future iterations:**
  - Import buildEvaluationContext and evaluateAutoCondition from "@/lib/strategy" barrel
  - When upserting tradeRuleChecks, use onConflictDoUpdate on composite key [tradeId, ruleId]
  - Don't overwrite userOverride field when auto-evaluating - preserve user's manual override
  - Filter rules by `ruleType !== "manual"` to get auto/semi_auto rules
  - Parse autoCondition as JSON from rule record before passing to evaluateAutoCondition
  - Pass trade data as TradeForEvaluation interface (minimal subset) to evaluator
---

## 2026-01-31 - US-017
- What was implemented: Integration tests for strategy auto-evaluation engine
- Files changed:
  - tests/integration/strategy-evaluation.test.ts (created) - 23 test cases
- **Learnings for future iterations:**
  - Test pattern for evaluators: create strategy with enabled config, create trade with DB insert, call evaluateTradeRules, assert on results
  - For testing scale out evaluator, must create TradeExecution records via db.insert(schema.tradeExecutions)
  - For testing daily loss limit, create multiple trades on same day before the test trade
  - For testing concurrent positions, create trades with overlapping entry/exit times
  - Missing data tests: omit stopLoss/takeProfit/mfePrice fields to trigger dataQuality: 'unavailable'
  - Trade needs strategyId set to link to strategy, otherwise evaluateTradeRules throws "Trade has no strategy assigned"
  - Use getTestDb() and schema.trades for direct trade insertion when testing specific field combinations
  - Docker is REQUIRED for integration tests - testcontainers needs a running container runtime
---

## 2026-01-31 - US-018
- What was implemented: Rule type badges (AUTO, SEMI, MANUAL) for strategy rules display
- Files changed:
  - src/components/strategy/strategy-rules-display.tsx (updated) - added RuleTypeBadge component with RULE_TYPE_CONFIG
  - src/app/(protected)/strategies/[id]/page.tsx (updated) - formRules now includes ruleType
- **Learnings for future iterations:**
  - Use RULE_TYPE_CONFIG pattern for badge styling (label, icon, bgClass, textClass)
  - Rule interface must include ruleType as optional for backwards compatibility
  - When passing rules to display components, remember to include new fields from schema
  - Badge styling: inline-flex shrink-0 items-center gap-0.5 rounded px-1 py-0.5 font-mono text-[8px]
  - Test badges: data-testid={`rule-type-badge-${ruleType}`} for E2E testing
---

## 2026-01-31 - US-019
- What was implemented: Enhanced trade checklist with auto-evaluation display
- Files changed:
  - src/components/strategy/rule-checklist.tsx (updated) - Added auto-evaluation display, override functionality
  - src/server/api/routers/strategies.ts (updated) - checkRule mutation now supports userOverride field
- **Learnings for future iterations:**
  - Use parseEvaluationResult helper to safely parse JSON evaluation results from string
  - AutoEvaluationResult type from lib/strategy provides proper typing for evaluation results
  - For auto-evaluated rules: show CheckCircle (green) or XCircle (red) instead of checkbox
  - Override mode uses local useState to track which rules user wants to override
  - When overriding, pass userOverride: true to checkRule mutation
  - Disabled checkboxes need cursor-default on label, and htmlFor should be undefined
  - Pattern for conditional indicator: isAutoRule && isAutoEvaluated && !isInOverrideMode
  - data-testid convention for rule items: rule-checklist-item-{id}, rule-eval-passed-{id}, rule-eval-failed-{id}
  - RuleTypeBadge component can be reused - show OVERRIDDEN badge in orange when userOverride is true
---

## 2026-01-31 - US-020
- What was implemented: User override for auto-evaluated rules (badge update)
- Files changed:
  - src/components/strategy/rule-checklist.tsx (updated) - Fixed isOverridden badge to show when in override mode
- **Learnings for future iterations:**
  - Most override functionality was already implemented in US-019 (Override button, handleCheck with userOverride, checkRule mutation)
  - The missing piece was showing OVERRIDDEN badge when user enters override mode (not just when saved)
  - Badge condition should be: hasUserOverride (from server) OR isInOverrideMode (local state)
  - This provides immediate visual feedback when user clicks Override button
---

## 2026-01-31 - US-021
- What was implemented: Auto-evaluation triggered when trade closes
- Files changed:
  - src/server/api/routers/trades.ts (updated) - Added autoEvaluateTradeRules helper and integrated into close/update mutations
- **Learnings for future iterations:**
  - Use same pattern as strategies router's evaluateTradeRules - reuse buildEvaluationContext and evaluateAutoCondition
  - Helper function catches errors and logs them - trade close should NEVER fail due to evaluation errors
  - Check for strategyId before calling evaluation (skip trades without strategies)
  - For update mutation, check if isBeingClosed (status changing from non-closed to closed)
  - Import type `db as DbType` for Db type definition (same pattern as evaluation.ts)
  - Preserve userOverride field when upserting tradeRuleChecks - don't overwrite existing overrides
---

## 2026-01-31 - US-022
- What was implemented: E2E tests for strategy checklist flow
- Files changed:
  - tests/e2e/strategy-checklist.spec.ts (created) - 10 E2E tests covering full checklist flow
  - src/components/strategy/strategy-form.tsx (updated) - Added data-testid attributes
  - src/components/strategy/risk-config.tsx (updated) - Added data-testid to toggle and input
  - src/components/strategy/rule-checklist.tsx (updated) - Added data-testid to compliance section
- **Learnings for future iterations:**
  - data-testid naming: [component]-[element]-[qualifier] e.g., strategy-form-tab-risk
  - E2E tests should gracefully handle cases where data doesn't exist (use .isVisible() checks)
  - For tests that depend on existing data, use conditional logic to skip if no data exists
  - Playwright strict mode: always use data-testid for reliable selectors, never CSS classes
  - Use .first() when a testid might match multiple elements (e.g., autoBadge.first())
  - waitForURL with regex pattern is useful for dynamic route navigation checks
---
