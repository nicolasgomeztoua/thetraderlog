# Ralph Progress Log
Started: Fri Jan 16 20:44:45 +01 2026
---

## Codebase Patterns
- P&L utilities: `formatPnL`, `formatCurrency`, `getPnLColorClass` in `@/lib/shared/utils.ts`
- Trade stats: `calculateAggregateStats`, `calculateWinRate`, `calculateProfitFactor` in `@/lib/analytics/stats.ts`
- Date/time: Comprehensive timezone utilities in `@/lib/shared/timezone.ts` (formatDateInTimezone, getDayBoundsInTimezone, etc.)
- Dashboard already imports from `@/lib/shared` - reuse these instead of creating new formatters
- Streak API exists: `dailyJournal.getStreak` - returns consecutive journaling days
- Constants pattern: All constants belong in `src/lib/constants/` (never hardcode)
- Terminal design: monospace fonts, chartreuse accent (#d4ff00), ice blue for AI (#00d4ff)

---

## 2026-01-16 - US-000: Audit Existing Utilities for Dashboard Command Center
- Completed audit of existing utilities

### P&L Calculation Utilities (src/lib/shared/utils.ts:11-41)
- `formatCurrency(value, currency)` - Formats number as currency ($1,234.56)
- `formatPnL(value, currency)` - Formats with sign (+$1,234.56 or -$1,234.56)
- `getPnLColorClass(value)` - Returns text-profit, text-loss, or text-breakeven

### Date/Time Formatting Utilities (src/lib/shared/utils.ts:79-123 & timezone.ts)
- `formatDate(date, options, timezone?)` - Basic date formatting
- `formatTime(date, options, timezone?)` - Basic time formatting
- `formatDateTime(date, timezone?)` - Combined date+time
- `formatDateInTimezone(date, timezone, options)` - Timezone-aware date (for trade times)
- `formatLocalDate(date, formatStr)` - Calendar dates without TZ conversion
- `formatTimeInTimezone(date, timezone, options)` - Timezone-aware time
- `getDayBoundsInTimezone(dateString, timezone)` - UTC bounds for date queries

### Trade Statistics (src/lib/analytics/stats.ts)
- `calculateAggregateStats(trades, beThreshold)` - Returns wins/losses/breakevens/winRate/profitFactor/avgWin/avgLoss
- `calculateWinRate(wins, losses)` - Win percentage
- `calculateProfitFactor(grossProfit, grossLoss)` - PF ratio
- `parsePnl(netPnl)` - Safe string-to-number parsing
- `classifyTrade(pnl, beThreshold)` - Returns 'win', 'loss', or 'breakeven'

### Trade Calculations (src/lib/trades/calculations.ts)
- `calculatePoints(entry, exit, direction)` - Price movement
- `calculateTicks(points, symbol)` - Futures ticks
- `calculatePips(points, symbol)` - Forex pips
- `calculateAllStats(trade)` - All display stats for trade detail

### Existing Streak API (src/server/api/routers/dailyJournal.ts:1088)
- `dailyJournal.getStreak` - Returns current journaling streak count

### Dashboard Imports (src/app/(protected)/dashboard/page.tsx)
- Already using: `formatCurrency`, `getPnLColorClass`, `toDateString` from `@/lib/shared`
- `CircularProgress` component exists for gauges
- `WinLossBar` component for distribution visualization

### Files changed
- scripts/ralph/progress.txt (this file)

### Learnings for future iterations:
- ALWAYS reuse utilities from `@/lib/shared` - they handle edge cases (null, undefined, NaN)
- Use `@/lib/analytics/stats.ts` for aggregate calculations
- `dailyJournal.getStreak` already exists - no need to implement streak logic
- Dashboard page has helper components (CircularProgress, WinLossBar) that could be extracted to shared components if needed elsewhere
---

## 2026-01-16 - US-001: Add Economic Events Database Schema
- Added eventImpactEnum with values: low, medium, high
- Added economicEvents table with full schema:
  - id, name, currency, category, eventTime, actual, forecast, previous, impact, source, fetchedAt
- Added indexes on eventTime and currency columns for efficient queries
- Added unique constraint on (name, currency, eventTime) for upsert support
- Added economicEvent ID generator to src/lib/shared/id.ts ("ee" prefix)

### Files changed
- src/server/db/schema.ts (economicEvents table, eventImpactEnum, type exports)
- src/lib/shared/id.ts (economicEvent ID generator)

### Learnings for future iterations:
- When adding new tables, remember to also add type exports at the end of schema.ts
- Biome formatter requires multi-element arrays and function arguments to be on separate lines
- New ID generators need a unique 2-char prefix (ee for economicEvent)
---

## 2026-01-16 - US-002: Create Economic Calendar Constants
- Created src/lib/constants/economic-calendar.ts with:
  - EVENT_IMPACT_COLORS: high (loss/red), medium (yellow), low (muted) with bg/text/border classes
  - MAJOR_CURRENCIES: USD, EUR, GBP, JPY, CHF, AUD, CAD, NZD
  - HIGH_IMPACT_KEYWORDS: NFP, FOMC, CPI, GDP, etc. for impact classification
  - Type exports: EventImpact, MajorCurrency
- Updated src/lib/constants/index.ts with exports

### Files changed
- src/lib/constants/economic-calendar.ts (new file)
- src/lib/constants/index.ts (added exports)

### Learnings for future iterations:
- Biome requires `type` exports to be sorted after regular exports in the same block
- Use Terminal design system colors: text-loss for red, text-muted-foreground for gray
- Constants with 'as const' provide literal types for better type inference
---

## 2026-01-16 - US-003: Create Economic Calendar tRPC Router
- Created src/server/api/routers/economicCalendar.ts with two endpoints:
  - `getTodayEvents`: Returns events within today (UTC bounds), with optional currency/impact filters
  - `getUpcoming`: Returns events for next N hours (default 24), with configurable limit
- Both procedures use protectedProcedure for authentication
- Registered router in src/server/api/root.ts as `economicCalendar`

### Files changed
- src/server/api/routers/economicCalendar.ts (new file)
- src/server/api/root.ts (added import and registration)

### Learnings for future iterations:
- Use `getTodayBoundsUTC()` helper for day-based queries on UTC timestamps
- Biome prefers single-line Date.UTC calls when arguments fit - don't split across lines unnecessarily
- For optional input objects in tRPC, chain `.optional()` on the z.object() to allow calling without args
---

## 2026-01-16 - US-004: Add Recent Trades Endpoint
- Added getRecent query to src/server/api/routers/trades.ts
- Accepts limit (default 5, max 10) and optional accountId parameters
- Returns trades ordered by entryTime descending
- Uses getActiveAccountsSubquery helper when no accountId specified
- Includes account relation in response

### Files changed
- src/server/api/routers/trades.ts (added getRecent query)

### Learnings for future iterations:
- The getActiveAccountsSubquery helper is available from @/server/api/helpers for filtering to active accounts
- Use sql template literal to embed subqueries: `sql\`\${trades.accountId} IN (\${subquery})\``
- Dashboard quick view endpoints should be simple - just return data, let frontend format
---

## 2026-01-16 - US-005: Integration Tests for Economic Calendar Router
- Created tests/integration/economic-calendar/economic-calendar.test.ts with 19 tests
- Tested getTodayEvents:
  - Returns events within today's date range (excludes yesterday/tomorrow)
  - Orders events by eventTime ascending
  - Filters by currency (USD, EUR)
  - Filters by impact (high, medium, low)
  - Combines currency and impact filters
  - Returns empty array when no events match
- Tested getUpcoming:
  - Returns events within default 24 hours
  - Orders events by eventTime ascending
  - Respects limit parameter (1, 2, etc.)
  - Respects hoursAhead parameter
  - Combines hoursAhead and limit
  - Returns empty array when no upcoming events
- Tested empty results handling for both endpoints

### Files changed
- tests/integration/economic-calendar/economic-calendar.test.ts (new file)

### Learnings for future iterations:
- Use `getTestDb()` from utils to get raw Drizzle DB instance for direct inserts in tests
- Economic calendar tests insert directly to `schema.economicEvents` rather than using tRPC - no public API for creating events
- Test structure: insert test data in beforeAll, clear in afterAll with truncateAllTables
- For date-range tests, use the same date boundary logic as the router (getTodayBoundsUTC)
---

## 2026-01-16 - US-006: Integration Tests for Trades getRecent
- Created tests/integration/trades/get-recent.test.ts with 16 tests
- Tested ordering:
  - Returns trades ordered by entryTime descending (most recent first)
- Tested limit parameter:
  - Respects default limit of 5
  - Respects custom limit parameter (1, 3, 10)
  - Enforces maximum limit of 10
- Tested account filtering:
  - Filters by accountId when provided
  - Respects active accounts filter when no accountId specified (excludes inactive)
  - Includes trades from inactive account when explicitly filtered by accountId
- Tested empty results:
  - Returns empty array when no trades exist
  - Returns empty array when filtering by account with no trades
- Tested soft-deleted trades:
  - Excludes soft-deleted trades from results
- Tested account relation:
  - Includes account object in response with id and name

### Files changed
- tests/integration/trades/get-recent.test.ts (new file)

### Learnings for future iterations:
- Test file structure: create separate users/accounts per describe block for isolation when testing multiple scenarios
- The getRecent endpoint has a max limit of 10 - test should not exceed this
- Active accounts filter only applies when NO accountId is specified - explicit accountId bypasses the filter
- Use separate test users for isolation in empty results and soft-delete test scenarios
---

## 2026-01-16 - US-007: Create Trigger.dev Economic Calendar Sync Task
- Created src/trigger/sync-economic-calendar.ts with scheduled cron task (hourly at minute 0)
- Implemented JBlanked API fetch from /forex-factory/calendar/week/ endpoint
- Added impact classification using HIGH_IMPACT_KEYWORDS from constants
- Implemented database upsert using onConflictDoUpdate for (name, currency, eventTime) unique constraint
- Added graceful handling for missing JBLANKED_API_KEY - logs warning and skips sync
- Added JBLANKED_API_KEY to src/env.js as optional environment variable

### Files changed
- src/trigger/sync-economic-calendar.ts (new file)
- src/env.js (added JBLANKED_API_KEY as optional)

### Learnings for future iterations:
- Trigger.dev v3 uses `schedules.task()` for scheduled tasks, not regular `task()`
- For scheduled tasks, cron expression goes directly in the task definition, not in a separate config
- When accessing env vars in Trigger.dev tasks, use `process.env.X` directly rather than the env.js module (env validation may fail in task runtime)
- TypeScript strict mode requires explicit null checks for array index access and regex match groups
- Use onConflictDoUpdate with sql`EXCLUDED.column` syntax to update specific fields on upsert
- API times in ET need conversion to UTC - add 5 hours for EST (simplification)
---

## 2026-01-16 - US-009: Dashboard UI - Today's Snapshot Component
- Created TodaysSnapshot component in dashboard page
- Uses dailyJournal.getWithTrades API to fetch today's trades
- Calculates total P&L by summing trade.netPnl values
- Displays "Today: X trades, +$Y" or "No trades today" empty state
- P&L colored using getPnLColorClass (text-profit/text-loss)
- Loading skeleton shows while data is fetching
- Compact card styling with rounded border

### Files changed
- src/app/(protected)/dashboard/page.tsx (added TodaysSnapshot component and imports)

### Learnings for future iterations:
- Reuse existing utilities: formatPnL and getPnLColorClass from @/lib/shared
- The dailyJournal.getWithTrades returns trades with netPnl as string (decimal column) - use parseFloat
- Keep dashboard components simple and focused - single responsibility
---

## 2026-01-16 - US-010: Dashboard UI - Economic Calendar Widget
- Created EconomicCalendarWidget component in dashboard page
- Fetches events using economicCalendar.getTodayEvents API (no filters, shows all events)
- Displays up to 5 events with: currency badge, event name, and time
- Color-codes by impact using EVENT_IMPACT_COLORS from @/lib/constants (high=red, medium=yellow, low=gray)
- Shows "No high-impact events today" empty state when no events
- Terminal-style card with header showing "Economic Calendar" and ClockIcon
- Loading skeleton with 3 placeholder items

### Files changed
- src/app/(protected)/dashboard/page.tsx (added EconomicCalendarWidget component, ClockIcon, EVENT_IMPACT_COLORS import)

### Learnings for future iterations:
- Import constants from @/lib/constants barrel export
- Use nullish coalescing for safe access: EVENT_IMPACT_COLORS[event.impact] ?? EVENT_IMPACT_COLORS.low
- Template literals for dynamic className: `${impactColors.bg} ${impactColors.text}`
- Use divide-y divide-border for clean separation between list items
---

## 2026-01-16 - US-011: Dashboard UI - Recent Trades Widget
- Created RecentTradesWidget component in dashboard page
- Fetches trades using trades.getRecent API with limit 5
- Displays each trade with: direction badge (LONG/SHORT), symbol, date, P&L
- Direction badge colored: long=green (bg-profit/20 text-profit), short=red (bg-loss/20 text-loss)
- P&L colored using getPnLColorClass utility
- Each trade row is clickable Link, navigates to /journal/{tradeId}
- View All link in header navigates to /journal with ArrowRightIcon
- Empty state: "No trades yet. Log your first trade" with link to /trade/new
- Terminal-style card with TrendingUpIcon header
- Two-column grid layout: Economic Calendar + Recent Trades on md breakpoint and above

### Files changed
- src/app/(protected)/dashboard/page.tsx (added RecentTradesWidget, updated layout to two-column grid, added imports)

### Learnings for future iterations:
- Use formatDateInTimezone for trade dates with format option (e.g., "MMM d")
- Two-column responsive grid: `grid grid-cols-1 gap-6 md:grid-cols-2`
- Biome requires sorted CSS classes (font-medium before font-mono)
- Link component from next/link works well for clickable trade rows with hover:bg-muted/50
---
