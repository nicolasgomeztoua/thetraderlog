# Ralph Progress Log
Started: Sat Jan 31 13:26:41 WET 2026
---

## Codebase Patterns
- Strategy types go in src/lib/strategy/ module
- Use discriminated unions with `type` field for condition types
- Export from index.ts barrel files
- Unit tests (no DB needed): `bunx vitest run --config vitest.config.unit.ts`
- Integration tests (needs Docker): `bun run test`

---

## 2026-01-31 - US-002
- What was implemented: Auto-evaluation type system for strategy rules
- Files changed:
  - src/lib/strategy/types.ts (created)
  - src/lib/strategy/index.ts (created)
- **Learnings for future iterations:**
  - Created discriminated union pattern for AutoCondition types
  - DataQuality enum matches schema's dataQualityEnum concept
  - All condition interfaces have a `type` discriminator field
---

## 2026-01-31 - US-003
- What was implemented: Rule generation service that transforms strategy config into checklist rules
- Files changed:
  - src/lib/strategy/rule-generator.ts (created)
  - src/lib/strategy/index.ts (updated barrel export)
- **Learnings for future iterations:**
  - Use `node:crypto` protocol for Node.js imports (Biome enforces this)
  - parseRLevelFromTrigger regex: `/\+?(\d+(?:\.\d+)?)\s*R\b/i` handles +1R, +1.5R, 2R patterns
  - Config interfaces (RiskParameters, ScalingRules, TrailingRules) are in component files, not lib
  - hashConfig uses MD5 first 16 chars - consistent format for change detection
  - Rule types: risk params → auto, scale-in → manual, scale-out → auto if R-level parseable, trailing fixed_ticks → semi_auto, ATR/swing → manual
---

## 2026-01-31 - US-004
- What was implemented: Unit tests for the rule generator service
- Files changed:
  - tests/unit/rule-generator.test.ts (created) - 32 tests covering all generator functionality
  - vitest.config.unit.ts (created) - separate config for unit tests without database
  - vitest.config.ts (updated) - added unit test path to includes
- **Learnings for future iterations:**
  - Unit tests don't need database - use separate vitest.config.unit.ts without globalSetup
  - Run unit tests with: `bunx vitest run --config vitest.config.unit.ts`
  - Integration tests still use: `bun run test` (requires Docker for PostgreSQL container)
  - Biome auto-organizes imports when running `bunx biome check --write`
---

## 2026-01-31 - US-005
- What was implemented: Added enabled toggle flags to RiskParameters interface
- Files changed:
  - src/components/strategy/risk-config.tsx (updated) - added enabled flags to interface
  - src/server/api/routers/strategies.ts (updated) - updated Zod schema
- **Learnings for future iterations:**
  - RiskParameters interface is in src/components/strategy/risk-config.tsx
  - Nested objects like positionSizing, maxRiskPerTrade, dailyLossLimit get `.enabled` property
  - Standalone fields like maxConcurrentPositions and minRRRatio use separate `*Enabled` boolean
  - Zod schema in strategies router must mirror the TypeScript interface exactly
---

## 2026-01-31 - US-006
- What was implemented: Toggle switches for "Track as rule" in Risk Config UI
- Files changed:
  - src/components/ui/switch.tsx (created) - Radix Switch component following Terminal design
  - src/components/strategy/risk-config.tsx (updated) - Added toggle switches to all field sections
  - package.json (updated) - Added @radix-ui/react-switch dependency
- **Learnings for future iterations:**
  - Switch component uses @radix-ui/react-switch - must install package first
  - Terminal design for Switch: border-white/20, bg-white/5, data-[state=checked]:bg-primary
  - When updating nested objects (positionSizing, maxRiskPerTrade, etc.), use spread operator to preserve existing fields like `enabled`
  - Biome enforces CSS class sorting - run check to see suggested fixes
---

## 2026-01-31 - US-007
- What was implemented: Added enabled toggle flags to ScalingRules interface
- Files changed:
  - src/components/strategy/scaling-config.tsx (updated) - added enabled?: boolean to scaleIn and scaleOut array items
  - src/server/api/routers/strategies.ts (updated) - updated Zod scalingRulesSchema
- **Learnings for future iterations:**
  - ScalingRules interface is in src/components/strategy/scaling-config.tsx
  - Array items (scaleIn/scaleOut rules) get their own `.enabled` property per item
  - This is different from RiskParameters where enabled flags were on parent objects or as separate booleans
---

## 2026-01-31 - US-008
- What was implemented: Toggle switches for each scale-in and scale-out rule row
- Files changed:
  - src/components/strategy/scaling-config.tsx (updated) - added Switch import and compact toggle to each rule row
- **Learnings for future iterations:**
  - For array item toggles, add a compact vertical layout: "Track" label + Switch below Size % input
  - Update the field union type to include "enabled" in update functions (updateScaleIn, updateScaleOut)
  - Reuse the existing Switch component from ui/switch.tsx
---

## 2026-01-31 - US-009
- What was implemented: Added enabled toggle flags to TrailingRules interface
- Files changed:
  - src/components/strategy/trailing-config.tsx (updated) - added enabled?: boolean to moveToBreakeven and trailStops array items
  - src/server/api/routers/strategies.ts (updated) - updated Zod trailingRulesSchema
- **Learnings for future iterations:**
  - TrailingRules interface is in src/components/strategy/trailing-config.tsx
  - moveToBreakeven is an optional object → add `.enabled` property inside it
  - trailStops is an array → each item gets its own `.enabled` property
  - Pattern matches ScalingRules: parent objects/arrays get enabled at the item level
---

## 2026-01-31 - US-010
- What was implemented: Toggle switches for "Track" in Trailing Config UI
- Files changed:
  - src/components/strategy/trailing-config.tsx (updated) - Added Switch import, toggles to breakeven and trail stop rows
- **Learnings for future iterations:**
  - Pattern for adding toggles matches scaling-config.tsx exactly: flex col with "Track" label above Switch
  - For move-to-breakeven: changed grid from 2 cols to 3 cols to accommodate toggle
  - For trail stop rules: add toggle between Method select and Delete button
  - Update function union type must include "enabled" (e.g., `field: "triggerR" | "method" | "value" | "enabled"`)
  - Update function value type must include boolean for enabled field
---

## 2026-01-31 - US-011
- What was implemented: tRPC endpoint to sync auto-generated rules with strategy config
- Files changed:
  - src/server/api/routers/strategies.ts (updated) - added syncGeneratedRules mutation
  - src/lib/strategy/rule-generator.ts (updated) - rule generator now respects enabled flags
  - tests/unit/rule-generator.test.ts (updated) - added enabled flags to test data, new tests for enabled behavior
- **Learnings for future iterations:**
  - Rule generator must check enabled flags before generating rules (e.g., `riskParams.maxRiskPerTrade?.enabled`)
  - For risk params: nested objects have `.enabled` inside, standalone fields use separate `*Enabled` boolean
  - For scaling/trailing: each array item has its own `.enabled` flag
  - syncGeneratedRules compares by configSource key and sourceConfigHash for change detection
  - Only isGenerated: true rules are synced; manual rules (isGenerated: false) are preserved
  - Return { added, updated, deleted } counts for visibility into sync operations
---

## 2026-01-31 - US-012
- What was implemented: Integrated rule sync into strategy create and update mutations
- Files changed:
  - src/server/api/routers/strategies.ts (updated) - added syncGeneratedRulesInternal helper, integrated into create/update/duplicate
- **Learnings for future iterations:**
  - Extract shared logic into internal helper functions for reuse across mutations
  - syncGeneratedRulesInternal takes db, strategyId, and config objects directly (no need to fetch strategy again)
  - When updating, preserve manual rules by only deleting isGenerated: false rules
  - Duplicate mutation must copy ALL rule properties including ruleType, configSource, autoCondition, isGenerated, sourceConfigHash
  - Import `PostgresJsDatabase` type from "drizzle-orm/postgres-js" for db parameter typing
  - Import schema types with `import type * as schema from "@/server/db/schema"` for generic typing
---

## 2026-01-31 - US-013
- What was implemented: Integration tests for rule syncing functionality
- Files changed:
  - tests/integration/strategy-rules-sync.test.ts (created) - 17 test cases covering all sync scenarios
- **Learnings for future iterations:**
  - Integration test pattern: beforeAll/afterAll with truncateAllTables(), setupTrader() or createTestUser() + createTestCaller()
  - Test file structure: describe blocks for each acceptance criterion, it() for specific test cases
  - Strategies router tests can use caller.strategies.create/update/getById to test full flow
  - Manual rule insertion for testing orphaned rules: use getTestDb() to insert directly via db.insert(schema.strategyRules)
  - Docker is REQUIRED for integration tests - testcontainers needs a running container runtime
  - Test that hash prevents updates by creating strategy, getting hash, updating with same values, verifying hash unchanged
  - Test config sections: riskParameters (nested .enabled), scalingRules (array item .enabled), trailingRules (mixed)
---
