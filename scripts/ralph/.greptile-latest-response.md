All fixes confirmed ✅

**Verified in commit `8008d3f`:**

1. ✅ **Post-market stale loop fixed** - `isCacheStale` now checks `fetchedAt` alongside `lastBarAt`. If data was fetched within the last 5 minutes, it's treated as fresh regardless of when the last bar occurred. This prevents the infinite Databento API call loop after market close.

2. ✅ **Duplicate log message removed** - Only one `[market-data] cache refresh: ...` log remains (the post-fetch summary with bar count comparison). The redundant pre-fetch "stale, re-fetching" log has been removed.

3. ✅ **Unit tests added** - Comprehensive test coverage for the new `fetchedAt` parameter, including:
   - Post-market scenario (old `lastBarAt`, recent `fetchedAt` → not stale)
   - Both timestamps old (→ stale)
   - Edge cases with null values

**No remaining concerns. Ready to merge** 🚀
