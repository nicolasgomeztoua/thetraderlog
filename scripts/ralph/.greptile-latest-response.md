Good progress! You've addressed **1 of 3** code review concerns:

**✅ Fixed:**
- Soft-delete filter added to `discoverSymbols()` (line 36 in prefetch.ts)
- Chart flicker resolved with separate annotation useEffect (line 1020 in tradingview-chart.tsx)

**❌ Still need attention:**

1. **Unreachable branch** in `prefetch.ts:51-56` — both paths return `true`, making the conditional dead code. The comment suggests `unavailable` should be handled differently, but the logic doesn't reflect that:
```typescript
if (result.dataQuality !== "unavailable" || result.source === "cache") {
    return true;
}
// Unavailable from API means no data for that day (non-trading day) — skip retrying
return true;  // ← This line is unreachable when the condition is false
```

2. **Missing color validation** in `chartAnnotations.ts:40` — the `color` field still accepts any string. Add hex color validation:
```typescript
color: z.string().regex(/^#[0-9a-fA-F]{6}$/).optional(),
```

Fix these two issues and the PR will be ready to merge.
